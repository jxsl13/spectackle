schema: v1
langs: [go]
ignore:
  - ".git/**"
  - "bin/**"
verify:
  - "go build ./..."
  - "go test ./..."
budget_default: 2000
compact:
  journal_max: 500
  done_max: 8
feedback:
  # require per ADR-01KYES0TM; parked at warn until T-01KYD9J lands
  # per-finding waivers - the first armed landing hit four TRUE
  # target-divergence findings with no waiver path and no target-amendment
  # tool (B-01KYER). Re-arming require is T-01KYD9J's exit criterion.
  validate: warn
