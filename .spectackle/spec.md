---
schema: v1
prefix: SPX
---

# spectacle — living spec (root scope)

## intent
spectacle is a token-efficient, spec-driven MCP server: cross-language AST
maps, cascading EARS contracts and a git-native spec lifecycle, fused so an
LLM plans against structure and contracts instead of file contents.
- P-01KY8RVMZRE7QS4ZSK152YJ4DA dedicated unit tests with end-to-end coverage for every implementation: every package now carries a dedicated end-to-end unit test; concurrency race in draft found and fixed with a server mutex + regression test
- P-01KY8XJ7B8FG5RDFJSX3X02B75 M1 structural core: go/parser indexer + cgo edges: go/parser indexer live: 3-layer saxpy chain reproduces, anchors real (span+hash), find/get graph-backed
- P-01KY8ZE78REN7V2DXQC1CFHV7H streamable HTTP transport: spectacle as resident localhost service: serve -http: Streamable HTTP via official SDK handler, resident localhost service; stdio default unchanged
- P-01KY8ZE78RE619GSDH193NS07V orchestrator + fresh cheap subagents: document and embed the swarm workflow: orchestrator + fresh cheap implementer workflow documented (README, docs/agent-workflow.md) and embedded in server instructions
- P-01KY90J2QGFZ7RKXC987Y00BT1 Plan 9 asm chain live: AsmParser nodes + go<->asm EAsm edges: plan9 asm chain live: internal/plan9 scanner pkg, AsmParser nodes, EAsm edges
- P-01KY90J2QGF2BBK7YMBP1A2ZAB forward-skip state machine: every forward jump is one move call: forward-skip state machine: total order, any forward jump one call, Mermaid automaton in README
- P-01KY91BP28F4SSVZRYVE9H90NM persistent parse-blob store: warm graph start across sessions (M2 slice): sqlite parse-blob store: second IndexAll = zero parses, wired per active root
- T-01KY91BP28ESFB5H8M35J4EEP8 gofmt legacy debt: five files: gofmt debt: 5 files whitespace-only; rest (sync.go, sync_test.go, workspace.go) noted
- T-01KY922T9REGS987EJEHRCXAFX docs refresh: status table, saxpy transcript footnote, roadmap ticks: docs live-status verified against running graph
- T-01KY922T9RE34A5FJCAPJY0WCZ gofmt rest: sync.go, sync_test.go, workspace.go: gofmt -l clean repo-wide
- R-01KY922T9REA1T1GENS7CD7P2Z design sketch: go/types-based call resolution (M3): two-tier design recommended: syntax pass stays, cached go/types pass upgrades call edges additively; exit criterion go:coord.DB.Sweep shows real callers
- T-01KY93DFH8E1QSJ47BTDBX5ZVD M5 prep: GitHub Actions CI + CONTRIBUTING with the lifecycle loop: CI with headless self-hosting check gate + CONTRIBUTING
- T-01KY93DFH8FRGTZXKDW2KGQVTP docs nits: transcript rule count + README triple-hop demo with real output: transcript count fixed, README live-chain demo from shipped binary
- P-01KY9DMW38F8NVJAKZVWK2G5WS harness-agnostic orchestration language + fanout in the workflow prompt: orchestration as model-tier pattern with fanout, on every surface incl. manifest and workflow prompt
- P-01KY9DMW38EG4VXNNY0TC66ECQ CD: goreleaser release pipeline (multi-platform binaries on tag): goreleaser CD live: tag-gated release workflow, snapshot verified end-to-end, version injection confirmed
- P-01KY9K611GFHBRT5960HBT2CJQ SDD orchestration v2: research-first, grill, native decisions, bounded loops (blocked state): SDD v2 live: research/grill/decide tools, blocked side state with three decide exits, bounded rounds, two-mode /spectacle
- ADR-01KY9K7B18E7E970WQKGQ2MTR7 Merge PR #11 automatically after gates pass?: demo decision, answered cross-session
- R-01KY9MRDB8F6KBJGFH2AQH45SD wazero x tree-sitter feasibility: pure-Go WASM parser runtime (M6 architecture concept): Deliverable: docs/design-wasm-parsers.md (100-140 lines, style of docs/architecture.md). Questions to answer with WEB research (pkg.go.dev, github): (1) does a maintained pure-Go tree-sitter runtime over wazero exist (candidates to verify, not assume: wasilibs projects, malivvan/*, anything importing wazero + tree-sitter); (2) are official grammar .wasm builds published per language (web-tree-sitter artifacts, tree-sitter CLI build --wasm) and what ABI do they expect (emscripten imports?) - can wazero satisfy it or is a WASI build needed; (3) realistic integration shape for spectacle: one LanguageParser adapter owning a wazero runtime + N grammar modules, symbol extraction via tags queries (.scm) - which pieces exist vs must be written; (4) cost estimate (binary size per grammar, parse latency vs our line scanners); (5) recommendation: go/no-go/defer + first-slice cut (e.g. ONE language e2e as PoC) + exit criterion. Also state the fallback clearly: the langspec declarative scanner framework (P-01KY9ME75RFNFTP29Z24EB2H65) covers breadth today; wazero adds fidelity, not reach.
- P-01KY9ME75RFNFTP29Z24EB2H65 langspec: declarative multi-language support - a language is data, not code: 9 languages live: go/asm/cuda hand-written + py/js/ts/rust/java/ruby/php/c/cpp as langspec data; adding a language is one data file
- P-01KY9PXP4RE55VBHFW9TT4AEWW wazero C-PoC: measure the wasm/tree-sitter path against the langspec chain (ADR-01KY9NJ3KRFF8RPNARZMXP90VF: c-poc): measured verdict: parity PASS (1151/0/0), size PASS (9.24MB of 10MB), latency FAIL (0.8x-2.4x headroom, GC churn from the binding's calling convention), availability FAIL (no post-wasi-sdk cuda/objc grammar releases) — per R-01KY9MRDB8F6KBJGFH2AQH45SD §5 exit rule: no green-light, langspec chain stays the path; docs/design-wasm-poc-c.md
- ADR-01KY9NJ3KRFF8RPNARZMXP90VF R-01KY9MRDB8F6KBJGFH2AQH45SD empfiehlt: wasm/tree-sitter-Parser DEFERREN; Erst-Slice waere ein C-PoC (tree-sitter-c via wazero) mit Parity-Oracle gegen die Regex-Kette, Binärgrößen- und Latenzbudget innerhalb der M4-Envelope (docs/design-wasm-parsers.md). Wie verfahren wir mit wazero?: answered c-poc per user directive; PoC executed as T-01KY9PXP4RER8RH07XZF09AKNS — verdict no-green-light, langspec chain stays
- P-01KYA5X10GEP29H9XZWSY4EWFE full rebrand spectacle → spectackle (ADR-01KYA0YFRRE508YVYDEJ8648FN: full): full rebrand shipped hard (no fallbacks, pre-release): module path, cmd/spectackle, MCP identity, .spectackle folders (git mv, 14 dirs), SPECTACKLE_AGENT, docs/templates/commands regenerated via the commands tool; historical journal content and SPX rule IDs preserved as data. USER ACTION: rename the GitHub repo to spectackle.
- T-01KYA6JPC0FMDTC20S6P4RYJ8J cover target: Makefile + CI coverage gate at 70%: Scope (disjoint, only these two files): Makefile, .github/workflows/ci.yml. Measured baseline 2026-07-24: 75.0% total statements (go tool cover -func). Implementation: (1) Makefile target `cover`: run `go test -coverprofile=bin/cover.out ./...` then `go tool cover -func=bin/cover.out`, extract total percent, fail with exit 1 if below COVER_MIN (default 70, overridable via make var). Pure POSIX shell + awk, no new deps. Add `cover` to .PHONY. (2) ci.yml: add a `coverage` step after `test` running `make cover`. Exit criterion: `make cover` passes locally at >=70% and fails when COVER_MIN=99; CI workflow contains the step. Rollback: revert the two-file diff; no state, no schema, no runtime behavior touched. Verification commands: make cover; COVER_MIN=99 make cover (must fail); go vet ./....
- P-01KYA6JPC0EWV8ETYQ9T741ZX1 M5 coverage threshold: CI enforces a minimum go test coverage: Roadmap M5 lists 'CI: lint + check + coverage threshold'. CI (.github/workflows/ci.yml) already has build/vet/test -race/lint specs/smoke/headless check gate, but no coverage threshold. Add a coverage gate: run go test with -coverprofile across ./..., compute total coverage, fail the job below a fixed threshold. Threshold picked from current actual coverage (measure first, set slightly below to avoid flaking, ratchet later). Wire as a Makefile target `cover` so it runs identically locally and in CI.
- T-01KYA6YT28EWKSS8BT65HDJS05 orphan applies targets: o-record in check + test + docs grammar: Contract: MCP-004. Scope (disjoint): internal/mcpserver/tools.go (Server.check only), internal/mcpserver/tools_test.go, docs/tools.md. Implementation: in Server.check after drift.Load build map[rule+"\x00"+node]bool from anchors; iterate c.All() spec files' rules; for each rule with len(Applies)>0, for each applies node missing from the map append line fmt.Sprintf("o %s %s", rule.ID, node) (sorted, before drift lines, after duplicate-ID block). Output diet (R-01KY90ADKREDGV3TN10CFBAVZR): one line per missing pair, no prose. Exit criterion (explicit): (1) new test TestCheckOrphanApplies: author a rule with applies to an indexed node, delete its anchors.tsv row, check reports 'o <id> <node>'; with the row present check stays 'ok'. (2) TestCheckOnOwnRepo still passes (this repo has 0 orphans). (3) docs/tools.md check output grammar documents the o record. Verify: go test ./internal/mcpserver/ -run 'TestCheck' -race; make cover; ./bin/spectackle lint . Rollback: revert diff; MCP-004 stays as the contract.
- P-01KYA6YT28F5TSTMNAK0P8F41K M3 orphan-rule coverage: check reports applies targets without anchor rows: Roadmap M3 lists 'orphan-rule coverage'. Today check surfaces dir-level gaps (source dirs with zero rules) and drift on existing anchors, but a rule that declares {applies: node} whose (rule,node) anchor row is missing is silent — the binding intent exists, nothing is bound. Causes: manual anchors.tsv edits, replay loss, Reconcile shrinkage. Fix: in Server.check, after drift.Load, index anchor (rule,node) pairs; for every cascade rule with non-empty Applies, report each applies node lacking an anchor row as 'o orphan <rule> <node>'. Rules without applies stay exempt (process rules). Scope: internal/mcpserver/tools.go, internal/mcpserver/tools_test.go, docs/tools.md (check output grammar). Exit: new test proves a dropped anchor row is reported and a complete anchor set stays silent; check on this repo stays ok. Rollback: revert diff, no state or schema touched.
- T-01KYA762FRFTTBEZZ75TAF88D0 mergeCandidates: pair detection + compact wiring + test + docs: Contract: MCP-005 (or next minted ID). Scope (disjoint): internal/mcpserver/tools.go, internal/mcpserver/tools_test.go, docs/tools.md. Implementation: new func (s *Server) mergeCandidates(sub string) []string — load spec.Load cascade, for each SpecFile within sub, for each unordered rule pair with equal Pattern: tokenize both sentences (lowercase, strip non-alnum, drop stopwords when,while,if,then,where,shall,the,a,an,to,of,and,or,as,is), Jaccard = |intersection|/|union|; candidate if j>=0.6 or (len(Applies)>0 both sides and equal as sets). Emit fmt.Sprintf("c %s mergeable %s+%s j=%.2f", orDot(dir), id1, id2, j), sorted. Call from Server.compact only, append to cands before dry-run render; apply=true ignores them (no auto-merge). Test TestCompactMergeableCandidates: two near-identical U rules in one dir -> record with both IDs; a third dissimilar rule -> not paired. Verify: go test ./internal/mcpserver/ -race; make build; ./bin/spectackle lint .; compact dry-run on own repo manually once for false pairs. Exit: tests green, no false pair on own repo, docs/tools.md compact section documents the record. Rollback: revert diff.
- P-01KYA762FRECQVZZZR2EXRB3SE M3 mergeable-rule compaction candidates in compact dry-run: Roadmap M3 lists 'mergeable-rule compaction candidates'. compact currently surfaces done-items and journal folds only; near-duplicate rules accumulate silently. Add deterministic pair detection: two rules in the same spec file with the same EARS pattern whose normalized sentences (lowercase, punctuation stripped, EARS scaffolding words when/while/if/then/where/shall dropped) have token Jaccard >= 0.6, OR identical non-empty applies sets. Emit one record per pair in compact output: 'c <dir> mergeable <ID1>+<ID2> j=<score>'. Suggestion only: apply=true never auto-merges (merging changes contract semantics; that stays a human/LLM rule op=edit+retire). Emitted only by compact, not check — check stays signal-only for real integrity problems, TestCheckOnOwnRepo unaffected. Scope: internal/mcpserver/tools.go (compact + new mergeCandidates func), internal/mcpserver/tools_test.go, docs/tools.md compact section. Exit: test with two near-identical rules in one dir reports the pair with both IDs; two dissimilar rules stay silent; compact on own repo emits no false pair (verify manually once, then locked by TestCompact tests). Rollback: revert diff, read-only feature, no state written.
- T-01KYA7E0CREVPVKWCZZECM5AAM ForNode + getNode contract records + tests + docs: Contract: the SPC rule just minted (applies go:spec.Cascade.ForNode; anchor resolves after implementation). Scope (disjoint): internal/spec/cascade.go, internal/spec/cascade_test.go, internal/mcpserver/tools.go (getNode only), internal/mcpserver/tools_test.go, docs/tools.md. Implementation: (1) cascade.go: func (c *Cascade) ForNode(id, file string) []ResolvedRule — first pass all files' rules where slices.Contains(r.Applies, id) (file order), second pass ForPath(file) skipping IDs already taken. (2) tools.go getNode: load cascade via spec.Load; after node/edge lines call splitContractRules(c.ForNode(id, n.File), nil, rootSeen, &rl, &rootIDs); append rl lines then one 'r-root <IDs>' line if any (same rendering as draft pack). Contracts only for the requested root node. (3) docs/tools.md get section documents the r/r-root tail. Exit criterion (explicit): TestForNode in cascade_test.go covers applies-first order, cascade union, dedup; TestGetNodeShowsContracts in tools_test.go: rule with applies go:pkg.A + get id=go:pkg.A renders the r record; on own repo get go:saxpy.Saxpy shows SXP-API-002. Verify: go test ./internal/spec/ ./internal/mcpserver/ -race; make build; ./bin/spectackle lint .; make cover >= 70. Rollback: revert diff, read-only.
- P-01KYA7E0CRENH9BG06WWHC3NF8 M3 applies-aware ForNode: get on a code node returns its binding contracts: Roadmap M3 lists 'applies-aware ForNode'. Today get id=go:X renders only n/e graph records; the binding EARS contracts (rules with {applies: go:X} plus the cascade rules of the node's file) are invisible unless the caller separately walks specs — the one-stop context pack is incomplete. Fix: (1) internal/spec: new Cascade.ForNode(id, file) []ResolvedRule — rules whose Applies contains id (explicit bindings, first), then ForPath(file) cascade rules, deduped by ID. (2) internal/mcpserver getNode: append contract records for the requested node only (not impact neighbors — output diet R-01KY90ADKREDGV3TN10CFBAVZR): non-root rules as full r lines, root-scoped collapsed to one r-root ID record (SPX-MCP-004 precedent, splitContractRules reused). (3) docs/tools.md get section. Exit: spec unit test for ForNode (applies match, cascade union, dedup, applies-first order); mcpserver test: get on an anchored node renders its r record; get README example on own repo shows SXP-API-002 for go:saxpy.Saxpy. Rollback: revert diff, read-only feature.
- T-01KYA7RJ98ENMVCZJ5QTMAE2F0 PersonalizedRank + find focus param + tests + docs: Contract: the GRA rule just minted (anchor pending until indexed). Scope (disjoint): internal/graph/pagerank.go (new), internal/graph/pagerank_test.go (new), internal/mcpserver/tools.go (findIn + find scope=code branch only), internal/mcpserver/tools_test.go, docs/tools.md. Implementation per P-01KYA7RJ98EG7VFVR2FTXCW9HW body. Exit criterion (explicit): TestPersonalizedRank (seed highest, near > far, disconnected = 0, deterministic across two runs); TestFindFocusReranks (low-degree 1-hop neighbor of focus outranks high-degree far hub; unknown focus -> nf). Verify: go test ./internal/graph/ ./internal/mcpserver/ -race; make build; ./bin/spectackle lint .; make cover. Rollback: revert diff.
- P-01KYA7RJ98EG7VFVR2FTXCW9HW M3 personalized PageRank: find scope=code accepts focus for relevance ranking: Last open M3 deliverable. Global Rank is in+out degree (rankByDegree, M1) — good for hub-first default ordering, blind to what the agent is working on. Add: (1) internal/graph/pagerank.go: PersonalizedRank(g, focus []NodeID, depth, iters int, damping float64) map[NodeID]float64 — subgraph via Impact(focus, depth, Both, nil), undirected adjacency (relevance flows both ways in a call graph), restart mass uniform over seeds, fixed iteration count, deterministic, no allocation outside the maps. Nodes outside the subgraph score 0. (2) findIn gains Focus string (scope=code only, default empty = global rank; SPX-ARC-004 default declared). When set: over-fetch candidates (4x K), stable-sort by PPR score desc, truncate to K — match-class order preserved among equal scores. Unknown focus node -> nf record (SPX-ARC-003). (3) docs/tools.md find schema + prose. Exit criterion (explicit): graph unit test — focus seed scores highest, 1-hop neighbor outscores 3-hop node, disconnected node scores 0, two runs produce identical maps (determinism); mcpserver test — find q with focus reorders a low-degree near node above a high-degree far hub; find with unknown focus returns nf. Verify: go test ./internal/graph/ ./internal/mcpserver/ -race; make build; ./bin/spectackle lint .; make cover >= 70. Rollback: revert diff; read-only feature, schema addition is backward-compatible (optional param).
- T-01KYA7Z948FHGR4BGATN3TNDWB ears fuzz targets + make fuzz + CI step + roadmap tick: Contract: the EAR rule just minted. Scope (disjoint): internal/ears/fuzz_test.go (new), Makefile, .github/workflows/ci.yml, docs/roadmap.md. Implementation per P-01KYA7Z948EM5TSP57F98604HR body; fuzz targets never assert grammar semantics, only crash-freedom and cheap invariants. Exit criterion (explicit): make fuzz exits 0 with three targets at -fuzztime 10s; go test ./internal/ears/ -race green; ci.yml has a fuzz step after coverage; roadmap shows M3 done + M4 annotation. Verify: make fuzz; go test ./internal/ears/ -race; make build; ./bin/spectackle lint .; git diff docs/roadmap.md renders one-row change plus M4 parenthetical. Rollback: revert diff.
- P-01KYA7Z948EM5TSP57F98604HR M4 linter fuzzing + roadmap truth: M3 done, M4 status annotated: M4 lists 'linter fuzzing'; the repo has zero Fuzz targets. Add Go-native fuzz targets for the EARS surface that eats untrusted text: FuzzLintSentence(sentence), FuzzParseRules(text) and FuzzStripFrontMatter(text) in internal/ears/fuzz_test.go — invariants: no panic, ParseRules findings non-nil rule IDs, StripFrontMatter bodyStartLine >= 1, seeds from the real grammar corpus (valid EARS sentences, malformed headings, CRLF, frontmatter edge cases). Wire `make fuzz` (go test -fuzz per target, -fuzztime 10s each, sequential) and a CI step after coverage. Second file: docs/roadmap.md truth pass — M3 -> done (all four deliverables shipped: applies-aware ForNode SPX-SPC-007, orphan-rule coverage MCP-004, mergeable-rule candidates MCP-005, personalized PageRank SPX-GRA-004; exit criterion 'check gates CI' live since T-01KY93DFH8E1QSJ47BTDBX5ZVD), M4 row annotated like M2's parenthetical (perf gate cleared 7-15x per internal/index intent, streamable HTTP live T-01KY8Z4XDGEP79KJXZJ1Z48TG5, fuzzing this proposal; cursors-everywhere still open). Exit criterion (explicit): make fuzz green locally (3 targets x 10s, zero crashers); fuzz_test.go seeds also run under plain go test; ci.yml contains the fuzz step; roadmap M3 row shows the check mark. Rollback: revert diff; fuzz corpus files under testdata are additive. Scope (disjoint): internal/ears/fuzz_test.go (new), Makefile, .github/workflows/ci.yml, docs/roadmap.md.
- T-01KYA88JZGE90RVFS0FCDY3ZGM wire budget.Resume into find/get/check/grill/research/state + paging test + docs: Contract: SPX-ARC rule just minted + existing SPX-ARC-002. Scope (disjoint): internal/mcpserver/tools.go, grill.go, research.go, state.go, tools_test.go, docs/tools.md. Implementation per P-01KYA88JZGF6Y96DNSV61D9AG4 body: findIn/checkIn/grillIn/researchIn/stateIn gain Cur string json cur (findIn also Budget int json budget, default 2000, applied AFTER focus re-rank truncation to K — over-fetch stays internal); every TruncateRecords(lines, 0, b) call becomes TruncateRecords(lines, budget.Resume(in.Cur), b); getNode/getPath signatures gain offset int, get passes budget.Resume(in.Cur). Exit criterion (explicit): TestCursorsResumeAcrossPages green (find + get paging, concat equals unbudgeted, malformed cur = page 0); full suite -race green. Verify: go test ./internal/mcpserver/ -race; make build; ./bin/spectackle lint .; make cover. Rollback: revert diff.
- P-01KYA88JZGF6Y96DNSV61D9AG4 M4 cursors everywhere: find gains budget+cur, every read tool resumes: Last open M4 deliverable. SPX-ARC-002 (truncate at record boundary, append cur) is only half-implemented: find renders unbudgeted in both paths (code + FTS) and has no budget/cur inputs; get accepts cur but ignores it (offset hardcoded 0); check/grill/research/state truncate at offset 0 with no cur input — every emitted cursor is a dead end. Fix: (1) findIn += budget (default 2000) + cur; both find paths truncate via TruncateRecords(lines, budget.Resume(in.Cur), in.Budget). (2) get passes budget.Resume(in.Cur) as offset into getNode/getPath. (3) checkIn/grillIn/researchIn/stateIn += cur, wired to Resume. (4) docs/tools.md schema updates. New root contract: WHEN a tool receives a cur argument, the server SHALL resume at the encoded record offset so consecutive pages concatenate without overlap or gap. Exit criterion (explicit): TestCursorsResumeAcrossPages — find scope=code with tiny budget yields cur; resuming with it pages through; concatenated pages equal the unbudgeted output exactly; same asserted for get depth=2. Malformed cur degrades to offset 0 (existing Resume semantics), asserted. Verify: go test ./internal/mcpserver/ -race; make build; ./bin/spectackle lint .; make cover. Rollback: revert diff; added params are optional, backward-compatible. Scope (disjoint): internal/mcpserver/tools.go, grill.go, research.go, state.go, tools_test.go, docs/tools.md.
- T-01KYA8JHB0ER0TV8370K59G06W cookbook doc + Fortran spec via the recipe + e2e test: Contracts: SPX-LSP-001 (language = one Spec value), SPX-REPO-002, SPX-TST-001. Scope (disjoint): docs/cookbook-new-language.md, internal/graph/graph.go, internal/index/langs.go, internal/langspec/fortran.go, internal/langspec/fortran_test.go. Implementation per P-01KYA8JHB0FDJSQHV9QFGM8DQ3. Fortran Defs detail: (?i) case-insensitive; function def with optional attribute prefixes (pure/impure/elemental/recursive/module) and optional type prefix (integer/real/logical/character/complex/double precision, each optionally parameterized) then function <name>( — the prefix alternation deliberately excludes the word end so end function lines cannot match (Go regexp has no lookahead; document this trap in the cookbook); subroutine same attribute prefixes; module <name> end-of-line as KType excluding module procedure/function forms. CallRe nil (no brace-delimited bodies). Exit criterion (explicit): TestFortranSpecIndexAllE2E green (function+subroutine+module minted with f90: prefix and QualFileStem, end-lines mint nothing, deterministic re-parse); go test ./... -race green; lint clean; cookbook lists exactly the touched files. Verify: go test ./internal/langspec/ ./internal/index/ -race; make build; ./bin/spectackle lint .; make cover. Rollback: revert diff.
- P-01KYA8JHB0FDJSQHV9QFGM8DQ3 M6 new-language cookbook, proven live by adding Fortran via the recipe: M6 lists the new-language cookbook; its exit criterion (adding a language touches minimal surface) was superseded by P-01KY9ME75RFNFTP29Z24EB2H65: a language is one Spec data value + one extLang entry (+ one Lang constant). Deliverable: docs/cookbook-new-language.md — the exact recipe (choose Lang tag + constant in internal/graph/graph.go, extension entries in internal/index/langs.go extLang, one Spec value in internal/langspec/<lang>.go with QualMode choice table, Def regex conventions incl. the end-keyword false-positive trap and no-lookahead workaround, CallRe rules and when to leave it nil, test conventions from r_test.go, dogfood check via SPX-REPO-002/TST-001). Proof: follow the cookbook verbatim to add Fortran (LangF90 tag f90; exts .f90 .f95 .f03 .f08; QualFileStem; case-insensitive Defs: function with optional type/attribute prefixes, subroutine, module as KType; CallRe nil — no brace bodies in Fortran). Exit criterion (explicit): fortran_test.go proves a real .f90 file through index.New+IndexAll mints f90:<stem>.<name> nodes for function/subroutine/module and end-lines mint nothing; cookbook file lists exactly the files the Fortran diff touches (verifiable against git diff --stat); full suite -race green; lint clean. Rollback: revert diff; additive. Scope (disjoint): docs/cookbook-new-language.md (new), internal/graph/graph.go (Lang const + name only), internal/index/langs.go, internal/langspec/fortran.go (new), internal/langspec/fortran_test.go (new), README.md status table language count if named.
- T-01KYA8TCA8F0CSSWM9145VYY3R README: driver prints initialize instructions; first-contact list item: Scope (disjoint, exactly one file): README.md. Two edits in the 'Headless quickstart' section. EDIT 1 — in the python driver snippet, replace the line pair `send({...initialize...})` / `recv()` handling so the initialize result is captured and its instructions surfaced. Concretely: the current snippet has `send({"jsonrpc": "2.0", "id": 1, "method": "initialize", ...})` followed by a bare `recv()`. Change the bare `recv()` line to: `init = recv()` and add directly after it: `print(init.get("result", {}).get("instructions", ""))`. Nothing else in the snippet changes. EDIT 2 — the numbered list ('The three things that cost time on first contact') becomes four items: append a new item 4: '**Read the manifest**: the `instructions` field of the `initialize` response IS the workflow contract (lifecycle loop, swarm protocol, orchestration/fan-out and model tiering). Feed it to the driving LLM verbatim — a driver that discards it leaves the LLM without the intended division of labor.' Adjust the intro sentence from 'The three things' to 'The four things'. Verification: (1) grep README.md for 'instructions' shows the print line inside the python block and the new list item; (2) extract the python block and run python3 -m py_compile on it (must compile); (3) the block still matches the driver behavior against ./bin/spectackle serve (optional live check: pipe a swarm call, instructions text appears before tool output). Constraints: no other README section touched; keep line width consistent with surrounding prose (~76 cols). Rollback: git checkout README.md.
- P-01KYA8TCA8FNRBZF9XYB40RM8H headless drivers must consume initialize instructions — README recipe discards the workflow contract: Root cause found while answering why the tiered orchestrator/implementer model was not applied by a headless session: the server's initialize response carries the self-bootstrapping instructions manifest (lifecycle loop, SWARM, ORCHESTRATION incl. fan-out and model tiering), but the README Headless-quickstart driver calls recv() after initialize and DISCARDS the result — a driver built from the recipe never sees the workflow contract, so the driving LLM never learns the intended division of labor. The MCP instructions themselves are complete (P-01KY8ZE78RE619GSDH193NS07V/P-01KY9DMW38F8NVJAKZVWK2G5WS); the delivery fails only on the headless path. Fix (docs-only): README headless quickstart — (a) driver snippet captures the initialize result and prints the instructions field to stdout before the tool loop; (b) the numbered first-contact list gains an item: the instructions field of the initialize response IS the workflow contract (lifecycle + swarm + orchestration/fan-out) — feed it to the driving LLM verbatim, do not discard it. Exit criterion (explicit): README python snippet contains a print of instructions (result.serverInfo-adjacent field capabilities/instructions per MCP spec: result.instructions); numbered list has the new item; snippet stays runnable (python3 -c compile check of the extracted block or manual run against ./bin/spectackle). Rollback: revert README. Scope: README.md only.
- T-01KYA976F8E42S249SV235X99J langspec objc: Spec + five tests + IndexAll E2E: Scope (disjoint, exactly two NEW files): internal/langspec/objc.go, internal/langspec/objc_test.go. Recipe: docs/cookbook-new-language.md steps 3-4 only (graph.LangObjC and extLang .m/.mm entries already exist — do NOT touch graph.go or langs.go). Reference specs: internal/langspec/r.go (structure), internal/langspec/fortran.go (comment style, trap notes). Spec value objcSpec: Lang graph.LangObjC, Exts [".m", ".mm"], Qual QualFileStem, CallRe nil (first slice: definitions only), Defs exactly: (1) Kind KMethod, Re `^\s*[-+]\s*\([^)]*\)\s*(\w+)`, Name 1 — matches `- (void)viewDidLoad {` and `+ (instancetype)sharedInstance {`, capturing the first selector segment; (2) Kind KType, Re `^@(?:interface|implementation)\s+(\w+)`, Name 1 — matches `@interface Foo : NSObject` and `@implementation Foo`; (3) Kind KFunc, Re `^\s*(?:static\s+)?(?:[A-Za-z_][\w\s*]*?)\s+(\w+)\s*\(([^;{]*)\)\s*\{`, Name 1, Sig 2 — plain C functions defined in .m files (brace on same line, so prototypes ending in ; never match). init() appends objcSpec to registry. Tests mirror fortran_test.go exactly: TestObjcSpecLangExtensions, TestObjcSpecNodes (positive source with one -method, one +method, one @interface, one @implementation with different name, one C function; assert full NodeIDs objc:<stem>.<name>, Kind, Line), TestObjcSpecNegativeLines (`@end`, `[obj message];` call site, `// - (void)commented`, C prototype `int f(int);` mint nothing), TestObjcSpecRegisteredInAll, TestObjcSpecDeterministic, TestObjcSpecIndexAllE2E (real .m file through index.New+IndexAll, helper pattern copied from fortran_test.go TestFortranSpecIndexAllE2E). Verify: go vet ./internal/langspec/ && go test ./internal/langspec/ -run Objc -race. Constraints: no edits outside the two files; follow existing test helper nodesByID. Rollback: delete both files.
- T-01KYA976F8F4CBEGAST7QE96JM langspec metal: Spec + five tests + IndexAll E2E: Scope (disjoint, exactly two NEW files): internal/langspec/metal.go, internal/langspec/metal_test.go. Recipe: docs/cookbook-new-language.md steps 3-4 only (graph.LangMSL and extLang .metal entry already exist — do NOT touch graph.go or langs.go). Reference specs: internal/langspec/r.go (structure), internal/langspec/fortran.go (comment style). Spec value metalSpec: Lang graph.LangMSL, Exts [".metal"], Qual QualFlat (entry points are globally named, like CUDA kernels — see cookbook Qual table), CallRe nil, Defs exactly: (1) Kind KKernel, Re `^\s*kernel\s+[\w:<>,*&\s]+?\b(\w+)\s*\(([^;{]*)\)`, Name 1, Sig 2 — matches `kernel void add_arrays(device const float* a [[buffer(0)]], ...)`; (2) Kind KKernel, Re `^\s*(?:vertex|fragment)\s+[\w:<>,*&\s]+?\b(\w+)\s*\(([^;{]*)\)`, Name 1, Sig 2 — matches `vertex float4 vertexShader(...)` and `fragment half4 fragmentShader(...)`; (3) Kind KFunc, Re `^\s*(?:static\s+|inline\s+)*(?:[A-Za-z_][\w:<>]*)\s+(\w+)\s*\(([^;{]*)\)\s*\{`, Name 1, Sig 2 — plain MSL helper functions with brace on the same line (prototypes with ; never match). Metal is case-sensitive C++ dialect: NO (?i). init() appends metalSpec to registry. Tests mirror fortran_test.go exactly: TestMetalSpecLangExtensions, TestMetalSpecNodes (positive source with one kernel, one vertex, one fragment, one helper; NodeIDs are msl:<name> — QualFlat, no stem prefix; assert Kind KKernel for the three entry points, KFunc for helper), TestMetalSpecNegativeLines (`float4 proto(float2 uv);` prototype, `// kernel void commented`, `return pos;` mint nothing), TestMetalSpecRegisteredInAll, TestMetalSpecDeterministic, TestMetalSpecIndexAllE2E (real .metal file through index.New+IndexAll, helper pattern from fortran_test.go). Verify: go vet ./internal/langspec/ && go test ./internal/langspec/ -run Metal -race. Constraints: no edits outside the two files. Rollback: delete both files.
- P-01KYA976F8F25SWR30N0G2ANCQ M6 slice: ObjC and Metal langspec parsers via the cookbook (parallel implementers): M6 lists ObjC/Metal/Vulkan support. extLang already claims .m/.mm (LangObjC) and .metal (LangMSL) but no parser exists for either — those files mint zero nodes today, so cross-language chains into ObjC/Metal are invisible. First slice: two langspec Specs per docs/cookbook-new-language.md (Lang constants and extLang entries already exist — cookbook steps 1-2 are no-ops, only step 3-4 apply). Two fully disjoint tasks (objc.go+objc_test.go vs metal.go+metal_test.go), one fresh implementer each, in parallel — dogfooding the tiered fan-out. Resolvers (objc bridging, gpupipe Vulkan) stay a later slice per RSV-001 layering. Exit criterion (explicit): both languages pass the cookbook five-test convention plus IndexAll E2E; full suite -race green; lint clean; a .m and a .metal file in a temp repo mint objc:/msl: nodes end-to-end. Rollback: revert additive files. Scope: internal/langspec/objc.go, objc_test.go, metal.go, metal_test.go (all new).
- T-01KYA9BM2GEZPTQX0YBRHR8SC9 TOKEN ECONOMY paragraph: instructions + template + prompt + docs + regen + test: Contract: MCP-006. Scope: internal/mcpserver/server.go, internal/mcpserver/templates/commands/workflow.md.tmpl, internal/mcpserver/prompts.go, internal/mcpserver/tools_test.go, docs/agent-workflow.md, plus the files regenerated by commands op=gen (AGENTS.md, .claude/commands/*.md, .github/prompts/*.md). EDIT 1 — server.go: the `instructions` const is one Go raw string with three paragraphs (lifecycle, SWARM:, ORCHESTRATION:); append a NEW fourth paragraph, newline-separated, verbatim: >>>TOKEN ECONOMY: prefer server records over shell text tools whenever you only need locations or structure — shell grep/find return file contents (cost O(codebase)), server tools return IDs+spans (cost O(results)). Substitutions: grep/rg <symbol> -> find q=<symbol> scope=code (ranked n records; add focus=<node> for near-my-work ranking); grep -r over specs/history -> find scope=rule|rejection|history (FTS5); find(1) by filename -> find scope=code (file paths and signatures match too); where is X defined / what calls X -> get id=<node> depth=N; before any sed-style bulk edit survey the sites via get depth and run check after — sed is for the edit, never for the search; NEVER grep or sed .spectackle/ files (server-owned: reads via find/get, writes via tools only). Read raw file bytes with shell only when you already know the one file you need.<<< (Go raw string: the text contains no backticks, safe to paste). EDIT 2 — workflow.md.tmpl: directly after the step-1 Research block (after the line ending 'never ask the user something the pack could answer.'), insert an indented continuation paragraph: '   Token economy: never shell-grep/find what the server can answer — `find scope=code` for symbols/paths, `find scope=rule|rejection|history` for specs and past failures, `get id=<node> depth=N` for impact. Shell reads raw bytes of one known file only; `.spectackle/` files are never grepped or sed-edited (server-owned).' EDIT 3 — prompts.go: the workflow prompt step string starting '1 research q=' gets appended before its closing quote: '; token economy: find scope=code / get depth replace shell grep-find exploration, .spectackle/ never shell-edited'. EDIT 4 — docs/agent-workflow.md: append a short section '## Token economy: shell vs server' with a 6-row table (shell habit -> server tool -> why) mirroring EDIT 1 content. EDIT 5 — tools_test.go: add TestInstructionsTeachTokenEconomy asserting strings.Contains(instructions, "TOKEN ECONOMY"), strings.Contains(instructions, "scope=code") and strings.Contains(instructions, "NEVER grep or sed .spectackle/"). REGEN — make build, then via the driver: {"name": "commands", "arguments": {"op": "gen"}} — commit-ready regenerated files must show the EDIT-2 text in AGENTS.md, .claude/commands/spectackle.md, .github/prompts/spectackle.prompt.md. Verify: go vet ./internal/mcpserver/ && go test ./internal/mcpserver/ -run 'TestInstructions|TestCommands' -race; make build; ./bin/spectackle lint .; grep -l 'Token economy' AGENTS.md .claude/commands/spectackle.md .github/prompts/spectackle.prompt.md. Rollback: revert diff. Constraint: wording verbatim as given — no rephrasing.
- P-01KYA9BM2GEM2VWX5Y1TZRKA5E token economy: teach agents to replace shell grep/find/sed exploration with server tools: Requirement: wherever it saves tokens, agents working a spectackle repo should use the MCP server instead of shell find/grep/sed. Today neither the initialize instructions, the workflow prompt/template, nor docs/agent-workflow.md map shell habits to server tools — an agent with shell access defaults to grep/find and pays O(codebase) content tokens for what the server answers in O(results) ID+span records. Fix (wording authored here, pasted by one implementer): a TOKEN ECONOMY paragraph with the substitution map — grep/rg symbol -> find q= scope=code (ranked n-records, focus for proximity); grep -r over specs/history/rejections -> find scope=rule|rejection|history (FTS5); find(1) by filename -> find scope=code matches file paths too (SPX-GRA-003); where-defined/what-depends -> get id= depth=N instead of chained greps; pre-bulk-edit site survey -> get depth + check after, sed only for the edit itself; NEVER grep/sed .spectackle/ files (server-owned, SPX-ARC-005); rule of thumb: shell returns file contents, server returns IDs+spans — use shell only to read actual bytes of one known file. Delivery points: (1) server.go instructions const; (2) templates/commands/workflow.md.tmpl (regenerate all four harness surfaces via commands op=gen); (3) prompts.go workflow prompt step 1; (4) docs/agent-workflow.md subsection. Exit criterion (explicit): new test asserts the instructions const contains the TOKEN ECONOMY guidance; regenerated AGENTS.md/.claude/.github files carry it (git diff shows all four); full suite -race green; lint clean. Rollback: revert diff, wording-only. Scope: internal/mcpserver/server.go, internal/mcpserver/templates/commands/workflow.md.tmpl, internal/mcpserver/prompts.go, internal/mcpserver/tools_test.go, docs/agent-workflow.md + generated AGENTS.md, .claude/commands/*, .github/prompts/*.
- T-01KYA9PPJ8FQGAJX2D26JVDEMY gpupipe Metal slice: host tracking + launch regex + fixture test: Contract: the RSV rule just minted (anchor resolves after implementation). Scope (disjoint, two files): internal/resolve/gpupipe.go (replace stub Resolve), internal/resolve/gpupipe_test.go (new). Reference files (read, do not modify): internal/resolve/cuda.go (brace-depth host tracking, launchKey dedupe, braceDelta — REUSE braceDelta, do not redefine), internal/resolve/cuda_test.go + internal/resolve/resolver_test.go (FileSet fixture conventions), internal/langspec/objc.go (the two host-def regexes to mirror). Implementation: keep type/Name/Langs unchanged; keep the doc comment but move the Vulkan bullet under a 'still future' note and drop the confidence/Rank-penalty sentence (RSV-001: edges only). Resolve: for each path in fs.ByLang(graph.LangObjC): scan lines; host begin when line matches objc method regexp ^\s*[-+]\s*\([^)]*\)\s*(\w+) OR C-function regexp ^\s*(?:static\s+)?(?:[A-Za-z_][\w\s*]*?)\s+(\w+)\s*\(([^;{]*)\)\s*\{ (name = group 1); track brace depth from that line with braceDelta; host ends at depth<=0. Inside a host body, launch regexp newFunctionWithName:\s*@"(\w+)" (FindAllStringSubmatchIndex, may hit multiple per line); per hit emit graph.Edge{Src: ids.Mint("objc", stem+"."+host), Dst: ids.Mint("msl", kernel), Kind: graph.ELaunch, File: path, Line: lineNo} where stem = filepath.Base(path) minus extension; dedupe with a launchKey-style struct per (host, kernel, path). Note: ObjC method lines may open the brace on the SAME line or the NEXT line — after a host-begin match with braceDelta(line)==0, do NOT close the host immediately (unlike cuda.go): treat depth 0 as 'body not yet opened' until the first '{' appears, then close at depth<=0. Test gpupipe_test.go: fixture .m source with (1) method -(void)runKernel calling newFunctionWithName:@"add_arrays" inside its body; (2) C function static void setupPipeline(...) { ... newFunctionWithName:@"scale_rows" ... }; (3) a top-level line newFunctionWithName:@"orphan_kernel" outside any body -> must NOT edge; (4) the add_arrays call duplicated on a second line in the same method -> exactly one edge. Assert exactly two edges: objc:fixture.runKernel -launch-> msl:add_arrays and objc:fixture.setupPipeline -launch-> msl:scale_rows, Kind ELaunch, correct File. Use the FileSet test helper pattern from cuda_test.go/resolver_test.go. Verify: go vet ./internal/resolve/ && go test ./internal/resolve/ -race. Rollback: git checkout the two files.
- P-01KYA9PPJ8F70RYJFKKDAHNEQ7 M6 gpupipe resolver, Metal slice: newFunctionWithName string binding -> ELaunch edges: The GpuPipeResolver stub documents its own detection contract 'implemented in M6'; with objcSpec and metalSpec now minting objc:/msl: nodes (P-01KYA976F8F25SWR30N0G2ANCQ), the Metal half is implementable: scan LangObjC files, track the enclosing host symbol (ObjC method or C function) via the same brace-depth walk cuda.go uses, and for every string literal argument of newFunctionWithName:@"name" inside a host body emit ELaunch from the host node (objc:<filestem>.<name>, matching objcSpec QualFileStem qualification) to msl:<name> (QualFlat), deduped per (host, kernel, file) like cuda.go launchKey. Vulkan half stays future (no SPIR-V/GLSL entry-point parsing yet); the stub's confidence/Rank-penalty note is out of scope — RSV-001 forbids resolvers mutating nodes, edges only. Exit criterion (explicit): gpupipe_test.go mirrors cuda_test.go: a fixture .m file with one method-hosted and one C-function-hosted newFunctionWithName launch plus one launch outside any body (must NOT edge) and one duplicated launch (must dedupe) yields exactly the two expected ELaunch edges with correct Src qualification; resolver_test.go registry test keeps passing; full suite -race green; lint clean. Rollback: revert gpupipe.go to stub, delete test. Scope (disjoint): internal/resolve/gpupipe.go, internal/resolve/gpupipe_test.go.
- T-01KYAA11M0E33BESK72PJWB33Y QualMode-aware callEdges + objcSpec.CallRe + message-send tests: Contract: the LSP rule just minted plus existing LSP-001. Scope (disjoint, three files): internal/langspec/langspec.go, internal/langspec/objc.go, internal/langspec/objc_test.go. EDIT 1 — langspec.go callEdges: change `Dst: graph.NodeID(ids.Mint(string(p.S.Lang), callee))` to `Dst: graph.NodeID(ids.Mint(string(p.S.Lang), p.qualify(path, callee)))` and extend the callEdges doc comment sentence 'Destinations are minted in the same language and may be dangling' with ', qualified per the Spec QualMode (QualFlat: bare name, unchanged; QualFileStem: same-file resolution, cross-file dangles)'. EDIT 2 — objc.go: add to objcSpec: CallRe: regexp.MustCompile(`\[\s*\w+\s+(\w+)`) capturing the first selector segment of a message send, and Stop: []string{"alloc", "init", "release", "retain", "autorelease", "copy", "dealloc", "self", "super"}; replace the existing 'CallRe stays nil' doc paragraph with one explaining message-send capture, the Stop list, and that cross-file sends dangle until a future bridging resolver. EDIT 3 — objc_test.go: extend the positive fixture with a helper method the main method calls via [self helperMethod] and a Stop-listed [obj retain]; add TestObjcSpecMessageSendEdges asserting exactly the expected ECall edges: caller -> objc:<stem>.helperMethod present; no edge for retain; no self-recursion edge; assert via pr.Edges. IMPORTANT: adding CallRe changes EndLine semantics for objc KFunc/KMethod nodes (body span instead of def line) — existing TestObjcSpecNodes may assert Line==EndLine; adjust its EndLine expectations to the brace-counted span (see how c_test.go handles spans) and note this in the report. Verify: go vet ./internal/langspec/ && go test ./internal/langspec/ -race (FULL package suite — c/cpp CallRe tests must pass unchanged). Rollback: revert diff.
- P-01KYAA11M0F4K9MHTY8200PMM1 M6 ObjC message-send call edges: QualMode-aware CallRe callees + objcSpec.CallRe: Last implementable M6 resolver piece. langspec callEdges mints Dst flat (ids.Mint(lang, callee)) — correct for QualFlat CallRe users (c, cpp), but for a QualFileStem language every callee edge would dangle forever since nodes are stem-qualified. Fix in two parts, one task: (1) framework: callEdges mints Dst via p.qualify(path, callee) — byte-identical behavior for QualFlat (qualify returns the bare name), and for QualFileStem it resolves same-file calls exactly while cross-file calls dangle (tolerated, same as Go's syntactic pass; a future resolver may bridge). (2) objcSpec gains CallRe matching the first selector segment of a message send \[\s*\w+\s+(\w+) with a Stop list for memory-management noise (alloc init release retain autorelease copy dealloc self super) — ObjC bodies are brace-delimited so cspan bounds them already. Exit criterion (explicit): extended objc_test.go proves [self helperMethod] inside a method body yields ECall objc:<stem>.<caller> -> objc:<stem>.helperMethod; Stop-listed sends and own-name recursion yield no edge; full langspec suite -race green proving c/cpp behavior unchanged (their CallRe tests must not change); lint clean; cover >= 70. Rollback: revert diff. Scope (disjoint): internal/langspec/langspec.go (callEdges + doc), internal/langspec/objc.go, internal/langspec/objc_test.go.
- T-01KYAA8VM0E51AQWRW2M9TQF3A README: three exact replacements (graph row, self-hosting row, focus paragraph): Scope (disjoint): README.md only. EDIT 1 — replace the table row starting '| Cross-language graph:' entirely with: '| Cross-language graph: `go/parser` + Plan 9 asm + CUDA chains, langspec (29 data-driven languages incl. ObjC/Metal, [cookbook](docs/cookbook-new-language.md)), gpupipe Metal launch + message-send edges, persistent parse cache | ✅ live (wazero/tree-sitter still future, for full C/C++) |'. EDIT 2 — replace the row '| Self-hosting gate | 🔜 M5 ([roadmap](docs/roadmap.md)) |' with: '| Self-hosting gate: CI runs lint + check + coverage (≥70%) + fuzz; features developed through the server''s own lifecycle | ✅ live ([roadmap](docs/roadmap.md)) |'. EDIT 3 — replace the paragraph 'Initial focus: **Go** with arbitrary native bindings (cgo/C, C++, CUDA, Plan 9 ASM, Objective-C/Metal, Vulkan); parser and resolver layers are plugin interfaces for arbitrary languages later.' with: 'Focus: **Go** with arbitrary native bindings (cgo/C, C++, CUDA, Plan 9 ASM, Objective-C/Metal; Vulkan future); the langspec parser layer makes a language one data value ([cookbook](docs/cookbook-new-language.md), 29 languages), resolvers bridge the FFI boundaries.' Keep ~76-col wrapping. Exit criterion: grep README.md for 'Self-hosting gate' shows the new row with ✅; grep for 'plugin interfaces' has NO hits; grep for 'cookbook-new-language' has 2 hits in README (docs list + one of the new spots... note: docs list already links it — then 3 total). Verify: grep as above; no other lines changed (git diff shows exactly 3 hunks). Rollback: git checkout README.md.
- P-01KYAA8VM0F7091F2DP3B14N0Z README truth pass: status table and focus paragraph reflect the shipped state: README claims are stale: status table row 'Cross-language graph' omits langspec (29 data-driven languages), ObjC/Metal chain and gpupipe; row 'Self-hosting gate | soon M5' is outdated — CI gates (lint+check+coverage+fuzz) are live and features ship through the lifecycle; the focus paragraph still says 'parser and resolver layers are plugin interfaces for arbitrary languages later' although langspec + cookbook exist. Exit criterion (explicit): the three named spots updated with exact replacement text provided in the task body; no other README section touched; grep assertions per task; lint clean. Rollback: revert README.md. Scope (disjoint): README.md only.
- T-01KYAAGWERFKWTJ3GFST0B0PAE metalcompute example: sources + server-authored bundle + live transcript: Scope (disjoint): examples/metalcompute/** only — nothing outside. Files: (1) examples/metalcompute/Renderer.m — @interface Renderer : NSObject / @implementation Renderer with exactly two methods: - (void)buildPipeline { id fn = [library newFunctionWithName:@"add_arrays"]; (void)fn; } and - (void)dispatch { [self buildPipeline]; [encoder dispatchThreadgroups]; } (plain compilable-looking ObjC, no imports needed — this is an index fixture, not a buildable app; keep braces on the def lines). (2) examples/metalcompute/shaders.metal — kernel void add_arrays(device const float* a [[buffer(0)]], device float* out [[buffer(1)]], uint i [[thread_position_in_grid]]) { out[i] = a[i] + 1.0; } plus a vertex float4 passthrough(uint vid [[vertex_id]]) { return float4(0); }. (3) After creating both files run make build (binary may be stale) then ./bin/spectackle reindex, then author the spec bundle VIA THE SERVER (never write spec.md by hand): driver rule op=add dir=examples/metalcompute stem MTC-API pattern E system 'the compute pipeline' trigger 'add_arrays is dispatched' response 'process each thread index exactly once so out[i] depends only on a[i]' applies ["msl:add_arrays"]; second rule op=add same dir stem MTC-API pattern U system 'the Renderer' response 'bind the kernel by the literal name add_arrays through newFunctionWithName' applies ["objc:Renderer.buildPipeline"]. (4) examples/metalcompute/README.md — three sentences (what it demos: ObjC->Metal chain via message-send + string-binding launch edges) plus the VERBATIM output of driver get objc:Renderer.dispatch depth=2 in a code fence. Driver: python3 /tmp/claude-0/-home-user-spectackle/d0b8e016-f097-5792-857b-fd9ea4a8a781/scratchpad/mcp_orch.py /home/user/spectackle with heredoc lines like {"name": "get", "arguments": {"id": "objc:Renderer.dispatch", "depth": 2}}. Exit criterion: transcript shows n objc:Renderer.dispatch, e objc:Renderer.dispatch call objc:Renderer.buildPipeline, e objc:Renderer.buildPipeline launch msl:add_arrays, n msl:add_arrays kernel; ./bin/spectackle lint . clean; go test ./internal/spec/ -run TestSelfSpecs -race green. Verify commands as listed. Rollback: rule op=retire both MTC rules, delete examples/metalcompute.
- P-01KYAAGWERFDVTHGPF95KPBPH9 examples/metalcompute: live ObjC->Metal chain demo (saxpy analog): The new ObjC/Metal axis (P-01KYA976F8F25SWR30N0G2ANCQ/P-01KYA9PPJ8F70RYJFKKDAHNEQ7/P-01KYAA11M0F4K9MHTY8200PMM1) has no in-repo proof like saxpy's Go->C->CUDA chain. Add examples/metalcompute: Renderer.m (ObjC class, dispatch method message-sends [self buildPipeline], buildPipeline does newFunctionWithName:@"add_arrays"), shaders.metal (kernel void add_arrays + vertex/fragment pair), server-authored spec bundle (rule op=add dir=examples/metalcompute, stem MTC-API: one rule applies msl:add_arrays, one applies objc:Renderer.buildPipeline — the LLM never writes spec files), README.md with the real get transcript. Exit criterion (explicit): after reindex, get objc:Renderer.dispatch depth=2 shows the full chain — ECall dispatch->buildPipeline (message-send edge) and ELaunch buildPipeline->msl:add_arrays (gpupipe) — captured verbatim into the example README; ./bin/spectackle lint . clean including the new bundle; full suite -race green (self-spec lint test covers the bundle); check ok with the two new anchors. Rollback: delete examples/metalcompute + rule op=retire the two rules. Scope (disjoint): examples/metalcompute/** only.
- T-01KYAAY1BGEMRRCY09GVHH10K9 go get -u sweep + tidy + full gate + verified action majors: Scope disjointness: exactly go.mod, go.sum, .github/workflows/ci.yml, .github/workflows/release.yml — no sibling implementer is active (swarm shows only the orchestrator), and go.mod/go.sum are the canonical shared files the orchestrator serializes itself per the documented workflow, so no lease conflict is possible. Steps: (1) lease claim on the four files; (2) go get -u ./... && go mod tidy; (3) full gate: make build && go vet ./... && go test -race ./... && ./bin/spectackle lint . && make smoke && make cover && FUZZTIME=5s make fuzz; (4) verify action majors via the proxy (api.github.com latest release per action); bump ci.yml/release.yml only for verifiably existing new majors; (5) exit: go list -m -u all shows no remaining updates within current majors. Rollback: git checkout go.mod go.sum .github/workflows.
- P-01KYAAY1BGF19SSSS7BVBM1Z49 dependencies up to date: go get -u sweep + tidy + full gate; actions majors verified: Requirement: all dependencies must be up to date. Facts (go list -m -u all): the four direct modules (modelcontextprotocol/go-sdk v1.6.1, x/tools v0.48.0, yaml.v3 v3.0.1, modernc.org/sqlite v1.54.0) are current; 11 indirect/toolchain modules have newer versions (cloud.google.com/go/compute/metadata, pprof, go-isatty, segmentio/asm, goldmark, x/oauth2, x/telemetry, check.v1, modernc cc/gc/libc). Plan: go get -u ./... then go mod tidy (raises direct+indirect to latest minor/patch within module compatibility), then the FULL gate: make build, go vet, go test -race ./..., lint-specs, smoke, make cover, plus a 10s fuzz smoke. GitHub Actions: pinned by major tag (checkout@v4, setup-go@v5, goreleaser-action@v6) — verify current majors via api.github.com through the proxy; bump only majors that verifiably exist, else leave (major tags track latest within major automatically). Shared-file wiring justification: go.mod/go.sum are the one file pair every future implementer depends on — per the documented workflow the orchestrator serializes shared-file wiring itself, no fan-out. Exit criterion (explicit): go list -m -u all reports zero available updates for modules within current majors; full gate green; git diff touches only go.mod, go.sum and (if majors verified) ci.yml/release.yml. Rollback: git checkout go.mod go.sum + workflows.
- T-01KYABDEGRFNKSBY93M68NES3N langspec glsl: Spec + five tests + IndexAll E2E: Scope (disjoint): internal/graph/graph.go (add one Lang const after LangF90: LangGLSL Lang = "glsl"), internal/index/langs.go (add extLang entries ".comp", ".vert", ".frag", ".geom", ".tesc", ".tese", ".glsl" all -> graph.LangGLSL), internal/langspec/glsl.go (new), internal/langspec/glsl_test.go (new). Recipe: docs/cookbook-new-language.md. Reference specs: internal/langspec/metal.go (closest analog — C-like brace bodies, QualFlat), internal/langspec/fortran.go (comment style). glslSpec: Lang graph.LangGLSL, Exts []string{".comp", ".vert", ".frag", ".geom", ".tesc", ".tese", ".glsl"}, Qual QualFlat (Vulkan entry points are globally named; GLSL has no modules/classes), CallRe nil, Defs exactly one KFunc: Re `^\s*(?:[A-Za-z_]\w*(?:\s+[A-Za-z_]\w*)*\s+|void\s+)(\w+)\s*\(([^;{]*)\)\s*\{` Name 1 Sig 2 — matches a GLSL function definition `void main() {`, `float sdf(vec3 p) {`, `vec4 shade(...) {` where the brace is on the same line, so prototypes ending in ; never match. GLSL is case-sensitive: NO (?i). Note: GLSL has no preprocessor-function ambiguity like C's `if(` because the leading type-word requirement (at least one identifier before the name) excludes control-flow keywords standing alone — but to be safe DO NOT add a Stop list (CallRe is nil, Stop is only consulted with CallRe). init() appends glslSpec to registry. Tests mirror internal/langspec/metal_test.go EXACTLY (same five + E2E): TestGlslSpecLangExtensions, TestGlslSpecNodes (positive .comp source with `void main() {`, `float helper(float x) {`, `vec3 compute(vec3 a, vec3 b) {`; assert NodeIDs glsl:main, glsl:helper, glsl:compute — QualFlat, no stem prefix; Kind KFunc; correct Line), TestGlslSpecNegativeLines (`layout(local_size_x=64) in;` a layout qualifier, `if (x > 0) {` control flow, `float proto(float x);` prototype, `// void commented() {` comment, `return a + b;` -> all mint nothing), TestGlslSpecRegisteredInAll, TestGlslSpecDeterministic, TestGlslSpecIndexAllE2E (real .comp file through index.New+IndexAll, helper pattern copied verbatim from metal_test.go TestMetalSpecIndexAllE2E). WATCH the layout-qualifier negative case carefully — `layout(...) in;` must NOT match the func Def (it ends in ; not {, and has no name-before-paren in the right shape); verify this in the test. Verify: go vet ./internal/langspec/ && go test ./internal/langspec/ -run Glsl -race && go test ./internal/langspec/ ./internal/index/ -race. Constraints: no edits outside the four listed files; follow existing nodesByID test helper. Rollback: delete glsl.go+glsl_test.go, revert the two one-line registrations.
- T-01KYABHAHGE86A7WMNAWC7BQNE gpupipe + roadmap: record the Vulkan-resolver finding: Scope (disjoint from T-A, two files): internal/resolve/gpupipe.go (doc comment only — NO code change), docs/roadmap.md (M6 row annotation only). EDIT 1 — gpupipe.go: in the GpuPipeResolver doc comment, replace the Vulkan bullet '(still future)' text with a precise finding: Vulkan host code binds a compiled SPIR-V module blob by FILE PATH via vkCreateShaderModule and names the entry point through VkPipelineShaderStageCreateInfo.pName, which is almost always the literal "main" — so unlike Metal's newFunctionWithName:@"<kernelName>" there is no host-side kernel-name literal to string-match against a glsl entry point; a meaningful Vulkan launch edge needs module-provenance tracking (which .spv/.comp compiles into which module handle), a separate harder problem than the line-scanner name-match this resolver does for Metal. GLSL shader SOURCE is now parsed (glsl: nodes, P-01KYABHAHGFE8SGDTB00D0G4JA) so the target nodes exist; only the host->module binding is unresolved. EDIT 2 — docs/roadmap.md: in the M6 row parenthetical, change the Vulkan mention to note GLSL source is parsed (30 languages) and that the Vulkan host-binding resolver remains open as module-provenance work (not a name-match analog). Keep wording tight, ~1 line each. Exit criterion: gpupipe.go still compiles (doc-only change: go build ./internal/resolve/ green); grep gpupipe.go for 'module-provenance' hits; roadmap M6 row mentions GLSL/30 languages. Verify: go build ./... && ./bin/spectackle lint . (after make build). NOTE: this task depends on T-A having shipped glsl: nodes — do it AFTER T-A is merged. Rollback: git checkout the two files.
- P-01KYABHAHGFE8SGDTB00D0G4JA M6 un-defer Vulkan parser side: GLSL shader langspec (30th language) via the cookbook: Requirement 'nix deferred': the gpupipe stub lists Vulkan as future because no shader source is parsed. First clean slice: a GLSL langspec parser so .comp/.vert/.frag/.geom/.tesc/.tese/.glsl shader entry points and functions become graph nodes (glsl:<name>), exactly like metalSpec did for MSL. This un-defers the PARSER side. NOTE recorded for the resolver side (do not fake it): unlike Metal's newFunctionWithName:@"kernel" direct name-match, Vulkan host code binds a compiled SPIR-V module blob by filename with pName almost always "main" — there is no host-side kernel-NAME literal to match against a glsl entry point, so a meaningful Vulkan launch-edge resolver is a genuinely harder, separate problem (module-provenance tracking), not a line-scanner analog; this proposal ships the parser and documents that finding in the gpupipe doc + roadmap rather than inventing edges. Follows docs/cookbook-new-language.md exactly. Exit criterion (explicit): glsl_test.go proves a real .comp file through index.New+IndexAll mints glsl:main and glsl:<helper> KFunc nodes, and non-def lines (control flow, calls, prototypes) mint nothing; full suite -race green; lint clean; cover>=70. Scope (disjoint, T-A): internal/graph/graph.go (LangGLSL const), internal/index/langs.go (7 shader exts), internal/langspec/glsl.go + glsl_test.go (new). Separate follow-up task T-B (disjoint file): gpupipe.go doc + docs/roadmap.md record the Vulkan-resolver finding. Rollback: revert additive files.
- T-01KYABWEZRFTK8DGRNH1JJN2JQ add cover to make all; update CONTRIBUTING make-all section: Scope (disjoint, two files): Makefile, CONTRIBUTING.md. EDIT 1 — Makefile: the target line is currently `all: build vet test lint-specs smoke`. Change it to `all: build vet test lint-specs smoke cover` (append cover; cover is already a defined .PHONY target). Do NOT add fuzz to all (too slow). Do not touch any other target. EDIT 2 — CONTRIBUTING.md: the section '## `make all` must be green before any PR' currently says 'make all runs build, vet, test, spec lint, and smoke.' Replace that sentence with 'make all runs build, vet, test, spec lint, smoke, and the coverage gate (`cover`, ≥70%).' and change the following sentence's 'plus the self-hosting check gate' to 'plus `make fuzz` and the self-hosting `check` gate'. Keep the rest of that section verbatim. Exit criterion (explicit): `grep 'all: build' Makefile` shows cover at the end; `make all` exits 0 and its output ends with a 'coverage NN.N% (min 70%)' line; CONTRIBUTING '## make all' section names coverage in make all and fuzz under CI. Verify: grep 'all: build vet test lint-specs smoke cover' Makefile; make all 2>&1 | tail -3 (must show coverage line and exit 0); grep -c 'coverage gate' CONTRIBUTING.md (>=1); grep 'make fuzz' CONTRIBUTING.md. Rollback: git checkout Makefile CONTRIBUTING.md.
- P-01KYABWEZRFMHRYS17AJYHQDX1 align make all with the CI gate: add coverage; document fuzz + check in CONTRIBUTING: Gap: CONTRIBUTING.md promises 'make all must be green before any PR' and describes make all as build+vet+test+lint-specs+smoke — but CI (.github/workflows/ci.yml) additionally enforces `make cover` (coverage >=70%) and `make fuzz`, neither reachable via make all. A contributor sees make all green locally and gets surprised by a red CI coverage gate. Fix: (1) Makefile — add `cover` to the `all` target (fast, deterministic, a hard CI gate; belongs in the pre-PR sweep). Leave `fuzz` OUT of `all` (30s+ per target is too slow for the default gate) but keep it a named target CI runs. (2) CONTRIBUTING.md — update the 'make all' section: make all now runs build, vet, test, spec lint, smoke AND coverage; note CI additionally runs `make fuzz` and the self-hosting `check` gate. Exit criterion (explicit): `make all` runs the coverage gate (grep Makefile all target contains cover; `make all` exits 0 and prints the coverage line); CONTRIBUTING mentions coverage in make all and fuzz under CI; no behavior change to cover/fuzz targets themselves. Rollback: revert two files. Scope (disjoint): Makefile, CONTRIBUTING.md.
- T-01KYAC8MMGEBA85QFV1Q9QSEFN author_test.go: direct unit tests for AddRule/EditRule/RetireRule/MaxNum/NextID/AppendIntent: Scope (disjoint, ONE new file): internal/spec/author_test.go. Package: spec (white-box, same package as author.go — can call unexported ruleBlock if useful but prefer exported surface). API reference (read internal/spec/author.go + internal/spec/cascade.go): AddRule(ws workspace.Root, c *Cascade, req AuthorReq)(AuthorRes,error); EditRule(ws, c, id, sentence, rationale string, applies []string)(AuthorRes,error); RetireRule(ws, c, id string)(string,error); (*Cascade).MaxNum(stem string)int; (*Cascade).NextID(stem string)string; AppendIntent(ws, ctx, line string)error. AuthorReq fields: Dir, Stem, ForceID, Mint func(stem string,floor int)(int,error), Sentence, Rationale, Applies. AuthorRes: ID, Path, Findings []ears.Finding, Written bool. Cascade built via spec.Load(root string)(*Cascade,error) (see cascade_test.go buildTree/Load pattern). workspace.Root is a struct literal workspace.Root{Dir: root}. Test helper: write a tempdir root, call Load, exercise author fn, re-Load to assert persisted state. Required cases (use a valid EARS sentence like 'The system SHALL log to `stderr` only.' for happy paths; an invalid one like 'do stuff' for lint-error): (1) TestAddRuleCreatesBundleAndMintsID — empty root, AddRule Dir="pkg" Stem="PKG-API" valid sentence -> res.Written true, res.ID=="PKG-API-001", res.Path=="pkg/.spectackle/spec.md"; re-Load finds the rule; file has front matter with prefix PKG. (2) TestAddRuleLintErrorBlocksWrite — invalid sentence -> res.Written false, res.Findings has a Severity==ears.Error, no file written (Load has no such rule). (3) TestAddRuleAppliesAndRationale — Applies []{"go:pkg.A"} + Rationale set -> reloaded rule heading has {applies: go:pkg.A}, Rationale line present. (4) TestAddRuleForceIDReplay — ForceID "PKG-API-042" -> res.ID that exact, no minting. (5) TestAddRuleStemInference — add PKG-API-001 first, then AddRule same Dir with empty Stem -> infers PKG-API and mints -002. (6) TestAddRuleMissingStemErrors — empty Dir with no existing file and empty Stem -> non-nil error. (7) TestMaxNumAndNextID — after adding PKG-API-001/002, c.MaxNum("PKG-API")==2 and c.NextID("PKG-API")=="PKG-API-003" (reload c first). (8) TestEditRuleReplacesSentence — add a rule, EditRule new valid sentence -> reloaded rule.Text changed, Written true. (9) TestEditRuleEmptyKeepsOld — EditRule with sentence="" rationale="" applies=nil -> old text/rationale/applies preserved. (10) TestEditRuleLintErrorKeepsFile — EditRule invalid sentence -> Written false, Findings error, reloaded rule still old text. (11) TestEditRuleUnknownID — non-nil error. (12) TestRetireRuleDeletesBlock — add two rules, RetireRule the first -> reload: first gone, second present, returned path correct. (13) TestRetireRuleUnknownID — non-nil error. (14) TestAppendIntentCreatesAndInserts — AppendIntent ctx="" 'first line' then 'second line' -> reloaded spec.md '## intent' section contains both lines in order (read the file bytes to assert; intent is a prose section). Verify: go test ./internal/spec/ -race -run 'Author|AddRule|EditRule|RetireRule|MaxNum|NextID|AppendIntent' then go test ./internal/spec/ -cover (must be >=65%). Constraints: test-only, no edits to author.go/cascade.go or any non-test file. Rollback: delete author_test.go.
- P-01KYAC8MMGFZ4TH1QXACMFE6W6 internal/spec: direct unit tests for the rule-authoring surface (author.go): internal/spec sits at 40.3% coverage — its authoring surface (AddRule, EditRule, RetireRule, MaxNum/NextID, AppendIntent in author.go) has ZERO direct unit tests; it is exercised only transitively through internal/mcpserver's rule-tool integration tests. author.go composes the lint-gated EARS block, mints collision-free IDs, and edits/retires rule blocks in place — the write path the whole `rule` tool depends on. Direct package-level tests pin this contract independent of the MCP layer, catch regressions integration tests can mask (e.g. front-matter creation, applies-heading rendering, block-boundary detection, ForceID replay), and raise a core package meaningfully. No production code changes — test-only. Exit criterion (explicit): new internal/spec/author_test.go covers AddRule (happy path writes file+front-matter+auto-ID, lint error blocks write with Written=false, applies+rationale rendering, ForceID replay, stem inference, missing-stem error), EditRule (sentence replace lint-gated, empty-sentence/rationale keeps old, applies replace, unknown-id error), RetireRule (block deleted, unknown-id error), MaxNum/NextID (floor + next free across cascade), AppendIntent (creates section, inserts before next heading); internal/spec package coverage rises to >=65%; go test ./internal/spec/ -race green; make cover still green. Rollback: delete the test file. Scope (disjoint): internal/spec/author_test.go (new) only.
- T-01KYAGK490FWXTGC2JJ4TDHGWB compose_test.go: Compose/MissingSlots/PatternFromString/ParseSections/IsProseSection/LintFile: Scope (disjoint, ONE new file): internal/ears/compose_test.go, package ears (white-box). API (read internal/ears/compose.go + ears.go): type Slots{System,Response,Trigger,State,Condition,Feature string}; Compose(p Pattern, s Slots)(string,error); MissingSlots(p Pattern, s Slots)[]string; PatternFromString(s string)Pattern; Classify(sentence string)Pattern; Pattern.String()string; ParseSections(text string)[]Section (Section{Name,Line int,Text}); IsProseSection(name string)bool; LintFile(path string)([]Finding,error) (Finding{Code,Severity,File,Line,Msg}, Severity ears.Error/ears.Warn). Pattern consts: PInvalid,PUbiquitous,PEvent,PState,PUnwanted,POptional,PComplex. Encoding letters: U=PUbiquitous,E=PEvent,S=PState,N=PUnwanted,O=POptional,C=PComplex. Cases: (1) TestComposeUbiquitous — Slots{System:"the server",Response:"log to stderr."} pattern PUbiquitous -> exactly 'The server SHALL log to stderr.' (note: leading 'the ' stripped from system, trailing '.' trimmed from response then re-added). (2) TestComposeEvent — System 'the wrapper',Response 'check status',Trigger 'a kernel is launched' -> 'WHEN a kernel is launched, the wrapper SHALL check status.'. (3) TestComposeState/Unwanted/Optional — WHILE.../IF...,THEN.../WHERE... forms exactly. (4) TestComposeComplex clause order — System+Response+Trigger+State+Feature set -> sentence begins 'WHERE <f>, WHILE <s>, WHEN <t>, the ...' (order WHERE<WHILE<WHEN); and an IF variant when Condition set instead of Trigger yields 'THEN the'. (5) TestComposeRoundTripsThroughClassify — for EACH of the 6 patterns, compose a valid sentence then assert Classify(sentence)==p (composition and recognition are inverse — the key invariant). (6) TestComposeMissingSlotErrors — PEvent with empty Trigger -> non-nil error, empty string. (7) TestMissingSlots — PUbiquitous needs system+response; PEvent adds trigger; PComplex with <2 clauses reports the 'two of' entry; fully-filled -> empty slice. (8) TestPatternFromString — 'U'->PUbiquitous, lowercase 'e'->PEvent, all six; ' x '/'' -> PInvalid; and for each of the 6, PatternFromString(p.String())==p round-trip. (9) TestParseSections — text with '## intent\nline a\n\n## SPX-FOO-001\nrule\n## notes\nn1' -> two sections intent+notes, intent.Text=='line a', rule heading ignored, correct Line numbers. (10) TestIsProseSection — 'intent','notes','design','context' true; 'SPX-FOO-001','random' false. (11) TestLintFile — write a temp .md file with front matter + one valid rule ('## A-001\nThe system SHALL log to `stderr` only.') + one invalid ('## A-002\ndo stuff') -> findings contain an Error for the vague one; valid one yields no Error. Verify: go test ./internal/ears/ -race then go test ./internal/ears/ -cover (>=75%). Constraints: test-only, no edits to compose.go/ears.go. Rollback: delete compose_test.go.
- P-01KYAGK490FD7AD6ZFH8HC5BNC internal/ears: direct tests for slot composition + section/file lint (compose.go): internal/ears at 49.1% — the EARS slot-composition surface (Compose, MissingSlots, PatternFromString in compose.go) has ZERO direct coverage, plus ParseSections/IsProseSection/LintFile in ears.go are untested. compose.go is the heart of slot-based rule authoring: end users never hand-write EARS sentences, they fill slots and the server Composes deterministically — a regression here silently produces malformed contracts. The fuzz targets only prove crash-freedom, never correct classification. High-value invariant to pin: every Composed sentence must Classify() back to the same Pattern (composition and recognition are inverse). Test-only, no production changes. Exit criterion (explicit): new internal/ears/compose_test.go covers Compose (all 6 patterns produce the canonical sentence AND round-trip through Classify to the same pattern; 'the '/'The ' system-prefix stripping; trailing-'.' response trim; Complex clause ordering WHERE<WHILE<WHEN/IF; missing-slot returns error), MissingSlots (per-pattern required slots; Complex needs >=2 clauses), PatternFromString (U/E/S/N/O/C upper+lower -> pattern, round-trips with Pattern.String(), garbage -> PInvalid), ParseSections (extracts intent/notes/design/context, ignores rule headings, multi-section ordering), IsProseSection (whitelist true, others false), LintFile (temp spec file with a valid + an invalid rule -> correct findings); internal/ears coverage rises to >=75%; go test ./internal/ears/ -race green; make cover green. Rollback: delete the test file. Scope (disjoint): internal/ears/compose_test.go (new) only.
- T-01KYAGQ09RFSB81GGQQGBGRKWD roadmap: mark M4 + M5 done: Scope: docs/roadmap.md only. Two edits. EDIT 1 — the M4 row starts '| **M4** *(all deliverables live:' — change '| **M4** *(all' to '| **M4** ✅ *(all'. EDIT 2 — the M5 row starts '| **M5** | **Self-hosting gate** |' — change it to '| **M5** ✅ *(exit criterion met: features shipped through the server''s own lifecycle all cycle)* | **Self-hosting gate** |'. No other rows touched. Exit: grep '\*\*M4\*\* ✅' and '\*\*M5\*\* ✅' both hit; ./bin/spectackle lint . clean. Rollback: git checkout docs/roadmap.md.
- P-01KYAGQ190E5X95XADT4C4KA7W roadmap truth: mark M4 and M5 done — both live, M5 exit criterion met repeatedly: The roadmap M4 row's parenthetical already says 'all deliverables live' but the row lacks the ✅ marker M0-M3/M6 carry; M5 lacks ✅ too although its deliverables are live (CI runs lint+check+coverage+fuzz; CONTRIBUTING documents the find-scope=rejection+draft loop) and its exit criterion — 'a spectackle feature merged that was developed through spectackle's own lifecycle' — has been met dozens of times this development cycle (every proposal/task this session ran through draft->grill->rule->lease->check->archive). Fix: mark M4 ✅ and M5 ✅, add a one-line 'met' note to M5's parenthetical. Doc-only truth pass, no code. Exit criterion (explicit): roadmap M4 and M5 rows both start with the ✅ marker like the other done milestones; M5 parenthetical notes the exit criterion is met; ./bin/spectackle lint . clean; no other roadmap rows changed. Rollback: revert docs/roadmap.md. Scope (disjoint): docs/roadmap.md only.
- T-01KYAGXM70FV2V06R08AHYGQ9C extract run() + TestRunDispatch: Scope: cmd/spectackle/main.go, cmd/spectackle/main_test.go. Implementation preserves behavior exactly. main.go: keep the log.SetFlags/SetOutput setup in main(); move the args-parsing switch into new func run(args []string) int. run returns: len(args)==0 -> usage(); return 2. switch args[0]: 'serve' -> return serve(args[1:]); 'lint' -> return lint(args[1:]); 'reindex' -> return reindex(args[1:]); 'version' -> fmt.Println("spectackle "+mcpserver.Version); return 0; '-h'/'--help'/'help' -> usage(); return 0; default -> log.Printf("unknown subcommand %q", args[0]); usage(); return 2. main() becomes: log.SetFlags(0); log.SetOutput(os.Stderr); os.Exit(run(os.Args[1:])). No other funcs change. main_test.go: add TestRunDispatch(t) with subtests asserting run() return codes: {nil,2},{[]string{"version"},0},{[]string{"help"},0},{[]string{"-h"},0},{[]string{"--help"},0},{[]string{"bogus"},2}. Plus lint via run: reuse the existing writeSpec helper (already in main_test.go) to build a clean-spec tempdir and a bad-spec tempdir, then assert run([]string{"lint", cleanDir})==0 and run([]string{"lint", badDir})==1. Do NOT test 'serve' (blocks on stdio). Verify: go vet ./cmd/spectackle/ && go test ./cmd/spectackle/ -race && go build ./... (bin still builds). Rollback: git checkout the two files.
- P-01KYAGXM70FCJTQPG6GMRT1876 cmd/spectackle: extract testable run() dispatch from main (untestable os.Exit entry): main() calls os.Exit directly inside its subcommand switch, so the CLI dispatch contract (exit codes for version/help/unknown/no-args) is untestable and sits at 0% coverage. Standard Go fix: extract the switch into func run(args []string) int returning the exit code; main() becomes os.Exit(run(os.Args[1:])). No behavior change — same subcommands, same codes (no-args->2, unknown->2 with the log line, help/-h/--help->0, version->prints+0, serve/lint/reindex delegate to the existing int-returning funcs). This makes the dispatch + usage() testable. Then a dispatch test pins the CLI contract. Value is standalone (testable entry point, a real code-smell fix), not just coverage. Exit criterion (explicit): run([]string) int exists and main() is os.Exit(run(os.Args[1:])); new TestRunDispatch covers no-args->2, 'version'->0, 'help'/'-h'/'--help'->0, 'unknown'->2, 'lint <clean tempdir>'->0, 'lint <bad-spec tempdir>'->1 (serve path NOT tested — it blocks); go test ./cmd/spectackle/ -race green; cmd/spectackle coverage rises; go vet clean; the stdio serve behavior is unchanged (manual: bin still starts). Rollback: revert both files. Scope (disjoint): cmd/spectackle/main.go, cmd/spectackle/main_test.go.
- T-01KYAHWKDRFGVVGVH8133NTT9J agent-workflow.md: worktree isolation + orchestrator-owns-moves subsection: Orchestrator implements inline (no fanout — dogfoods the rule). Scope: docs/agent-workflow.md only. Insert a new subsection immediately AFTER the '## Fan-out' section (before '## Decisions, grill & bounded feedback loops'), titled '## Worktree isolation & who writes lifecycle state', with two rules and the rationale: (1) Each implementer runs in a DEDICATED git worktree, never the shared main working tree — so no two implementers (nor the orchestrator) ever write the same source file, spec bundle, or work.md at once; the server's work op=start provides this (re-roots the agent, semantic-replays its .spectackle back on submit), and a headless driver must replicate it (one worktree per implementer). (2) The ORCHESTRATOR owns every lifecycle move (draft -> submitted -> ... -> done -> archived). Implementers only claim and release their scope lease and edit code inside their worktree, then report completion — they do NOT move items. Rationale: item state lives in per-dir .spectackle/work.md, a plain file; two processes doing read-modify-write on it race (last writer wins) and silently drop item records. Scope leases are safe under concurrency because they live in the shared coord.db (WAL); work.md is not. Keeping all move calls in the single orchestrator process removes the item-record race by construction, and worktree isolation removes it for code and spec files too. Exit: the subsection exists between Fan-out and Decisions; ./bin/spectackle lint . clean. Rollback: git checkout docs/agent-workflow.md.
- P-01KYAHWKDRE7Z842NKNPK5J8GY workflow: implementers in dedicated worktrees; orchestrator owns all lifecycle moves: Live failure this cycle: my Agent-tool implementers edited the shared main tree AND wrote lifecycle state (draft/move) to the shared root .spectackle/work.md via their own driver process, concurrently with the orchestrator's driver. work.md is a plain per-dir file; two processes doing read-modify-write on it race (last-writer-wins) and silently drop item records — P-0000001JS0FDVBJFKRHDNQSDZE/T-0000002D4REXNAQJ1XE3Z21G6M were lost this way (deliverables stayed correct; only the item bookkeeping vanished). Leases did NOT race because they live in the shared coord.db (WAL, cross-process safe); work.md is not. User decision (AskUserQuestion): adopt BOTH fixes — implementers run in dedicated git worktrees, AND the orchestrator owns every lifecycle move. Codify in docs/agent-workflow.md so the rule is durable for anyone running the tiered loop headless. Doc-only. Exit criterion (explicit): docs/agent-workflow.md gains a subsection stating (a) implementers run in a dedicated worktree, never the shared main tree, and (b) the orchestrator makes every lifecycle move (draft..archived); implementers only claim/release leases (coord.db, safe) and edit code, then report — they never move items; with the reason (work.md read-modify-write race vs WAL-safe coord.db leases). ./bin/spectackle lint . clean. Rollback: revert docs/agent-workflow.md. Scope (disjoint): docs/agent-workflow.md only.
- T-01KYAJBJXGEGV995YYPWQZEWZ2 build+run poc/wasmparse, record wazero tax + parity, write findings section: IMPLEMENTER RUNS IN A DEDICATED WORKTREE and does NOT move lifecycle items (the orchestrator owns all moves per docs/agent-workflow.md 'Worktree isolation & who writes lifecycle state'). You only: lease claim, build+measure, write the findings doc, lease release, then REPORT — do not call move. Scope (disjoint): poc/wasmparse/** and docs/design-wasm-parsers.md. Steps: (1) lease claim item=T-01KYAJBJXGEGV995YYPWQZEWZ2 paths=[poc/wasmparse, docs/design-wasm-parsers.md]. (2) cd poc/wasmparse (separate go.mod). Build the size probes: go build -o /tmp/sizewith ./cmd/sizewith and go build -o /tmp/sizewithout ./cmd/sizewithout (read their source first to confirm what each imports; sizewith pulls wazero, sizewithout doesn't). Record ls -l byte sizes of both and compute the delta (the wazero tax) in bytes and MB. (3) Inspect cmd/poc/main.go, corpus/, tswasm/: if a tree-sitter .wasm grammar is present under tswasm/ and cmd/poc runs offline, run it over corpus/ and capture parse latency (wall time) and parity vs cparser (matching/mismatching node counts). If the grammar .wasm is absent or requires network, DO NOT fetch — record 'grammar wasm unavailable offline' as the finding and report only the size tax. (4) Append to docs/design-wasm-parsers.md a new section exactly titled '## Re-measurement (ADR-01KYAHT690ERFACEYDP9Q75RKF, reopen-poc)' with a dated bullet list: sizewithout bytes, sizewith bytes, delta (bytes + MB), and either the latency/parity numbers or the documented blocker. Plain measured facts only, no recommendation (the orchestrator writes that). (5) Verify: both size binaries exist and sizes are recorded; go vet ./... inside poc/wasmparse is clean; the doc section exists. (6) lease release. Report the raw numbers verbatim in your final message. Rollback: git checkout docs/design-wasm-parsers.md; poc build artifacts are in /tmp.
- P-01KYAJBJXGF4K9AGJ6QYR4E5YW wazero re-measurement (ADR-01KYAHT690ERFACEYDP9Q75RKF reopen-poc): refresh the tax numbers, decide on data: ADR-01KYAHT690ERFACEYDP9Q75RKF answered reopen-poc: re-measure the wazero/WASM tree-sitter path now that langspec spans 30 languages and cleared the M4 perf gate 7-15x. The PoC harness already exists under poc/wasmparse (its own go.mod, never touches spectackle's): cmd/sizewith + cmd/sizewithout (binary-size-with/without-wazero), cmd/poc (parse harness), corpus/, tswasm/, cparser/, triple/. Task: build+run the harness, capture the current wazero binary-size tax (sizewith - sizewithout, in bytes/MB) and, if the wasm grammar under tswasm/ is present+runnable offline, the parse latency + parity-vs-cparser over corpus/; if a grammar .wasm is missing/unfetchable in this sandbox, record THAT as the finding (no network fabrication). Append the numbers to docs/design-wasm-parsers.md as a new '## Re-measurement (ADR-01KYAHT690ERFACEYDP9Q75RKF, reopen-poc)' section; the orchestrator reads the numbers and writes the recommendation. Exit criterion (explicit): the two size binaries build and their byte sizes + delta are recorded; cmd/poc either runs (latency+parity captured) or its blocker is documented; docs/design-wasm-parsers.md has the new section with real measured numbers; ./bin/spectackle lint . clean (the doc is outside spec bundles so lint is unaffected). Rollback: revert docs/design-wasm-parsers.md; poc/ changes are additive/scratch. Scope (disjoint): poc/wasmparse/**, docs/design-wasm-parsers.md.
- T-01KYAM2RHGFDESN35P0ZS7DE0S add .claude to the four directory-walk skip-lists: IMPLEMENTER IN A DEDICATED WORKTREE; NO lifecycle moves (orchestrator owns them); only lease claim, code+test, lease release, report. Scope (disjoint): internal/workspace/workspace.go, internal/spec/cascade.go, internal/index/indexer.go, internal/mcpserver/tools.go. Edits (add the string '.claude' to each skip-list, matching existing style): (1) workspace.go — the walk switch `case ".git", "node_modules", "testdata":` (around line 203, inside ContextDirs) becomes `case ".git", "node_modules", "testdata", ".claude":`. (2) cascade.go — the same `case ".git", "node_modules", "testdata":` (around line 85) becomes `case ".git", "node_modules", "testdata", ".claude":`. (3) indexer.go — the ignoreDirs map literal (around line 86) gains `".claude": true,` (add to the map). (4) tools.go — the coverageGaps walk switch (around line 1056) that lists `case ".git", "node_modules", "testdata", "bin", ".spectackle":` gains `, ".claude"`. Nothing else. Test: create a throwaway nested bundle to prove the skip works — in a tempdir-based test OR manually: mkdir -p .claude/worktrees/x/.spectackle && cp .spectackle/spec.md .claude/worktrees/x/.spectackle/spec.md, then ./bin/spectackle lint . must still report 15 spec files / 61 rules / 0 findings (NOT doubled), then rm -rf .claude/worktrees/x. Also add/extend a Go test if one exists for ContextDirs or cascade discovery asserting a .claude subdir is skipped (check internal/workspace and internal/spec for existing discovery tests; if present, add a case; if not, skip the Go test and rely on the manual proof, noting it). Verify: go test ./internal/workspace/ ./internal/spec/ ./internal/index/ ./internal/mcpserver/ -race; make build; the manual dummy-bundle lint proof. lease release. Report: the four diffs, the lint-not-doubled proof, test results. Rollback: git checkout the four files.
- P-01KYAM2RHGFK6RYQFQTYN2Q41G server walk must skip .claude — agent worktrees pollute spec discovery + FTS cache: The worktree-isolation model (user-chosen) has a blocking gotcha found live: git worktrees created by the Agent tool live at .claude/worktrees/<id>/ INSIDE the repo, each a full checkout with its own .spectackle bundles. spectackle's directory walks descend into them: spec discovery doubled to 29 files / 111 rules with 50 duplicate-ID findings while a worktree existed, and the FTS cache retains .claude/worktrees/... journal/rejection entries even after the worktree is removed and reindex runs (they keep surfacing in research/grill/find as duplicates). Four walk skip-lists omit .claude: internal/workspace/workspace.go (ContextDirs, ~line 203 case), internal/spec/cascade.go (~line 85 case), internal/index/indexer.go (ignoreDirs map, ~line 86), internal/mcpserver/tools.go (check coverageGaps switch, ~line 1056). Fix: add '.claude' to all four so the walk never descends into agent worktrees; the fix + a cache rebuild (rm .spectackle/cache; reindex — the cache is local-only, rebuilds from disk) purges the stale rows permanently. This is the enabling fix that makes worktree-isolated implementers usable without polluting the orchestrator's spec state. Exit criterion (explicit): all four skip-lists include .claude; with a dummy .claude/worktrees/x/.spectackle/spec.md present, ./bin/spectackle lint . still reports only the real 15 files/61 rules (not doubled) and check stays ok; go test ./... -race green. Rollback: revert the four one-line additions. Scope (disjoint): internal/workspace/workspace.go, internal/spec/cascade.go, internal/index/indexer.go, internal/mcpserver/tools.go.
- T-01KYAMSC5RF73TC7M1XB2EX8HD README: add top Quickstart (download from releases), retitle build-from-source: IMPLEMENTER IN A DEDICATED WORKTREE; NO lifecycle moves (orchestrator owns them); only lease claim, edit, lease release, report. Scope (disjoint, ONE file): README.md. Two edits. EDIT 1 — insert a new '## Quickstart' section near the TOP: place it AFTER the intro TLDR (the bold tagline + the paragraph ending 'usage-billed APIs:' and its following ``` box) and BEFORE the '**The loop**' line. Content (markdown), forward-looking to GitHub releases: a short lead 'Grab a prebuilt binary — no build required:' then numbered steps: (1) Download the archive for your OS/arch from the releases page: link https://github.com/jxsl13/spectackle/releases/latest ; artifacts are named `spectackle_<version>_<os>_<arch>.tar.gz` (`.zip` on Windows) for linux/darwin/windows x amd64/arm64, plus `checksums.txt`. (2) Extract and put the `spectackle` binary on your PATH — show a concrete example fenced block: `tar -xzf spectackle_*_linux_amd64.tar.gz && sudo mv spectackle /usr/local/bin/`. (3) Register with any MCP client — reuse the EXISTING JSON mcpServers snippet already in README (command: absolute path to the spectackle binary, args: ["serve"]) — you may reference it: 'point your MCP client at the binary (see the mcpServers JSON below)'. (4) One line: the workspace root auto-detects; nothing else to configure. Add a note: 'No tagged release yet? Build from source (below) until the first release is published.' EDIT 2 — the EXISTING lower '## Quickstart' heading (the `make build` / `./bin/spectackle lint .` one) must be RETITLED to '## Build from source' so there are not two '## Quickstart' headings. Do not change its body. Exit criterion: exactly one '## Quickstart' heading (the new top one) and one '## Build from source' heading; the new section has the releases/latest link + artifact-name pattern + extract/PATH example + MCP-registration pointer; git diff shows only README.md. Verify: grep -c '^## Quickstart' README.md == 1; grep -c '^## Build from source' README.md == 1; grep 'releases/latest' README.md; grep 'spectackle_' README.md. lease release, report the diff. Rollback: git checkout README.md.
- P-01KYAMSC5RF0F937KNJQ9JN830 README Quickstart at top: download from GitHub releases, no build from source: Users should grab a prebuilt binary and start immediately, not build from source. Add a '## Quickstart' section near the TOP of README.md (right after the TLDR intro paragraph + box, before 'The loop'), covering the no-build path: download the release archive for your OS/arch from the GitHub releases page, extract the `spectackle` binary onto PATH, register it with any MCP client, done. goreleaser (.goreleaser.yaml) publishes `spectackle_<version>_<os>_<arch>.tar.gz` (`.zip` on Windows) + checksums.txt for linux/darwin/windows x amd64/arm64 on tag push. The existing lower '## Quickstart' (make build) stays as the from-source path but should be retitled to distinguish it (e.g. 'Build from source'). Note: no git tag/release exists yet, so the download links resolve once the first release is cut — write the section forward-looking with the correct release URL (github.com/jxsl13/spectackle/releases/latest) and artifact naming. Exit criterion (explicit): a top-of-README Quickstart shows the releases download + extract + PATH + MCP-registration steps with the correct artifact name pattern; the old make-build quickstart is retitled so there are not two identically-named sections; ./bin/spectackle lint . clean (README isn't a spec bundle so unaffected); git diff shows only README.md. Rollback: revert README.md. Scope (disjoint): README.md only.
- T-01KYAQSZ00F4ZRP2AE7TD1KD31 goreleaser: version-free archive names + copy-paste README Quickstart: IMPLEMENTER IN A DEDICATED WORKTREE (pre-created, based on HEAD); NO lifecycle moves; only lease claim, edit+verify, lease release, report. Scope (disjoint, EXACTLY two files): .goreleaser.yaml, README.md. DO NOT touch docs/*, internal/* — siblings own those. (1) .goreleaser.yaml: the archives name_template currently is `{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}` — remove the version segment so it becomes `{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}`. Keep formats (tar.gz, zip override for windows), ids and everything else untouched. This makes github.com/jxsl13/spectackle/releases/latest/download/spectackle_linux_amd64.tar.gz resolve. (2) README.md, the TOP '## Quickstart' section only: replace the VERSION/OS/ARCH shell block with a true copy-paste one-liner using the stable latest URL, e.g. `curl -L https://github.com/jxsl13/spectackle/releases/latest/download/spectackle_linux_amd64.tar.gz | tar xz` plus a following line `sudo mv spectackle /usr/local/bin/`, and adjust the surrounding prose: the asset naming sentence must now read `spectackle_<os>_<arch>.tar.gz` (`.zip` on Windows) WITHOUT the version, and mention the other platforms (darwin/windows, amd64/arm64) as a swap of the os/arch words. Do NOT touch the '## Build from source' section or any other part of the README. (3) Verify: `go run github.com/goreleaser/goreleaser/v2@latest release --snapshot --clean --skip=publish` (this is the repo's existing `make release-snapshot` target — run that instead if it is simpler) and confirm dist/ contains files named spectackle_<os>_<arch>.tar.gz WITHOUT a version segment; paste the resulting `ls dist/*.tar.gz *.zip` output into your report. If goreleaser cannot run offline in this sandbox, say so explicitly and instead validate by reading the template and stating the expected names — do NOT fake a run. Also: grep README for 'VERSION=' must return nothing, and grep for 'releases/latest/download' must hit. Rollback: git checkout the two files.
- P-01KYAQSZ00EJ3T2CV45A4SHNZ4 release artifacts: stable latest-download URLs so the Quickstart is copy-paste: The README Quickstart currently needs a VERSION variable because goreleaser's archive name_template embeds the version (spectackle_<version>_<os>_<arch>.tar.gz), so github.com/.../releases/latest/download/<name> cannot resolve without knowing the version. Dropping the version from the archive name makes the canonical `releases/latest/download/spectackle_<os>_<arch>.tar.gz` URL work, turning the Quickstart into true copy-paste. Exit criterion: .goreleaser.yaml name_template omits the version; README Quickstart uses the latest/download URL without a VERSION variable; a goreleaser snapshot build produces the expected file names. Rollback: revert both files. Scope (disjoint): .goreleaser.yaml, README.md.
- T-01KYAR1AB8FTWV3QKQZ15R7JSQ direction-aware drift + one-call heal/audit reporting in check: IMPLEMENTER IN A DEDICATED WORKTREE (already created for you, based on current HEAD); NO lifecycle moves (orchestrator owns them); only lease claim, code+tests, lease release, report. Scope (disjoint): internal/drift/drift.go, internal/drift/drift_test.go, internal/mcpserver/tools.go (the check function only), internal/mcpserver/tools_test.go, docs/tools.md. OUT OF SCOPE (separate follow-ups, do NOT touch): the audit shell-command config, the move-to-done gate, internal/lifecycle. PART A — internal/drift/drift.go: (A1) Add three Class constants alongside the existing ones: `Evolved Class = "evolved"` (code changed, rule sentence identical — the mechanically healable case), `Tightened Class = "tightened"` (code identical, rule sentence changed — the dangerous, never-healed case), `Diverged Class = "diverged"` (both changed). Keep OK/Moved/Gone/Stale/Pending exactly as they are. (A2) Result gains `NewRHash string`. (A3) Classify's 4th parameter changes from `ruleExists func(string) bool` to `ruleText func(string) (string, bool)` — returning the rule's current sentence and whether it exists. Inside: when the rule does not exist -> Stale (unchanged). Otherwise compute `curR := NormHash([]byte(text))` and set Result.NewRHash. Then classify with BOTH axes: codeSame := (h == a.CHash); ruleSame := (curR == a.RHash). codeSame && ruleSame -> OK if file/line also unchanged else Moved (position-only, existing silent-refresh semantics). !codeSame && ruleSame -> Evolved. codeSame && !ruleSame -> Tightened. !codeSame && !ruleSame -> Diverged. Keep the existing Pending/Gone short-circuits ahead of this. Preserve the existing doc comments' style and extend them to explain the two axes. PART B — internal/mcpserver/tools.go, func (s *Server) check ONLY: (B1) The Classify call site currently passes `func(id string) bool { _, ok := c.Rule(id); return ok }` — change it to return the text: `func(id string) (string, bool) { r, ok := c.Rule(id); return r.Text, ok }`. (B2) Handle the new classes: `Moved` keeps its existing silent refresh. `Evolved` -> HEAL: re-stamp the anchor's CHash to r.NewHash via drift.Upsert (set changed=true so drift.Save runs), emit one line `d healed <rule> <node> <file>:<start>-<end> was=<first8 of old CHash> now=<first8 of NewHash>`, and remember the rule ID for the trailer. Also append a journal event for the heal (mirror the existing backprop journal.Append call in s.backprop: use journal.EvDrift with Rule, Node, Cls="healed", Oh=old CHash, Nh=new hash, Dir=ruleCtx(rule.File)) so every heal is auditable. `Tightened` and `Diverged` -> NEVER heal: emit `d audit <rule> <node> <file>:<start>-<end> <class>` and remember the rule ID; when in.Fix is true also run the existing s.backprop path for them. `Gone`/`Stale` keep their current behavior. (B3) RULE-TEXT DEDUP: after the per-anchor lines, emit exactly ONE `r <id> <pattern> <dir> <text>` line per DISTINCT rule that appeared in a healed or audited line (reuse ruleLine(c.Rule(id)) — note it takes a spec.ResolvedRule; use the same rendering the get/find paths use so the grammar matches). Never print a rule twice. (B4) TRAILER: when at least one heal or audit happened, append `ok healed=<N> audit=<M>`. (B5) Keep everything else in check untouched (lint findings, coverage gaps, orphans, duplicate IDs, compact candidates, budget truncation via budget.TruncateRecords with budget.Resume(in.Cur)). PART C — tests. internal/drift/drift_test.go: add one test per quadrant driving Classify directly with a hand-built graph+anchor: Evolved (change the code span, keep the rule text), Tightened (keep the code, pass a DIFFERENT rule text so the RHash mismatches), Diverged (both), and assert OK/Moved still behave as before. internal/mcpserver/tools_test.go: add TestCheckHealsEvolvedAndReportsRule — author a rule with applies to an indexed node, then modify that node's source so the span content changes, then call check and assert: the output contains `d healed <rule-id>`, contains the rule's sentence exactly once (dedup) even if you anchor the same rule to two nodes, contains `ok healed=`, and a SECOND check call afterwards returns ok (the heal stuck). And TestCheckAuditsTightenedNeverHeals — after anchoring, edit the RULE text via the rule tool (rule op=edit id=... response=... which changes the sentence) WITHOUT touching the code, then check must emit `d audit` for it and must NOT heal it (a following check still reports the same audit line). PART D — docs/tools.md, the check section: document the new records (`d healed ...`, `d audit ...`, the deduped `r` line, the `ok healed=N audit=M` trailer) and the two-axis classification (code hash vs rule hash) including the rule that only the code-changed-rule-identical class is ever healed. Verify: go vet ./internal/drift/ ./internal/mcpserver/ && go test ./internal/drift/ ./internal/mcpserver/ -race && go test ./... -race && make build && ./bin/spectackle lint . && the live proof: in your worktree edit an anchored function body (e.g. add a comment line inside go:mcpserver.Server.check), run the check tool via the driver and paste the exact `d healed` + `r` + trailer output into your report, then run check again and show it is ok. Rollback: git checkout the five files.
- T-01KYAR1AB8E4JR6GZ4P83650VP ADR part 2: full ADR fields (Context/Decision/Consequences/Status): IMPLEMENTER IN A DEDICATED WORKTREE (pre-created, based on HEAD); NO lifecycle moves; only lease claim, code+tests, lease release, report. Scope (disjoint, EXACTLY four files): internal/item/item.go, internal/item/item_test.go, internal/mcpserver/decide.go, internal/mcpserver/decide_test.go. DO NOT TOUCH internal/mcpserver/tools.go, docs/*, internal/drift/* — a sibling implementer owns those right now; doc wiring is the orchestrator's job afterwards. Goal: ADR items carry the classic ADR template as first-class structured fields instead of prose. (1) internal/item/item.go: the Item struct gains four string fields — Context, Decision, Consequences, Status. Serialize/parse them in the work.md item block exactly like the existing header fields (kind/state/created/rounds/grilled/needs are already written and parsed there — mirror that mechanism precisely: same `key: value` line style, same writer, same parser, omit empty). Status carries the ADR convention values proposed|accepted|superseded|deprecated; treat an empty Status on an adr item as `proposed`. Do NOT change behavior for non-adr kinds (their fields simply stay empty and are omitted). (2) internal/mcpserver/decide.go: `decide op=ask` accepts an optional `context` argument (add it to the input struct with a jsonschema description like 'ADR context: the forces and constraints behind this decision') and stores it in the minted ADR's Context field, with Status=proposed. `decide op=answer` records the chosen option into Decision, sets Status=accepted, and accepts an optional `consequences` argument stored in Consequences. Keep every existing behavior (elicitation, need-records, option validation, journaling) intact. (3) Tests: item_test.go — a round-trip test writing an adr item with all four fields, re-reading it, and asserting every field survives verbatim including multi-word values, plus a test that a proposal/task item without those fields serializes unchanged (no stray empty keys). decide_test.go — ask with context mints an ADR whose Context is stored and Status=proposed; answer sets Decision + Status=accepted and stores consequences. Verify: go vet ./internal/item/ ./internal/mcpserver/ && go test ./internal/item/ ./internal/mcpserver/ -race && go test ./... -race && make build. Report the exact work.md block of a fully-populated ADR. Rollback: git checkout the four files.
- P-01KYAR1AB8ENTB91YFD2C2QPMK one-call re-audit: check heals the mechanical drift AND reports every heal with its audit material: Synthesis of the two rejected extremes (P-01KYAPBDQREY4S78GCYXQA5DQ1 silent auto-restamp = accepts unverified code; pure manual re-audit = the ten hand calls this cycle). User requirement: ONE tool call that does both — heal what is mechanically safe, and in the SAME response hand the LLM everything needed to validate each heal at a glance, so it can afterwards make dedicated corrections. NOTE the current half-state: check fix=true ALREADY re-stamps Changed anchors and drafts a backprop, but blindly (no direction awareness) and without showing what the contract says or what the code now is — so a reviewer cannot judge it. Design: (1) DIRECTION-AWARE CLASSIFY (from P-01KYAPBDQREY4S78GCYXQA5DQ1, the part that survives): compare BOTH stored hashes — CHash (code span) and the currently unused RHash (rule sentence) — yielding: ok; ImplementationEvolved (code changed, rule identical); ContractTightened (rule changed, code identical — today invisible and the dangerous direction); Diverged (both changed); plus existing gone/stale/pending. (2) HEAL ONLY THE MECHANICAL CLASS: ImplementationEvolved gets its CHash re-stamped in the same call. Nothing else is ever healed automatically. (3) NO SILENCE — every heal is reported in the same response with its audit material, deduped for token economy: one `d healed <rule> <node> <file>:<s>-<e> was=<old8> now=<new8>` line per anchor, plus ONE `r <rule> <pattern> <dir> <text>` line per distinct rule involved (the contract that still binds, printed once even if several anchors share it). That is exactly what a reviewer needs to judge 'does the new code still satisfy this sentence?' without a second round trip. (4) THE RISKY CLASSES ARE NEVER HEALED: ContractTightened and Diverged emit `d audit <rule> <node> ...` with the same rule text, and (fix=true) draft a backprop proposal. (5) MACHINE AUDIT WHERE AVAILABLE: if the workspace configures an audit command (sibling of the existing feedback.grill shell hook in FeedbackCfg), check runs it per healed/audited anchor with the rule text + file + span; a non-zero exit downgrades a would-be heal to `d audit` with the command output as the finding — so heals are machine-verified wherever the project supplies a verifier. (6) TRAILER + FOLLOW-UP: a summary line `ok healed=N audit=M` closes the block; every heal is journaled (auditable, git-reversible), and the LLM's dedicated follow-up is the normal path — rule op=edit if the contract should change, or draft a backprop if the code is wrong. (7) LIFECYCLE GATE: move to=done refuses while the item's scope has open `d audit` obligations (heals do not block). Exit criterion (explicit): unit tests per quadrant incl. an RHash-only change; a check run after editing an anchored function emits `d healed` + the rule text and leaves check ok, with zero manual rule calls; a rule-text-only edit emits `d audit` and never heals; the rule line is printed once for multiple anchors of the same rule; audit-command non-zero exit demotes a heal to audit; move-to-done gated on open audits; go test ./... -race green; docs/tools.md check grammar + docs/lifecycle.md document the classes and the heal/audit split. Rollback: revert; anchors.tsv format unchanged. Scope: internal/drift/drift.go+test, internal/mcpserver/tools.go (check), internal/workspace (audit command config), internal/lifecycle (done gate), docs.
- T-01KYART0CGEJXTE702AFVE4DK0 gate move-to-done on tightened/diverged anchors: IMPLEMENTER IN A DEDICATED WORKTREE (pre-created at .claude/worktrees/impl-t0090, based on HEAD); NO lifecycle moves (orchestrator owns them); only lease claim, code+tests, lease release, report. Scope (disjoint, EXACTLY two files): internal/lifecycle/lifecycle.go, internal/lifecycle/lifecycle_test.go. A sibling implementer owns internal/workspace, internal/spec/cascade.go, internal/index/indexer.go and internal/mcpserver/tools.go right now — do NOT touch them. Goal: an item cannot reach done while its bound contracts carry unresolved audit-class drift. Implementation: in the Move path, when the target state is done (or a forward skip that implies done, i.e. archived), resolve the item's targets, load anchors via drift.Load(ws) and classify them via drift.Classify(ws, g, anchors, ruleText) — study how internal/mcpserver/tools.go's check builds that call (it passes a func(string)(string,bool) returning the rule text from the spec cascade) and mirror it; internal/lifecycle may need the graph + cascade passed in, so extend the existing Move signature/params in the least invasive way that keeps every current call site compiling (update call sites in internal/mcpserver accordingly ONLY if strictly required — if that forces edits outside your two files, STOP and report the blocker instead of touching sibling-owned files; a viable alternative is a new exported helper in internal/lifecycle that the server calls before Move, which keeps your diff inside the two files). Refuse the move when any anchor bound to the item's targets classifies as drift.Tightened or drift.Diverged, returning a dense refusal naming each offender (e.g. '! GATE E <item> audit <rule> <node> <class>'); anchors classifying as drift.Evolved must NOT block (check heals those). Tests in lifecycle_test.go: (1) an item whose target has a tightened anchor cannot move to done and the refusal names the rule and node; (2) after the anchor is reconciled the same move succeeds; (3) an evolved-class anchor never blocks; (4) items with no bound anchors are unaffected. Verify: go vet ./internal/lifecycle/ && go test ./internal/lifecycle/ -race && go test ./... -race && make build. Report the exact refusal record text. Rollback: git checkout the two files.
- T-01KYAS4AF0F9RVYJGFCXB98R5X generic worktree ignore: nested-.git detection + config globs/regex, drop the .claude hardcode: IMPLEMENTER IN A DEDICATED WORKTREE (pre-created at .claude/worktrees/impl-t0091, based on HEAD); NO lifecycle moves; only lease claim, code+tests, lease release, report. Scope (disjoint): internal/workspace/workspace.go, internal/workspace/workspace_test.go, internal/spec/cascade.go, internal/spec/cascade_test.go, internal/index/indexer.go, internal/mcpserver/tools.go (the coverageGaps walk ONLY — nothing else in that file), docs/spec-cascade.md. A sibling implementer owns internal/lifecycle right now — do NOT touch it. Goal: replace the vendor-specific hardcoded '.claude' skip with a generic, harness-independent mechanism plus user-extensible config. VERIFIED FACT to build on (measured on this machine): a git LINKED WORKTREE contains a `.git` FILE (not a directory) whose content is `gitdir: /path/to/repo/.git/worktrees/<name>`; the main working tree and nested clones contain a `.git` DIRECTORY; submodules also carry a `.git` file. So: any subdirectory (below the workspace root, excluding the root itself) that contains a `.git` entry of EITHER kind is a separate git boundary and must be skipped by every walk. (1) internal/workspace: add one exported helper, e.g. `func IsNestedGitBoundary(dir string) bool` (returns true when filepath.Join(dir, ".git") exists, file or dir), plus an ignore matcher that evaluates the workspace Config.Ignore GLOB patterns (already an existing []string field, defaults .git/**, bin/**) AND a NEW `ignore_regex []string` yaml field of RE2 patterns matched against the repo-relative slash path. Export a single entry point the walks share, e.g. `func (r Root) SkipDir(rel, name string) bool` (or a small Matcher type) that returns true for: the nested-git-boundary case, the built-in defaults (.git, node_modules, testdata, bin, vendor, .spectackle), any configured glob, any configured regex. (2) Replace the hardcoded switch lists in internal/workspace ContextDirs, internal/spec/cascade.go Load, internal/index/indexer.go (ignoreDirs map) and internal/mcpserver/tools.go coverageGaps with calls to that shared matcher, and DELETE the '.claude' literal from all four — it is superseded by nested-git detection. Keep index's existing config-glob honoring behavior working. (3) Tests: a nested dir containing a `.git` FILE with gitdir: content is skipped by ContextDirs and by spec Load (mirror the existing TestLoadSkipsClaudeDir but WITHOUT naming .claude — create e.g. tmp/worktrees/x/ with a .git file and a .spectackle bundle inside); a nested dir with a `.git` DIRECTORY is likewise skipped; a config.yaml ignore GLOB excludes a matching dir; an ignore_regex pattern excludes a matching path; defaults still behave as today (this repo lints 15 spec files / 61 rules / 0 findings). (4) docs/spec-cascade.md: document the ignore config (globs + regex) and the automatic nested-git-boundary skip. Verify: go vet ./... && go test ./internal/workspace/ ./internal/spec/ ./internal/index/ ./internal/mcpserver/ -race && go test ./... -race && make build && ./bin/spectackle lint . (must still be 15/61/0). Report the matcher API and the test proving a .git-FILE worktree is skipped without any .claude literal. Rollback: git checkout the seven files.
- P-01KYAS4AF0FTZSK9NNR5FRM8MA generic harness-independent worktree/ignore: unify walks on config.Ignore (globs+regex) + nested-git detection: T-01KYAM2RHGFDESN35P0ZS7DE0S hardcoded '.claude' into four walk skip-lists — but that is Claude-Code-specific and not extensible. The generic, vendor-neutral design: (1) All directory walks consult a SHARED ignore mechanism, not hardcoded switches. Today only IndexAll honors config.Ignore globs (T-01KY9CJV7GETET9DNVFDA656R6); ContextDirs (workspace.go), spec.cascade Load, and check coverageGaps (tools.go) use hardcoded case-lists. Unify them on one workspace-level ignore matcher. (2) The matcher supports config.yaml `ignore:` GLOBS (already a []string field, defaults .git/**, bin/**) AND regex patterns (add support: either an `ignore_regex: []string` field or a `regex:` prefix convention on ignore entries) so users can exclude extra folders, single files, globs, or regex. (3) Harness-INDEPENDENT worktree detection: instead of naming '.claude', skip any subdirectory that is itself a separate git worktree/clone — detected generically by the presence of a `.git` file or dir inside it (git worktrees carry a `.git` FILE pointing to the parent; nested clones a `.git` dir). This catches .claude/worktrees AND any other tool's worktrees with zero vendor coupling. (4) Extend the default ignore set to the sensible union (.git, node_modules, testdata, .spectackle, bin, vendor) so behavior is unchanged for existing repos, and DROP the hardcoded '.claude' (superseded by nested-git detection). Exit criterion (explicit): a config.yaml ignore glob AND a regex pattern both exclude matching dirs/files from spec discovery + index + check; a nested dir containing a .git file is skipped by all walks with no config needed; the four walks share one matcher (no duplicated hardcoded lists); default behavior unchanged on this repo (lint 15/61/0, check ok); go test ./... -race green; docs (spec-cascade.md or a config doc) document the ignore config incl. regex. Rollback: revert diff. Scope: internal/workspace/workspace.go (matcher + config), internal/spec/cascade.go, internal/index/indexer.go, internal/mcpserver/tools.go, their tests, docs. NOTE: collides with the in-flight ADR rename (T-01KYATGSA8EFG9G8ZXFB1XRWB7 touches tools.go) — sequence this AFTER T-01KYATGSA8EFG9G8ZXFB1XRWB7 lands.
- P-01KYAS4AF0EHHBWFJ8DZNZNYGX P-01KYAR1AB8ENTB91YFD2C2QPMK follow-up: gate move-to-done on open audits so drift cannot be archived away: The heal/audit split landed (P-01KYAR1AB8ENTB91YFD2C2QPMK): check heals 'evolved' and surfaces 'tightened'/'diverged' as audits. What is still missing is the lifecycle clamp — nothing stops an item from reaching done/archived while its scope carries open audit obligations, which is exactly how the two tightened anchors in examples/metalcompute slipped through unnoticed for several commits. Add the gate: move to=done (and by implication archived) verifies the anchors bound to the item's targets and refuses while any of them classify as tightened or diverged, naming them; evolved anchors do NOT block (check heals them). The existing rounds/blocked machinery already models gate failures, so a refusal is a normal, recoverable outcome, not an error. Exit criterion (explicit): a lifecycle test proves move-to-done is refused with a dense record naming the offending rule+node while an audit-class anchor is bound to the item's targets, and succeeds once the rule/code are reconciled; evolved-only drift never blocks; go test ./... -race green. Rollback: revert. Scope (disjoint): internal/lifecycle/lifecycle.go, internal/lifecycle/lifecycle_test.go.
- T-01KYASEWBGF8EVRAEKSPHJDJHJ typespass: mirror IndexAll's nested-git-boundary skip in moduleHashKey: IMPLEMENTER IN A DEDICATED WORKTREE (pre-created at .claude/worktrees/impl-b, based on HEAD); NO lifecycle moves; only lease claim, code+test, lease release, report. Scope (disjoint, ONE file plus its test if one exists): internal/index/typespass.go, and internal/index/typespass_test.go if a test file is present. A sibling owns internal/mcpserver — do NOT touch it. Background: internal/index/indexer.go's IndexAll walk skips (a) built-in ignoreDirs by name and (b) any nested git boundary via `p != root && workspace.IsNestedGitBoundary(p)` (a linked worktree/submodule has a .git FILE, a nested clone a .git DIRECTORY). typespass.go's moduleHashKey walk carries a doc comment promising it mirrors IndexAll's walk exactly, but it only got (a). Task: add the same nested-git-boundary skip to moduleHashKey's directory branch, guarded identically so it never fires on the walk root itself, importing internal/workspace like indexer.go does. Keep the doc comment accurate (mention both skips). Test: if internal/index/typespass_test.go exists, add a case proving a .go file under a nested boundary (create a subdir containing a `.git` FILE with `gitdir: /elsewhere` content plus a .go file) does not change the computed module hash key versus the same tree without that subdir; if no test file exists, create a minimal one for exactly that assertion. Verify: go vet ./internal/index/ && go test ./internal/index/ -race && go test ./... -race && make build. Report the diff and the test proving the hash key is unaffected. Rollback: git checkout the file(s).
- T-01KYASQERRFN89JS635AZHMFFZ arm the audit gate: pass WithAuditGate from the server's move path: IMPLEMENTER IN A DEDICATED WORKTREE (pre-created at .claude/worktrees/impl-a, based on HEAD); NO lifecycle moves (orchestrator owns them); only lease claim, code+tests, lease release, report. Scope (disjoint): internal/mcpserver/tools.go (the move handler / lifecycle.Move call sites ONLY), internal/mcpserver/tools_test.go. A sibling owns internal/index/typespass.go — do NOT touch it. Do NOT modify internal/lifecycle (its gate is already implemented and tested). Background: internal/lifecycle/lifecycle.go exports Move(ws, id, to, note string, opts ...MoveOption) plus WithAuditGate(g graph.Graph, ruleText func(string) (string, bool)) MoveOption; without the option the gate is skipped entirely (that is how every current call site still compiles). Task: at the server's lifecycle.Move call site(s) in tools.go, pass WithAuditGate wired exactly like check does it — the graph is s.g, and the ruleText closure loads the spec cascade (see how check builds `func(id string) (string, bool) { r, ok := c.Rule(id); return r.Text, ok }` from spec.Load(s.ws.Dir)) — so that move to=done/archived enforces the gate. Load the cascade once per move call; if spec.Load errors, fall back to NOT passing the option (never fail a move because the cascade could not be read) and keep the existing behavior. The refusal already comes back as a dense record from lifecycle; make sure the server returns it as the tool result text rather than as a Go error, matching how other server-side refusals (e.g. '! ARG E') are surfaced. Test in tools_test.go: build a tempdir workspace over the wire (connectRoot), author a rule with applies to an indexed node, draft+activate an item whose targets include that node, then make the rule text drift (rule op=edit changing the sentence WITHOUT touching the code — that is the tightened class), and assert: move to=done returns the '! GATE E' audit refusal naming the rule and node, the item is still active afterwards (get shows it), and after reconciling (re-stamp via rule op=edit applies, or check) the same move succeeds. Verify: go vet ./internal/mcpserver/ && go test ./internal/mcpserver/ -race && go test ./... -race && make build && ./bin/spectackle lint . (must stay 15 spec files / 61 rules / 0 findings). Report the exact refusal text as returned by the move tool. Rollback: git checkout the two files.
- T-01KYATDTK0FQY89Q2Y8VNS0S60 compact hint at 300 + full default config template: IMPLEMENTER IN A DEDICATED WORKTREE (pre-created at .claude/worktrees/impl-c, based on HEAD); NO lifecycle moves (orchestrator owns them); only lease claim, code+tests, lease release, report. Scope (disjoint): internal/workspace/workspace.go, internal/workspace/workspace_test.go, internal/mcpserver/swarm.go, internal/mcpserver/swarm_test.go (create if absent), docs/lifecycle.md. A sibling implementer owns internal/mcpserver/tools.go and tools_test.go RIGHT NOW — do NOT touch either, and do not touch internal/lifecycle or internal/index. PART A — threshold 300: internal/workspace/workspace.go has the compact default in TWO places: defaultConfig() returns `Compact: CompactCfg{JournalMax: 500, DoneMax: 8}` (~line 70) and load() has a zero-block fallback `if r.Cfg.Compact.JournalMax == 0 { r.Cfg.Compact.JournalMax = 500 }` (~line 146). Change BOTH 500s to 300. Leave DoneMax at 8. Add/extend a test asserting defaultConfig().Compact.JournalMax == 300 AND that a config.yaml with an explicit `compact:\n  journal_max: 0` still yields 300 (zero-block), while an explicit `journal_max: 42` is respected. PART B — self-documenting generated config: EnsureScaffold currently writes a minimal .spectackle/config.yaml. Change ONLY the creation path so a NEWLY created config.yaml contains every setting with its default value and a short trailing comment per line — schema, langs, ignore, ignore_regex, budget_default, compact.journal_max, compact.done_max, swarm.lease_ttl, swarm.agent_ttl, feedback.max_rounds, feedback.grill, worktrees_dir. Read the Config struct's yaml tags and defaultConfig() to get every key and value exactly right; the generated file MUST parse back through load() into a Config equal to defaultConfig() (assert this in a test — generate, re-load, compare field by field). CRITICAL: an EXISTING config.yaml must never be rewritten or touched — add a test that writes a custom config.yaml, calls EnsureScaffold again, and asserts the file bytes are unchanged. PART C — proactive hint in internal/mcpserver/swarm.go: postCall (~line 60) already appends piggybacked sibling `sw` lines to any tool result. Add the compact hint there so ANY tool call surfaces it. Requirements: (a) CHEAP — do not read the journal on every call; count at most once per debounced sync window (look at how preCall's debounced cache sync decides to run and reuse that cadence, or cache the count plus a timestamp on the Server struct); (b) QUIET — emit the hint AT MOST ONCE per threshold crossing: keep a flag/last-emitted-count on the Server, set it when emitted, and only re-arm when the count drops (a compact ran) or grows by another full threshold; (c) grammar — reuse the existing record shape exactly: `c . journal <n> events since last compact` (same as compactCandidates produces), appended like the sw lines are. Test in swarm_test.go (create the file if it does not exist): drive a server over the wire (use the connectRoot/callText helpers pattern — if they live in tools_test.go which you must not edit, write local equivalents in swarm_test.go), append more than 300 journal events to the root bundle via ordinary tool calls or by seeding the journal file directly, then assert an unrelated tool call's result carries the `c . journal` line, and that the IMMEDIATELY following identical call does NOT carry it again. PART D — docs/lifecycle.md: document the compact threshold (default 300, config key compact.journal_max) and that the server proactively surfaces the hint on any tool result once per crossing. Verify: go vet ./internal/workspace/ ./internal/mcpserver/ && go test ./internal/workspace/ ./internal/mcpserver/ -race && go test ./... -race && make build && ./bin/spectackle lint . (must stay 15 spec files / 61 rules / 0 findings). Report the generated config.yaml verbatim and the test output proving the hint fires once. Rollback: git checkout the five files.
- P-01KYATDTK0F4HR19TA1406X3FZ proactive compact hint at 300 journal events + self-documenting generated config: Requirement: the server must point out by itself when a compact is due (default threshold 300 journal events), and ship a global auto-generated config carrying all defaults. Current state (measured): compact.journal_max defaults to 500 in two places (defaultConfig() and the zero-block fallback in load()); the c-record is only produced by compactCandidates, which runs inside check and compact — so an agent that never calls check is never told, and this repo silently reached 506 events before anyone noticed. Design: (1) THRESHOLD 300 — change both default sites from 500 to 300; a config.yaml value keeps overriding it. (2) PROACTIVE HINT — postCall in internal/mcpserver/swarm.go already piggybacks sibling sw records onto ANY tool result; add the compact hint the same way so it reaches the agent regardless of which tool it called. It must be CHEAP and QUIET: do not read the journal on every call — evaluate at most once per debounced sync window, cache the count on the Server, and emit the hint AT MOST ONCE per threshold crossing (re-arm only after a compact runs or after the count grows by another threshold), so it never becomes per-call noise (respect the output-diet rejection R-01KY90ADKREDGV3TN10CFBAVZR). Record shape reuses the existing grammar: `c . journal <n> events since last compact`. (3) SELF-DOCUMENTING GENERATED CONFIG — EnsureScaffold writes .spectackle/config.yaml; make the generated file list every setting with its default value and a one-line comment (schema, ignore, ignore_regex, budget_default, compact.journal_max/done_max, swarm.lease_ttl/agent_ttl, feedback.max_rounds/grill, worktrees_dir), so a user can discover and tune everything without reading Go source. Existing config files must NOT be rewritten — only newly created ones get the full template. Exit criterion (explicit): defaults are 300 (unit test on defaultConfig and on the zero-block fallback); a fresh scaffold produces a config.yaml containing every key with its default and comments, and re-running EnsureScaffold on an existing config leaves it byte-identical; a server test proves the hint appears on an unrelated tool call once the journal exceeds the threshold and does NOT repeat on the next call; go test ./... -race green; lint 15/61/0. Rollback: revert. Scope: internal/workspace/workspace.go + test, internal/mcpserver/swarm.go + test, docs/lifecycle.md.
- P-01KYATDTK0E9JSVXBZS5MTBAK0 close the two loose ends: arm the audit gate + typespass nested-git parity: Two implementer-reported loose ends from P-01KYAS4AF0FTZSK9NNR5FRM8MA/P-01KYAS4AF0EHHBWFJ8DZNZNYGX, both deliberately deferred because they touched sibling-owned files at the time, both now collision-free. (1) THE AUDIT GATE IS DORMANT: internal/lifecycle grew Move(..., WithAuditGate(g, ruleText)) with five passing tests, but the MCP server never passes the option, so move to=done still succeeds while tightened/diverged anchors are bound to the item's targets — the gate exists but does not fire in production. Wire it at the server's Move call sites so the refusal record actually reaches the agent. (2) TYPESPASS CACHE-KEY PARITY: internal/index/typespass.go's moduleHashKey walk documents that it 'mirrors IndexAll's exactly', but IndexAll gained the nested-git-boundary skip while moduleHashKey did not, so a .go file inside a nested worktree still feeds the typed-call cache key. Not a correctness bug (the cache key just covers more files than it should) but a documented invariant that is now false, and cheap to restore. Exit criterion (explicit): a server-level test proves move to=done is refused (with the '! GATE E ... audit ...' record) while an audit-class anchor is bound, and succeeds after reconciliation; moduleHashKey skips nested git boundaries exactly like IndexAll; go test ./... -race green; lint 15/61/0. Rollback: revert. Scope: internal/mcpserver (Move call sites + test) and internal/index/typespass.go — two disjoint task scopes.
- ADR-01KYA0YFRRE508YVYDEJ8648FN Rebranding spectacle→spectackle: wie tief? Der Go-Modulpfad (github.com/jxsl13/spectacle) muss zum GitHub-Repo-Namen passen — ein kompletter Modulpfad-Rebrand setzt voraus, dass du das Repo auf GitHub in spectackle umbenennst. Der .spectacle-Workspace-Ordner ist zudem das persistierte Format bestehender Nutzer-Repos.: compact
- ADR-01KYAHT690ERFACEYDP9Q75RKF wazero/WASM tree-sitter backend: ADR-01KY9NJ3KRFF8RPNARZMXP90VF deferred it based on R-01KY9MRDB8F6KBJGFH2AQH45SD (binary size + latency vs the langspec regex chain, which since then reached 30 languages incl. ObjC/Metal/GLSL and cleared the M4 perf gate 7-15x). 'nix deferred' — reopen and build it now, or keep deferred as an explicit, measurement-justified choice?: compact
- T-01KYATGSA8EFG9G8ZXFB1XRWB7 ADR rename part 1: decision->adr, D->ADR, find scope=adr + indexer: IMPLEMENTER IN A DEDICATED WORKTREE; NO lifecycle moves (orchestrator owns them); only lease claim, code+test, lease release, report. This is the RENAME + SEARCHABLE foundation; ADR fields (Context/Decision/Consequences/Status) and data migration are SEPARATE follow-up tasks — do NOT do them here. Scope (disjoint): internal/item/item.go, internal/lifecycle/lifecycle.go, internal/mcpserver/decide.go, internal/mcpserver/tools.go, internal/mcpserver/tools_test.go, internal/mcpserver/decide_test.go (if exists), internal/sync/sync.go, docs/tools.md. Steps: (1) lease claim item=T-01KYATGSA8EFG9G8ZXFB1XRWB7 with those paths. (2) item.go: in the kinds->ID-letter map change '"decision": "D"' to '"adr": "ADR"'; change IDRe from `^[PTBRD]-\d{4}$` to `^(?:ADR|[PTBR])-\d{4}$` (multi-char ADR prefix alongside single-letter P/T/B/R); update the '// IDRe matches ... ADR-00000006TRFG3V9MS530RBWTR4 (decision)' comment to '... ADR-00000006TRFG3V9MS530RBWTR4 (adr)'. Search item.go for any other 'decision'/'D-' literal and rename to 'adr'/'ADR'. (3) lifecycle.go: change Draft(ws, mint, "decision", ...) (Escalate, ~line 300) to "adr"; rename 'decision item' wording in comments to 'ADR'. (4) decide.go: wherever it drafts/creates the item kind (grep for "decision"), change to "adr" so decide mints ADR-NNNN; update any 'D-' rendering. (5) tools.go: scopeKinds map add '"adr": {"adr"},' (keep 'decision' OUT — full rename, no alias); findIn Scope jsonschema enum/prose: replace any 'decision' with 'adr' and ensure 'adr' is listed. Also the draftIn kind jsonschema if it enumerates kinds — add/rename to include adr if decisions are draftable there. (6) sync.go: in feedWork's indexed-kinds list (the gap T-01KYAKD17GEHXA7W0W47FC5WGK found — grep for the kinds slice ~line 55), add "adr" so ADR items are FTS-indexed (this is REQUIRED or find scope=adr returns nothing). (7) Server instructions/prompts: grep internal/mcpserver for user-facing 'decision'/'D-xxxx' wording in the instructions const + prompts.go and rename to ADR (keep it accurate). (8) docs/tools.md: the decide section (14) and find schema (1) — rename decision->adr, D->ADR, find scope enum gains adr. (9) Tests: add TestFindScopeADR (mirror the pattern: draft/create an adr item, find scope=adr q=<word>, assert the ADR-id appears) and update any existing test referencing D-/decision/scope=decision. VERIFY EMPIRICALLY that find scope=adr returns a created ADR (the indexer step is essential). Verify: go test ./... -race (FULL suite — the rename touches many packages; all must pass); go vet ./...; make build; ./bin/spectackle lint .; and a live round-trip via the driver in your worktree: decide op=ask ... creates an ADR-NNNN (or need-record), then find scope=adr finds it. (10) lease release. Report: the full diff summary, the live find scope=adr round-trip output, full-suite result. Rollback: git checkout the touched files. NOTE: existing ADR-01KY9NJ3KRFF8RPNARZMXP90VF/03/04 in .spectackle are NOT migrated here (separate task) — after this rename they may show as unrecognized D- ids; that is expected and the migration task fixes it. Do NOT edit .spectackle files.
- P-01KYATGSA8EWCRM4VRTBESZCYT ADR as a first-class kind: rename decision->adr / D->ADR, full ADR fields, searchable, migrate: User requirement (structured, decided): architecture decisions must be first-class structured records in spectackle, named ADR (industry term), defined+searched via the MCP, never unstructured docs markdown. Scope decided by user: FULL migration of existing records AND FULL ADR fields. Four coupled parts: (1) RENAME kind decision->adr and ID letter D->ADR everywhere: internal/item/item.go (kinds map 'decision':'D' -> 'adr':'ADR'; IDRe '^[PTBRD]-\d{4}$' -> '^(?:ADR|[PTBR])-\d{4}$' to allow the multi-char prefix; any letter-parsing helpers), internal/lifecycle/lifecycle.go (Draft(...,'decision',...) -> 'adr'; comments), the draft-tool kind enum + decide tool (mints kind adr, ID ADR-NNNN), state/find rendering ('i <id> adr ...'), and the server instructions/prompts wording. (2) FULL ADR FIELDS: extend the item model + work.md block serialization with structured Context, Decision, Consequences, Status (classic ADR template) as first-class fields; the decide tool's ask/answer captures Context+options and records Decision+Consequences+Status on answer; get/state/find render them. (3) SEARCHABLE: find scope=adr in scopeKinds + findIn schema, AND the sync indexer must feed kind=adr into FTS (internal/sync feedWork — this was the real gap T-01KYAKD17GEHXA7W0W47FC5WGK found for decision; carry it forward for adr), so ADR bodies incl. context/consequences are queryable. (4) MIGRATE: rewrite existing ADR-01KY9NJ3KRFF8RPNARZMXP90VF/ADR-01KYA0YFRRE508YVYDEJ8648FN/ADR-01KYAHT690ERFACEYDP9Q75RKF in root .spectackle/work.md + journal.ndjson to ADR-01KY9NJ3KRFF8RPNARZMXP90VF/03/04 (one-time pre-v1 migration of this repo's own dogfood data), reindex, verify check ok + find scope=adr returns them; and migrate the wazero design rationale from docs/design-wasm-parsers.md into ADR-01KYAHT690ERFACEYDP9Q75RKF's structured Context/Decision/Consequences (the doc's Recommendation section becomes the ADR, leaving the measurement appendix). Exit criterion (explicit): draft kind=adr mints ADR-NNNN with Context/Decision/Consequences/Status; decide ask/answer populates them; find scope=adr returns ADRs by body text incl. consequences; the three legacy items are ADR-000x and check is ok; go test ./... -race green; ./bin/spectackle lint . clean; docs/tools.md decide+find sections document adr/ADR and the fields. Rollback: revert diff; migration is idempotent string rewrite. Scope: internal/item/**, internal/lifecycle/lifecycle.go, internal/mcpserver/** (decide/find/draft/state/tools + tests), internal/sync/sync.go, docs/tools.md, docs/design-wasm-parsers.md, root .spectackle/ (migration). Large + coupled: staged as tasks; orchestrator owns moves + verifies the migration.
- T-01KYATJ0C8EESA0NBAKDFQTKVD docs: ADR as a first-class searchable record in README + tools.md: IMPLEMENTER IN A DEDICATED WORKTREE (pre-created at .claude/worktrees/impl-d, based on HEAD); NO lifecycle moves; only lease claim, edit+verify, lease release, report. Scope (disjoint, docs only): README.md, docs/tools.md, docs/lifecycle.md. A sibling implementer owns internal/migrate, cmd/spectackle RIGHT NOW — do NOT touch any Go file. Goal: the docs must present ADRs as what they now are — first-class, structured, searchable lifecycle records — because that is the whole point of the rename and the reason users should not keep architecture decisions in loose markdown. (1) README.md Status table: the lifecycle row currently reads 'Spec lifecycle: draft/move/archive, revocable rejections, journal corpus'. Extend it to also name ADRs, e.g. '... journal corpus, ADRs as first-class structured records (context/decision/consequences/status, `find scope=adr`)'. Keep the row's ✅ live marker and the table format intact. (2) docs/tools.md: verify the `decide` section (numbered 14) describes the ADR reality — kind `adr`, ids `ADR-nnnn`, the four structured fields Context/Decision/Consequences/Status captured by ask/answer, and `find scope=adr`; fix any remaining `decision`/`D-` wording there. Also confirm the find tool's scope enum lists `adr` and the draft tool's kind enum lists `adr` (both were updated earlier — if they are already correct, say so and change nothing rather than churning the file). (3) docs/lifecycle.md: add a short subsection explaining that architecture decisions live in the lifecycle as ADR items (queryable via find scope=adr, drift-anchored like any other record) instead of in unstructured docs, and that the four ADR fields follow the classic ADR template. Constraints: prose only, no invented behavior — every claim must match what the code actually does (read internal/mcpserver/decide.go and internal/item/item.go to confirm field names and the ask/answer capture before writing). Verify: grep README.md for 'scope=adr'; grep docs/tools.md for 'ADR-'; ./bin/spectackle lint . (must stay 15/61/0); git diff --name-only must list only the three doc files. Report the three diffs. Rollback: git checkout the three files.
- P-01KYATGSA8EGYRQ8ZYWV6AEZS6 ADR migration tool: server-side rewrite of legacy D-ids, and stale pre-rebrand rule text: Two related gaps found by inspecting the living spec after the ADR rename landed. (1) MIGRATION IS BLOCKED WITHOUT A TOOL: the user asked for a FULL migration of legacy decision records to ADR ids. Parts 1+2 shipped (kind/ID rename, ADR fields, searchable). Part 3 cannot be done the way the others were: the two legacy records ADR-01KYAVBDVGF5RV88VGFYR8J1FK/ADR-01KYAVBDVGEZP9G0SVH1W6C85K are now archived, so they live only as journal tombstones, and rewriting journal history by hand is exactly what the architecture forbids (the LLM never edits .spectackle; only the server writes it). The honest fix is to give the SERVER a migration path: a `migrate-adr` CLI subcommand that rewrites D-<nnnn> ids to ADR-<nnnn> across every .spectackle work.md and journal.ndjson in the workspace, dry-run by default, idempotent, refusing to run when a target ADR id already exists. That turns a forbidden hand-edit into an auditable, testable server operation. (2) STALE PRE-REBRAND RULE TEXT: root spec.md still carries pre-rebrand wording — SPX-ARC-001 says 'The spectacle server' and SPX-ARC-005 says '.spectacle folders' (single c, the old name), and SPX-SWM-007 still says the server mints 'a linked `decision` item' although the kind is now adr. These are contract sentences an agent reads as ground truth, so the drift is user-visible and worth fixing via rule op=edit (server write path, orchestrator's job). Exit criterion (explicit): a migrate-adr subcommand exists, defaults to dry-run listing every id it would rewrite, applies only with an explicit flag, is idempotent (second run reports nothing), refuses on id collision, and is covered by tests over a fixture workspace containing both a live work.md item and journal tombstones; the three stale rule sentences read correctly (spectackle, .spectackle, adr) and lint stays clean. Rollback: revert the tool; rule edits are revertible via rule op=edit. Scope: internal/migrate (new) + cmd/spectackle/main.go + tests for the tool; the rule text fixes are orchestrator-side rule calls.
- T-01KYAVKYA8EEAAQSC76TN2ZGSB minter must not reuse ids whose create event was compacted away: IMPLEMENTER IN A DEDICATED WORKTREE (pre-created at .claude/worktrees/impl-n, based on HEAD); NO lifecycle moves (orchestrator owns them); only lease claim, code+tests, lease release, report. Scope (disjoint, EXACTLY two files): internal/lifecycle/lifecycle.go, internal/lifecycle/lifecycle_test.go. A sibling implementer owns internal/mcpserver/tools.go and tools_test.go — do NOT touch them. THE BUG: maxNum(ws, kind) (~line 632) scans journal CREATE events to find the highest used number for a kind. compact folds create/move/rule/drift events away (it keeps reject/archive/compact), so after a compact the ids of folded-away items are invisible and get minted a second time. Proven live in this repo TWICE: after a compact, four new ADRs were minted as ADR-01KY9K7B18E7E970WQKGQ2MTR7..0004 (numbers of four already-archived records), and a new proposal was minted as P-01KYAVJT60FV7BK5W3NC3WGTMP, the number of an already-archived proposal. This affects every kind (P/T/B/R/ADR) because they all mint through this path. THE FIX: maxNum must take the maximum over EVERY source that can still witness a used id — (a) journal events of ANY event type that carry both an id and the kind field k (archive and reject events survive compaction and carry them; keep create too), and (b) the live items currently in work.md across the workspace's context dirs (item.LoadAll or whatever this package already uses). Read the existing implementation first and keep its structure and idioms; the change is widening the scan, not rewriting the minter. Preserve the existing behavior that coord-backed minting (the Minter passed into Draft) still wins when present — you are fixing the local floor it is given. TESTS in lifecycle_test.go: (1) the regression: build a workspace where an item of some kind was created and archived, then rewrite its journal so ONLY the archive event remains (simulating a compact that folded the create away) and assert the next Draft of that kind mints a HIGHER number, never the archived one; (2) same for a rejected item (reject events also survive); (3) a live work.md item still bounds the floor; (4) an empty workspace still starts at 0001. Verify: go vet ./internal/lifecycle/ && go test ./internal/lifecycle/ -race && go test ./... -race && make build && ./bin/spectackle lint . (must stay 15/61/0). Report the diff and the regression test output. Rollback: git checkout the two files.
- T-01KYAVKYA8EV3BM7C35KNS1XWM get must render the ADR fields (context/decision/consequences/status): IMPLEMENTER IN A DEDICATED WORKTREE (pre-created at .claude/worktrees/impl-o, based on HEAD); NO lifecycle moves; only lease claim, code+tests, lease release, report. Scope (disjoint, EXACTLY two files): internal/mcpserver/tools.go (the getItem rendering path ONLY — do not touch check, compact, find, draft or the move handler), internal/mcpserver/tools_test.go. A sibling implementer owns internal/lifecycle — do NOT touch it. THE GAP: internal/item stores the four ADR fields correctly (verified: a real ADR block in .spectackle/work.md contains context:, decision:, consequences: and status: lines) and the Item struct carries Context/Decision/Consequences/Status, but getItem's output never prints them — so `get ADR-01KYAVBDVGF5RV88VGFYR8J1FK` shows only the title, the kind/state/created header and the radio options/choice, and an agent cannot read the structured record the ADR feature exists to provide. THE FIX: in getItem, after the existing header/body rendering, emit the four fields when non-empty, in the classic ADR order context, decision, consequences, status, using the same dense one-field-per-line style the item header already uses (look at how rounds/grilled/needs are rendered and match it exactly — do not invent a new record letter or a JSON shape). Empty fields stay omitted (output diet, R-01KY90ADKREDGV3TN10CFBAVZR). Non-adr items must render byte-identically to today (their fields are empty) — assert that. TESTS in tools_test.go: (1) drive the decide tool over the wire to create an ADR with a context, answer it with a choice and consequences, then assert `get <ADR-id>` output contains the context text, the decision text, the consequences text and `status: accepted`; (2) assert a plain proposal's get output is unchanged (no stray empty field lines). Verify: go vet ./internal/mcpserver/ && go test ./internal/mcpserver/ -race && go test ./... -race && make build && ./bin/spectackle lint . (must stay 15/61/0). Report the exact get output for an ADR. Rollback: git checkout the two files.
- T-01KYAVRDW0EKVT4N5K1512ZG8K add BROWNFIELD and RECORDS paragraphs to the instructions manifest: IMPLEMENTER IN A DEDICATED WORKTREE (pre-created at .claude/worktrees/impl-p, based on HEAD); NO lifecycle moves; only lease claim, edit+tests, lease release, report. Scope (disjoint, EXACTLY three files): internal/mcpserver/server.go, internal/mcpserver/tools_test.go (the instructions test lives there — see TestInstructionsTeachTokenEconomy), docs/agent-workflow.md. Siblings own internal/lifecycle and the getItem path of internal/mcpserver/tools.go — you touch tools_test.go only, never tools.go. EDIT 1 — server.go: the instructions const is one Go raw string whose paragraphs are newline-separated (lifecycle loop, SWARM:, ORCHESTRATION:, TOKEN ECONOMY:). Append two more paragraphs, in this order, verbatim: >>>BROWNFIELD IMPORT: onboarding an existing repo, in order. (1) Index first: state/reindex yields the graph immediately and costs no decisions — you get the real node IDs everything anchors to. (2) Survey in parallel: fan out READ-ONLY subagents over disjoint subtrees (one per top-level package/module); each reports the subtree's purpose, the invariants its code/tests/docs already assert, and candidate contracts. Read-only means no leases and no work.md contention, so fan out as wide as the tree. (3) Mint centrally: the orchestrator turns that survey into rule op=add contracts, scoped per context dir and anchored via applies to node IDs from step 1 — implementers never hand-write spec files, the server composes and lints. (4) Capture decisions: existing design/ADR documents become adr items via decide (context, decision, consequences, status); pure reference docs stay docs. (5) Baseline: run check until clean — the stamped anchors are the point from which drift detection means anything. (6) Then the normal loop: find scope=rejection, draft, grill, implement. Encode only invariants the code/tests/docs actually assert, never invented ones; start with the load-bearing few and let check's coverage gaps show what is still unowned.<<< and >>>RECORDS: write item bodies as compacted substance — constraints, decisions, measurements, rejected alternatives and why. Never paste verbatim user quotes or transcript excerpts: they bloat every later read of the record without adding information. Same token-economy family as the output diet.<<< Both texts contain no backticks, so they paste safely into the raw string. EDIT 2 — tools_test.go: extend the existing instructions test (or add a sibling test next to it, matching its style) asserting the manifest contains BROWNFIELD IMPORT, the phrase 'Survey in parallel', RECORDS, and 'Never paste verbatim'. EDIT 3 — docs/agent-workflow.md: add a section titled 'Importing a brownfield repo' presenting the same six steps in prose with the two guardrails, placed after the Fan-out section and before the worktree/lifecycle-state section. Keep it tight (roughly 25-35 lines) and consistent with the document's existing voice. Verify: go vet ./internal/mcpserver/ && go test ./internal/mcpserver/ -race && go test ./... -race && make build && ./bin/spectackle lint . (must stay 15/61/0). Report the two manifest paragraphs as they ended up and the test result. Rollback: git checkout the three files.
- P-01KYAVJT60FV7BK5W3NC3WGTMP ID reuse after compact (all kinds) + get does not render the ADR fields: Two defects found while migrating the design docs to ADRs, one of them serious. (1) ID REUSE AFTER COMPACT — lifecycle.maxNum (lifecycle.go ~632) determines the next item number by scanning journal CREATE events. compact deliberately folds create/move/rule/drift events away, keeping only reject/archive/compact. So once a context's journal has been compacted, every id whose create event was folded becomes invisible to the minter and gets handed out again. Observed live: after this repo's compact, minting four new ADRs produced ADR-01KY9K7B18E7E970WQKGQ2MTR7..ADR-01KYAHT690ERFACEYDP9Q75RKF — the numbers of four already-archived historical records, silently shadowing their tombstones (get ADR-01KY9K7B18E7E970WQKGQ2MTR7 would resolve to the new item, the old record unreachable by id). This is not ADR-specific: proposals, tasks, bugs and research items are all minted through the same path, so any compacted repo can reissue P-/T-/B-/R- ids. Fix: maxNum must consider every journal event that carries an id for the kind (archive and reject events survive compaction and carry both id and k), plus live work.md items, not just creates — take the maximum across all of them. (2) GET DOES NOT RENDER THE ADR FIELDS — internal/item stores context/decision/consequences/status correctly (verified in work.md), but getItem's rendering never prints them, so an agent that fetches an ADR sees only question/options/choice and cannot read the very structure the ADR feature exists for. Exit criterion (explicit): a lifecycle test proves that after folding a create event out of a journal, minting the next id of that kind does NOT reuse the archived item's number; a server test proves get on an ADR renders context/decision/consequences/status; the four existing ADRs (0008-0011) still resolve; go test ./... -race green; check ok; lint 15/61/0. Rollback: revert. Scope: internal/lifecycle/lifecycle.go + test for the minter; internal/mcpserver/tools.go (getItem only) + tools_test.go for the rendering — two disjoint task scopes.
- P-01KYAVQFK8FQET9DDF51XBWTBA instructions: brownfield import recipe + compacted record-keeping rule: Two additions to the self-bootstrapping instructions manifest. (A) BROWNFIELD IMPORT — the manifest teaches the change loop but assumes a repo that already has contracts; it says nothing about onboarding an existing codebase, which is the normal first contact. Worked-out approach, six steps, each mapped to existing tools so nothing new is built: (1) INDEX FIRST — the graph costs nothing to obtain and needs no decisions; state/reindex yields nodes and edges immediately, giving real node ids to anchor against later. (2) SURVEY IN PARALLEL — fan out READ-ONLY subagents over disjoint subtrees (one per top-level package or module), each reporting the subtree's purpose, the invariants its code, tests and docs already assert, and candidate contracts. Read-only means no leases and no work.md contention, so the fan-out is unconstrained. (3) ORCHESTRATOR MINTS — the orchestrator turns the survey into rule op=add contracts scoped per context dir, anchored via applies to real node ids from step 1. Implementers never hand-write spec files; the server composes and lints. (4) CAPTURE EXISTING DECISIONS — design or ADR-style documents become adr items via decide (context, decision, consequences, status); pure reference documentation stays documentation. (5) BASELINE THE DRIFT — run check until clean; the stamped anchors are the baseline from which drift detection becomes meaningful. (6) THEN THE NORMAL LOOP — find scope=rejection, draft, grill, implement. Guardrails to state explicitly: encode only invariants the code, tests or docs actually assert, never invented ones; start with the load-bearing few; let check's coverage gaps reveal what is still unowned rather than trying to cover everything at once. (B) COMPACTED RECORDS — item bodies must record user input as compacted substance (constraints, decisions, measurements, rejected alternatives), never as verbatim quotes, which bloat records without adding information. This is a token-economy rule of the same family as the existing output diet. Exit criterion: the instructions manifest carries both paragraphs, concise enough not to bloat the handshake; a test asserts both are present; docs/agent-workflow.md documents the brownfield recipe in prose; go test ./... -race green; lint 15/61/0. Rollback: revert. Scope: internal/mcpserver/server.go, internal/mcpserver/server_test.go or the existing instructions test, docs/agent-workflow.md.
- T-01KYAWGR68FMRT8GVJJBHHPVM4 retire the three stale D-000x prose references and refresh the M6 roadmap verdict: three dead D-000x prose references retired. roadmap M6 now states the live verdict (cgo past M6 per ADR-01KYAVBDVGEZP9G0SVH1W6C85K, wazero gated on wasi-sdk CUDA/ObjC grammar wasm per ADR-01KYAVBDVGF5RV88VGFYR8J1FK) instead of the superseded defer. design-wasm-parsers points its two mentions at ADR-01KYAVBDVGEZP9G0SVH1W6C85K and ADR-01KYAVBDVGF5RV88VGFYR8J1FK. decide.go names no record: the described shape (option text containing commas) matches three of the four ADRs, so no unique referent exists and the comment now describes the shape generically. Orchestrator fixed one ungrammatical clause in that rewrite.
- P-01KYAWDTE8F03AMAZWK5KHNAE5 purge stale pre-rename D-000x references from prose and comments: delivered by T-01KYAWGR68FMRT8GVJJBHHPVM4. test fixtures using ADR-00000000Z8F39R4FMZ3SENEBCF/ADR-00000001YGEJ0SNB3SVJD39N4M as grammar samples were correctly left alone.
- T-01KYB12WYGF9DVB5NS89PMZVVY README: mermaid flow diagram and a reference for every persisted structure: README gains a concepts section built around what one /spectackle invocation triggers, plus a reference for every persisted structure. All three mermaid blocks render cleanly, verified twice: the implementer rendered them with mermaid-cli and iterated when notes tokenized wrong and when a feedback edge broke dagre layout; the orchestrator re-rendered all three independently against the preinstalled chromium, zero parse errors. Concepts prose explains why the system is shaped this way -- cascading rules, EARS as a lint error rather than a style nit, the two-hash drift axes, why nothing is discarded, why exploration sits with the orchestrator -- before any file name appears.
- P-01KYB0ZHH0EDV9DET8MJBRC8WT README: a mermaid diagram of the whole flow and a reference for every persisted structure: delivered by T-01KYB12WYGF9DVB5NS89PMZVVY.
- ADR-01KYAVA0Y0E7B99G59FTGY0S2S Should GoParser.callEdges keep minting call edges syntactically, or add a go/types semantic pass to fix silently-dropped chained-selector calls?: compact
- ADR-01KYAVA0Y0FS8V0R9PAVDD1HSK Should spectackle build node-removal machinery for incremental IndexPaths, or keep the full-IndexAll rebuild?: compact
- ADR-01KYAVBDVGF5RV88VGFYR8J1FK Given correctness-first evaluation, should spectackle stay on cgo tree-sitter, adopt wazero/wasm for C/C++ only, or secure WASI grammars for CUDA/ObjC first?: compact
- ADR-01KYAVBDVGEZP9G0SVH1W6C85K Scored against the four PoC exit criteria, should spectackle adopt malivvan/tree-sitter now, invest in a wasi-sdk pipeline, or stay on cgo past M6?: compact
- T-01KYB2318RF7VA1NMCWQVJ4HH9 make dev: one idempotent command that rebuilds and restarts the resident server: code merged to main via ff at b75b4ad before the submit crash (make dev/dev-stop/dev-status, CONTRIBUTING/README invariant); worktree record delta was empty, so the abort discarded nothing — verified by implementer transcript: idempotent dev, stale-pidfile recovery, occupied-port failure, make all green
- P-01KYB215PGE50SAKGC8CV4Z1W6 the dev server always runs the current build: one command to rebuild and restart, and a hint when it drifts: both halves shipped: make dev/dev-stop/dev-status (T-01KYB2318RF7VA1NMCWQVJ4HH9) and the MCP-010 stale-binary hint (T-01KYB2318RFFGV6NA9WBWABMYB); the dev loop now rebuilds, restarts and self-reports staleness
- B-01KYC5ZJ6GF8ZT6MZBD34S9NZ4 replay rejects worktree journals inheriting pre-eid baseline events; suggested compact cannot clear them: fix live at beafdc3: replay skips baseline pre-eid events by (t,ev,id) key; verified by T-01KYB12WYGE1XVCQY5PF93M8C3/T-01KYB2318RFFGV6NA9WBWABMYB submits replaying over the 7 legacy archive events
- B-01KYC8SG48F3GTBHSV4A3DJ5SA MergeMain hardcodes the branch name main, so submit silently merges a stale ref and dies at the fast-forward on repos developing on another branch: fix live at 8c82a7f: MergeMain resolves the primary checkout branch; verified by both submits merging the real tip
- B-01KYC94PH0FFDS3059GMVGBD2X CommitCode misreads unstaged .spectackle changes as staged: the shared git() helper trims the leading space off the first porcelain status line: fix live at fe340ea: stagedness via git diff --cached --quiet exit code; verified by both submit retries no-opping cleanly on record-only dirt
- B-01KYC9CANGEQCVAGA74F3A11JZ worktree .spectackle live state blocks MergeMain: seeded uncommitted by copyBundles, excluded by CommitCode, refused by git when main's tip touches the same files: fix live at 0169bef: MergeMain preserves live .spectackle bytes across the merge; verified by both submits merging over dirty bundle files
- T-01KYC9QYQRF5JAF0N9S9QQGKD3 surface item Refs: draft accepts them, get renders them, grill demands deliberation: refs live end to end: draft validates against live+tombstoned IDs and refuses unknowns, get renders the refs line, grill asks for a recorded deliberation on proposals without one; variadic lifecycle.Draft kept every existing call site compiling
- R-01KYCC8MRRFAMBKA8E6HKPFP7F per-language parser gap analysis: langspec chain vs tree-sitter fidelity for symbol extraction and graph search: 32/32 language agents reported (empirical scratch-workspace probes against the real indexer). Verdicts: 30 major-gaps, 2 minor (go, lua), 0 clean. Recurring gap classes: (1) whole construct classes lack Def patterns — JS/TS class methods (10/19 and 10/23 symbols found), Java constructors, Python async def, ObjC @protocol, Julia macros, Fortran derived types, GLSL structs, Zig error sets, OCaml and-chains, C# Allman braces, Dart arrow bodies; (2) modifier-whitelist regexes reject idiomatic forms — Rust const fn, Swift override init, Groovy default visibility, CUDA static __global__, asm <> static and <ABIInternal> symbols; (3) end-terminated languages (ruby, lua, elixir, julia, fortran) get single-line spans and no brace-count body extent; (4) CallRe is nil for lua/perl/php/r/shell — impact BFS fully blind there; (5) hand-written Go is node-perfect but skips call edges in closure-var bodies and explicit generic instantiation Foo[T](). Honest caveat: samples were deliberately adversarial, so found/expected ratios understate typical-corpus recall, but the missed constructs are bread-and-butter idioms, not exotica. The C-PoC parity PASS (1151/0/0) does not contradict this: its corpus was synthetic-friendly; this probe targeted the regex chain's known blind spots. Full per-language matrix in the workflow journal wf_0bccbbd8-5fd.
- T-01KYCS7M28EN3V5R0H1BWZG3T4 parse and typed-call caches: mix a parser-identity component into both keys so upgrades self-invalidate: both cache paths now carry parser identity: parse blobs via the optional CacheVersioner (langspec digests its own Spec, so a regex edit self-invalidates that language only; three explicit constants for the hand-written parsers), and the typed-call module key via a pass-version component. Live proof at warm cache: 2 to 3 nodes after a Spec change with no cache deletion.
- B-01KYCGJ36RE88TVW5540JK759B parse-blob cache keyed by content hash only: parser upgrades silently serve stale nodes/edges until the cache is hand-cleared: fixed by T-01KYCS7M28EN3V5R0H1BWZG3T4 and proven live: an upgraded parser no longer replays pre-upgrade blobs, a version-less parser still keys byte-identically, and the typed-call pass gained the same discriminator (its cache had the identical defect, found while scoping the fix).
- ADR-01KYCFH6F8E76RV63TTRG6RP7D R-01KYCC8MRRFAMBKA8E6HKPFP7F found major parser gaps in 30 of 32 languages. Which remediation path should spectackle take?: engine-endspan chosen and executed: EndSpan engine plus regex hardening across all brace-style languages and the three hand-written parsers; wazero stays gated per ADR-01KYAVBDVGF5RV88VGFYR8J1FK
- T-01KYCG4YA0EVXRMQKWDC8FKKFK langspec engine: Spec.EndSpan keyword-counting body spans (cspan.KeywordSpan): Spec.EndSpan and cspan.KeywordSpan shipped; nil EndSpan proven byte-identical to brace-only behavior
- T-01KYCX365GEE0A29HZVJ87DRWC homebrew tap: formula instead of cask, so brew install works on Linux as well as macOS: formula replaces the cask: brew install now serves macOS and Linux from one artifact, the quarantine hook is gone with the cask, and the goreleaser version is pinned so the deprecated brews block cannot break a release unannounced. Snapshot verified: dist/homebrew/Formula/spectackle.rb carries both darwin and linux URLs.
- T-01KYCXJS68FNAA12D98S36RN7S convention: every change lands through a pull request with auto-merge on green CI: convention documented in CONTRIBUTING.md: PR plus auto-merge squash, with both repository prerequisites named (allow auto-merge, and a required CI check without which auto-merge degrades to merge-now) and the no-reuse rule for merged PRs
- P-01KYCG2J4GFZV82SQJ6F4CXP23 langspec Def/Call hardening: close the R-01KYCC8MRRFAMBKA8E6HKPFP7F regex misses across all brace-style languages: all five family batches shipped (T-01KYCG4YA0F18VSJ2C5ZNC7DCK to T-01KYCG4Z98FH3VGA3D75BM88NJ): every brace-style langspec language had its Def regexes hardened against the R-01KYCC8MRRFAMBKA8E6HKPFP7F findings, and the languages that lacked CallRe gained call edges. Residual low-severity items are documented in the per-language files.
- B-01KYCT1VX8F6BVH0HJB0ZHQ8EP search cache staleness: bundle freshness is decided by mtime and size alone, while the files table's sha column is never written or read: Bundle freshness now consults the files.sha content hash whenever the mtime+size gate reports no change (gen v0-5 -> v0-6 rebuilds once to populate it); asserted per bundle kind - spec, work, journal - that a same-size mtime-preserving rewrite becomes searchable and the old content stops answering, and that an untouched bundle is still not re-fed.
- T-01KYCT7KG8FD5VG9XKC2SQ37TD registry invariants for parsers and Specs, plus the B-01KYCQY9SREEMS0KEQQJQ6XPC5 kind-gate fix: B-01KYCQY9SREEMS0KEQQJQ6XPC5 fixed by admitting KKernel to a named span-eligible kind set; KType and KVar excluded after empirically trying KType, which broke 15 language contracts (a class body mints bogus member call edges). Six registry invariants added: every assembled parser declares a distinct non-empty CacheVersion (32 of 32), CallRe kinds are span-eligible or carry a recorded exclusion reason, capture indices are within their own regex, Specs are well-formed, extension overlaps are reported. Every invariant proven to fail against the defect it encodes.
- P-01KYCG2J4GEK0S7T45H67TGE32 langspec engine: end-keyword body spans, so end-terminated languages get real spans and call edges: EndSpan engine plus the five end-terminated languages shipped (T-01KYCG4YA0EVXRMQKWDC8FKKFK, T-01KYCG4YA0F83T7T0WVXZDGC3W): lua, ruby, elixir, julia and fortran have real body spans and call edges, and the nil-EndSpan path stays byte-identical to brace-only behavior
- P-01KYCG2J4GEQHB2J063NYESK25 hand-written parser fixes: go call-edge coverage, asm linker-suffix symbols, cuda kernel modifiers: hand-written parser fixes shipped (T-01KYCG4Z98FG9BWEPZ7WPBCWF8, T-01KYCG4Z98ET3VHXNMDQXTCN6P): go call edges from closure-var bodies and explicit generic instantiation, asm linker-suffix symbol forms, cuda kernel modifier orders
- P-01KYCT5VV0F5ZTSHRNWK7JVM18 class-level regression invariants: catch the next instance of each defect family, not just the nine already fixed: class-level invariants shipped across all four defect families: registry properties over parsers and Specs, the tool-surface bundle-location walk, the four-condition worktree end-to-end, and per-bundle-kind cache freshness. Two open fixes shipped with their invariants (B-01KYCQY9SREEMS0KEQQJQ6XPC5 kernel edges, B-01KYCT1VX8F6BVH0HJB0ZHQ8EP content freshness) because a test encoding a known defect as acceptable is worse than none.
- T-01KYCZ6Q9RFGT8H2VDJ9WWZ92Q ids: mint, encode, parse and adaptively shorten globally unique time-ordered record IDs: ids gained a Record-prefixed API (the package already hosts graph node IDs under the bare names): UUIDv7 minted now or at an explicit timestamp, Crockford base32 in fixed-width big-endian so string order equals time order, strict parsing, and adaptive shortening with a floor of 6. The floor matches the width of the sequential IDs it replaces, so the measured output-diet cost stays zero in the common case. Trap confirmed empirically: 500 same-millisecond IDs share an identical 7-char prefix and adaptive prefixes ran to 15, so a fixed git-style length would have been 500-way ambiguous.
- R-01KYCZRTD0FQQVTZZCR34609ZB cross-clone ID collision reproduced end to end, and the data loss it causes: collision reproduced end to end through the real server: two clones both minted T-00000047QRFM28JY2HF48KT0DB for different work, and reconciling by ID left only the second reachable while the first survived solely as a journal create event. The two-clone setup is the acceptance test for the new scheme, and the dogfood workspace built from this repository's real records is the migration fixture of record.
- T-01KYCZ6Q9RF5BBAJVT78XQATWK item and lifecycle: accept the new ID shape while legacy sequential IDs stay resolvable: item IDs now mint as kind-prefixed UUIDv7 base32 via item.MintID and MintIDAt; IDRe and the heading parser derive from one shared pattern so they cannot drift apart and render a work.md unreadable. Legacy IDs keep parsing permanently, verified live against this repository own records. Latent bug found and fixed on the way: item.Num used a decimal scan on the tail, so a record ID beginning 01234 would have parsed as counter 1234; it is legacy-only now. Hooks left for the migration: MintIDAt and LegacyIDRe. The brief said four files only while also requiring the whole suite to pass, which was contradictory: minting broke 34 tests that hardcoded P-01KY8XJ7B8FG5RDFJSX3X02B75, so the implementer fixed them via helpers that read the minted ID off the tool output, touching no production file outside scope.
- R-01KYD0DRAGFNRBTF90MFYYHNSF how to make the workflow force multi-perspective analysis, so specifications stop shipping with implementation gaps: Four of six lenses reported before the session limit killed the rest; 40 mechanisms proposed, 34 naming a real failure from this repository's history. Synthesis done by the orchestrator because the synthesis agent died with them.

THE ORGANIZING FINDING, which three lenses reached independently: every mechanism splits into a server-computed half that an author cannot fake and a written half that they can. The current review surface is almost entirely the written half. grill's brief heuristics are length and substring tests; its questions fire on the absence of a word. All are satisfied by writing the word, which is why they have never caught anything.

CONVERGENT MECHANISMS, ranked by real failures caught per unit of cost:

1. Grill stamps a verdict, not an occurrence. Today grill writes Grilled = today unconditionally after rendering a pack that may contain twenty findings, and move to=approved passes on any non-empty stamp. Make the stamp carry the open-gap count and let move refuse while server-computed gaps are open. Cost is one conditional around an existing write. This is the repository's own P-01KYAPBDQREY4S78GCYXQA5DQ1 rejection turned on the reviewer: a hash refresh is not a verification, it only records that something happened. Without it every other mechanism below is advisory text.

2. Test-bed monoculture scan, also called the fixture-constant ledger. Statically list the literals every test in a package agrees on, computed from the committed test files. Proposed independently by three lenses. Would have caught B-01KYC8A018EDTBMT4REZH2SJD1, B-01KYC8SG48F3GTBHSV4A3DJ5SA, B-01KYC94PH0FFDS3059GMVGBD2X and B-01KYC9CANGEQCVAGA74F3A11JZ at once, the four defects P-01KYCT5VV0F5ZTSHRNWK7JVM18 records as hiding behind one shared blind spot, because InitTestRepo hardcodes the branch name main and no test ever varied it. Static, sub-millisecond, and the list cannot be argued away in prose.

3. Red-run evidence, the mechanized form of prove-the-test-bites. No verification is complete without one execution whose expected result is failure. This session used it by hand throughout and it worked every time; B-01KYCYCFERF4KVN04NTA2K32EC exists precisely because lint was never once run against a path it could not read. Strong against faking: the transcript must show a named test failing with a specific diff.

4. Independent-oracle probing for recognizers. Any extractor is verified against source its author did not write, with a recall ratio reported. All four lenses proposed a version of this. Would have caught R-01KYCC8MRRFAMBKA8E6HKPFP7F wholesale, thirty of thirty-two languages, every one of which shipped with its prescribed fixtures green. Also the most expensive mechanism in the set, and the only one with a maintenance tail.

CHEAP AND FULLY UNFAKEABLE, worth doing regardless of the above:
- Declared-but-unconsumed sweep over an item's targets. B-01KYCT1VX8F6BVH0HJB0ZHQ8EP's title is literally the finding: the sha column was declared in the schema and never written or read. Static, sub-second, nothing an author writes changes it.
- Known-bad-evidence lint: a small regex table of verification methods recorded as wrong, matched against VERIFY blocks and pasted evidence. Reading an exit code after a pipe is the first entry, because that is exactly how B-01KYCYCFERF4KVN04NTA2K32EC was falsely confirmed.
- Anti-vacuity lint over changed test files: a range-over-collection test that passes on an empty collection, a subtest loop reaching no assertion.

WHAT TO DELETE RATHER THAN ADD: grillQuestions' word-presence checks and briefHeuristics' substring tests. They cannot fail for a determined author, they train bodies to grow padding, and they occupy the slot where a computed check belongs. The replacements are named above: an irreversibility detector computed from targets replaces the rollback question, and a verify-line executability check against the real flag sets replaces the no-verify substring.

THE GAP IN THIS ANALYSIS, stated because it is load-bearing: the two lenses that died are exactly the two that would have criticized the result. The anti-ceremony lens was to say what the aggregate token cost of all this is and which mechanisms are theatre; the orchestration lens was to ask whether an orchestrator and an implementer drawn from the same model can ever be independent reviewers, which is the assumption underneath half the shortlist. Adopt from this list incrementally, cheapest and most unfakeable first, and re-run those two lenses before committing to the expensive fourth mechanism.
- P-01KYCZ04BRFJF9AH75QRWMGXPC globally unique, chronologically sortable record IDs: close the cross-clone collision hole: globally unique time-ordered record IDs shipped: UUIDv7 base32 stored, shortest unambiguous prefix accepted and displayed, legacy sequential IDs still resolvable, and every workspace on the old stamp migrates itself on open
- B-01KYD1G9G1EVCAEWWVFR15GRT3 rule op=edit silently discards the edit and answers ok when the pattern slot is omitted: fixed under T-01KYD2: the tool layer recovers the stored pattern when slots are supplied without pattern=, so a slot edit either recomposes or refuses naming the missing slots; it can no longer answer ok while keeping the old text. Lint findings now describe the text actually stored
- T-0136 tool boundary: accept a short ID prefix everywhere an ID is taken, and name ambiguities: Landed in f2b5eaa. Short-prefix resolution now normalizes at the tool boundary (uppercase, I/L->1, O->0, U->V) and resolves against live, archived and rejected items; expand no longer returns the normalized input on a miss, which had let a nonexistent ref be leased under a name nobody else checks. Ambiguity names its candidates instead of guessing. Stored values are always full IDs; a rendered short ID is the shortest prefix unambiguous at that instant and is NOT a durable handle - T-0138 must mint and store full IDs. Not done: state.go, prompts.go, knowledge.go and internal/lifecycle still render full IDs (outside the five named files).
- P-01KYD8DK52EG5A1AKC1KSRR4Z2 close the four open field-reported defects: gitignore, worktree root, degraded index, stale hint: issues 26-29 closed: gitignore honored in the index walk and SkipDir, a nested worktree is its own root even when an ancestor carries a bundle, index degradation is a TYPED finding on state and check, and the stale-binary hint fires only for a dev build serving its own tree
- ADR-01KYCZ13KRF84VD5DSVQ4017MV Which ID scheme closes the cross-clone collision hole?: compact
- T-01KYD2XQG6E38APSR3EY4GY137 rule op=edit: recompose from the stored pattern instead of silently rewriting the old text, and stop eating the separator: compact
- P-01KYDBRWFZFXSBRF4PNRD6R4D9 the git workflow becomes server automation: state transitions drive branch, push, draft pull request, ready and merge: state transitions now drive branch, commit, push, draft pull request, ready and merge from server code; two modes with online the default and offline doing the same lifecycle locally; opt-out by config; no gh dependency, credentials via env then git credential helper
- T-01KYD5JB7GF34BFNVRAAX8B6GE serialize server-side whole-file rewrites through the coord.db lock table: cross-process whole-file rewrites serialize through the coord.db lock table: named locks with TTL and panic-safe release, every writer wrapping its full read-modify-write, reads never locking, proven with multiple real DB connections and unskipped concurrent-draft acceptance at count 20
- ADR-01KYDBQGMRFBN9SAHCWWNSAKX4 Who merges a task's pull request, and by what method?: decision fully implemented and repeatedly live-proven: forge Merge uses merge_method merge (never squash, 403 degrades to ReasonNoPermission leaving the PR open), the offline double performs a real git merge --no-ff, and awaitChecksAndMerge gates every mechanical merge on the head CI verdict — PRs 43 through 63 all landed through this machinery
- ADR-01KYDGXWH4FX9VQTG0G2CF8GQQ May the git automation ever suppress an action silently?: never-silent is implemented at every suppression point: gitGate emits its reason on the transitions it suppresses, deferrals carry g records, unmet obligations carry GIT E per the B-01KYDM taxonomy, and the identity fallback announces itself — the decision has been load-bearing through every landing since
- T-01KYD8M955E9NBPH27D21E4J9J merge policy: never squash - merge commits everywhere the workflow speaks, so the per-edge decision trail survives into main: IMPLEMENTER IN OWN WORKTREE. Read this whole body first. This is a documentation-and-policy task; it changes no Go code.
- P-01KYDPRXQSF8XAP1HCHY7390T8 every MCP text earns its tokens: a benchmark harness scores hint and record variants by tokens against validity, judged by independent agents over tricky fixture repos covering all eight states: every stage of the proposal is landed and the exit criterion is met in full: the harness runs CI-free locally (internal/bench, offline git mode); a baseline report exists and evolved with the fixture (v1 3486 bytes, v2 relabeled and 10x more sensitive); multiple text improvements landed strictly better by A/B — the state rules-inventory collapse measures minus 143 bytes net against the pre-collapse build on fixture v2 and minus 702 bytes per state call on this repository — and every one of the eight states appears in each scripted run by construction, with the harness catching two of its own defects (slot-direction inversion, run-noise floor) and three product bugs along the way. Stage 4 closed the loop: two independent haiku judges with a zero-coaching brief reached all goal states from the server texts alone, scored mechanically at 17 calls each, 4677 and 3846 bytes — the 831-byte wandering spread at equal validity is the metric future text changes move. The judge machinery ships as bench -agent-prep and -agent-score
- T-01KYDYSK4TE5R9JABNM5YJHBXT goreleaser changelog filter excludes spectackle process-commit subjects: Follow-up named by T-01KYD8M9, whose file scope excluded .goreleaser.yaml: the never-squash policy carries server process commits (subjects shaped spectackle(<ev>): ...) onto main, and docs/release.md documents them as changelog-excluded like docs:/spec:/chore: — but the enforcement is missing. Change: add ^spectackle\( to changelog.filters.exclude in .goreleaser.yaml (currently only ^docs: ^spec: ^chore: at lines 76-79). VERIFY: goreleaser check passes (respecting the pinned ~> v2.17 note in docs/release.md); a dry-run changelog over recent main history contains no spectackle( subject.
- P-01KYDF59RYEVYRDADEP65GF2ME total decoupling: the LLM issues no git or forge command, and every one is mechanical in the server: three of the four gaps are landed and live-proven at scale, measured against the running implementation: records commit mechanically on every transition (gitCommitRecords, the gap the proposal called largest); the merge is CI-gated with a blocking SHA-pinned await at done and a gated never-squash merge at archive (awaitChecksAndMerge, ADR-01KYDB satisfied); integration housekeeping is mechanical through the worktree flow — MergeMain with records preservation, semantic replay, stale-branch and records-only closures, the attach fix and the vacate machinery all adversarially hardened. The exit criterion is met in the strong form: thirty-plus consecutive pull requests landed with zero LLM git or forge commands, one merge commit per task with server-composed title and body, and the worktree judge batches proved fresh agents drive the whole flow — draft through merged — through tool calls alone, sibling replay included. The single remainder, automating the post-merge server rebuild-and-restart (item four, an ops step rather than a git command), is spun off as its own task with a full brief so this proposal closes on what it actually claimed: the LLM issues no git and no forge command, and it does not
- B-01KYD57FN3ERHBM5EQ3534YJXP concurrent draft from two agents silently loses items: work.md is read-modify-written with no cross-process lock: fixed by the coord.db named-lock serialization (T-01KYD5): item.Upsert takes the work lock around its whole read-modify-write, and TestConcurrentDraftsPersistEveryItem pins sixteen concurrent drafts from two servers all reaching disk
- B-01KYD1G9PSEH5AQHAV7N4ZQ4BT a degraded index is invisible through the MCP surface: state answers ok graph after the typed-call pass fails: fixed by TypedPassState: the typed-pass verdict is carried on the server and rendered by typedPassFinding in BOTH the state graph section and check as a TYPED W naming the cause and the under-reporting consequence — the exact degradation surfaced live on the CI runner whose older toolchain disabled the pass, proving the visibility end to end
- B-01KYD1G9J5EHBBT823EK0MGT3T indexer walks gitignored paths, so vendored and virtualenv copies inflate the graph and steal the unsuffixed node ID: fixed: workspace walks prune gitignored and vendored trees via SkipDir — probed live, a gitignored vendor dir with three functions leaves the graph at nodes=1 for the single real file
- B-01KYD1G9KQF87REB16T0AXRDYP workspace resolution walks past .git files, so a nested worktree can never be the root and writes land in the parent checkout: fixed in the ws/main split era: the served root is the workspace it was started in — this entire dogfeeding session runs from a linked git worktree whose records, work.md and journal all land IN the worktree, and the ServedDir distinction (T-01KYEH) made the remaining main-vs-served divergence explicit where it matters
- T-01KYD94KP4FBHR0RGR2P8CZNBZ grill computes its critique and stamps a verdict; the verdict is an independent review event with feedback and a research-demand path; the fakeable word-checks are deleted: IMPLEMENTER IN OWN WORKTREE. Read this whole body first. This task supersedes the rejected draft it cites in refs; everything that draft specified remains in force here and is restated - nothing is inherited by pointer.
- T-01KYD94M3EFXCBVRVWZCS5KBE9 validate: the post-implementation phase - computed pack over the diff, independent verdict gating archive, findings reopen the item as the implementer's next brief: IMPLEMENTER IN OWN WORKTREE. Read this whole body first.
- ADR-01KYES0TJ0E33RRP1JERC62DCA Approve the review-machinery work? The 14 submitted chain items (P-01KYD9 family: grill as recorded independent verdict gating approved, validate phase gating archived, per-finding addressal) plus P-01KYER (elicitation gate, single-reviewer sequential lens default, panel amendments) form one coherent design; P-01KYER child tasks are sequenced after the chain ones they amend.: kind: radio
- ADR-01KYES0TMSETQSBYHCZF3X66CJ Where should validation be a HARD gate (feedback.validate=require)? Token analysis: break-even at ~30-50 percent defect-catch rate; always-on require does not pay for small items.: kind: radio
- ADR-01KYES0TR8FP78R6CGSXM9MQ01 May a reviewer waive an ambiguity finding without a user round-trip? Recommended: waivable with recorded reason plus computed waive-rate tripwire (ask-only would make automation authoritative over judgment, inverting the steps-are-judgments contract).: kind: radio
- ADR-01KYES0TTVERW92GN8R86TCG6W Do review/validation verdict events survive journal compaction forever (~5KB per item, permanent audit corpus) or fold after archive?: kind: radio
- ADR-01KYF367A2EAERK9142M53JX8X v0.2.0 release cut: core cut (land addressal PR 105, fix both self-restart bugs, body-edit path, manifest nudge, elicitation gate, release notes and tag - ready in roughly 2-3 days at current pace) or full-chain cut (additionally the seven remaining chain tasks plus outcome benchmarks - roughly 5-7 days)? Pre-1.0 semantics: whatever is cut, the rest ships in v0.3.0.: kind: radio
- T-01KYD9JTSDFYHTEM8B6YXX6NSP steps are judgments, automations are implications: the reviewer's verdict is authoritative over computed findings, per-finding, with recorded reasons: validated pass by cross-val-t9j diff 5b6474082d21
- T-01KYD94MG8FBMTJP5CPC62PCYM edge-commit engine in gate: every tool call that writes .spectackle state commits it with a structured decision message composed from its journal events: validated pass by cross-val-94mg diff 615c8f9ea82d
- T-01KYD88KEDEAQ97QKQ46DSGTM4 evidence sweeps scoped to an item's targets: declared-but-unconsumed symbols and minority call shapes, with explicit per-symbol suppression: validated pass by cross-val-88ke diff 1e4605c6ba14
- T-01KYD9RJTREBEVQFV34HYW8VJ2 redundancy findings in the validation pack: diff-scoped duplicate-block detection against the graph, so implementations reuse instead of re-writing: validated pass by cross-val-9r diff 3c29d3acee92 :: detector calibrated on the real twin, isolation and caps pinned, index tested directly, 1.43s cold inside budget; the deferred persistent cache is measured-justified and stated
- T-01KYD87ZN7EJ49CMSEQE9XGGWS package-local contract coverage: silent by default with visibility in state, counted by check only under coverage_gate: validated pass by dogfeed-orchestrator diff 291d4522a314 :: Independent re-run from the diff alone: golden byte-identity (this repo check pre=post=ok, diff empty; pre-change binary on a bundle-less fixture emits ungated g-uncovered, post is silent ungated / g nocontract gated) and the cap+state tests re-executed green. COVERED matches the brief: bundle-at-or-above OR non-empty applies binding into the subtree; empty applies covers nothing. Eleven-dir worklist confirmed live. — Package-contract coverage landed: COVERED(pkg) via bundles+applies-anchors, state visibility (eleven-dir backfill worklist: internal/budget cache cspan evidence ignore journal migrate plan9 replay sync workspace), check counts g nocontract only under coverage_gate: package (cap 20+tail), byte-identical default proven against the pre-change binary. Old per-file coverageGaps preserved for research/knowledge/state consumers. Cross-verification re-ran byte-identity+cap+state proofs from the diff; verdict recorded under the orchestrator identity because the -http CLI path stamps the server agent and ignores SPECTACKLE_AGENT (gap filed separately). Waived: three offscope doc/test files (mandated completeness), one preexisting dup pair short8/shortHash flagged from hunk context lines (cleanup item candidate, plus a dup-detector refinement: exclude context-only functions).
- T-01KYD88KV5EX2SBYE81TKYHDH9 the backward path in every machine-facing surface: state-computed next steps, archive notes as the training signal, post-merge restart in the loop: validated pass by cross-val-88kv diff a3a8bd589b77 :: Independent re-run from the diff: regenerated .claude/commands/spectackle.md matches the template (steps 9-10 verbatim as reported); manifest substance + size ceiling + diet ceiling + template line ceiling + nextAction table re-executed green. Deviation verified as disclosed: composed-manifest diet ceiling 4000->4300 carries its justification in the test comment; instructions const 3688B stays under the brief ceiling 3883B. MCP-011 anchored to registerTools, lint clean at 83 rules. — Backward path landed on all machine-facing surfaces: manifest BACKPROP (610B; three stores, note-as-training-signal, make dev), workflow template steps 7-10 (independent re-verification from the diff alone, note substance, restart, research capture), promptNext leads with the exact computed next action per state (nextAction + grilledOpen, 8-state table test), agent-workflow.md BACKWARD PATH section (28 lines) + orchestrator verification sentence, MCP-011 pinning the manifest obligation. Measured: instructions 3083->3688B (ceiling 3883), template 62->74 lines (ceiling 102), composed-manifest diet ceiling raised 4000->4300 with in-test justification - the disclosed deviation. Cross-validated pass by cross-val-88kv via -root (the -http path stamps the server identity, B-01KYFPNCK). Learning landed: manifest+rule+docs; commands regenerated for this repo.
- T-01KYDA92TGF3FT994TEE6EDVN6 one task, one branch, one pull request: every finished task opens its own PR immediately, never batched: validated pass by cross-val-a9 diff 7bd9d2ebd29b :: Fresh-context cold reader (haiku, sections only, no repo knowledge) answered all four policy questions correctly from the text alone with quoted support: draft PR at first commit / ready immediately at done; one task per PR with the full task ID in the title; the user merges by hand or standing instruction with a merge commit; the two forbidden states (unpushed changes, pushed branch without a PR) at any time. Coherence verdict: coherent across never-squash, the settings list, and the new section. Consistency grep re-run: every batch/accumulation/session-end hit is the policy text itself or an unrelated approved-task partitioning sentence. — One-task-one-branch-one-PR is now written policy: CONTRIBUTING gained the section with the three decision-trail granularities, the session-accumulation anti-pattern, exact mechanics (draft at first push, ready immediately at done, full task ID in title, user merges by hand or standing instruction with merge commit, always-pushed-always-covered), and the follow-up/taskless-change exceptions; agent-workflow gained the orchestrator PR duties with the revert/bisect/review payoff. The pre-existing merges-on-green-no-button sentence and auto-merge settings item were reconciled into the human-judgment merge policy, kept truthful against the live archive flow via the standing-instruction clause. Cross-validated pass by cross-val-a9: a fresh-context cold reader answered all four policy questions from the text alone and judged the three sections coherent. Docs-only diff; suite green, lint clean, check ok.
- P-01KYD87FJREJ5SD0G2RDCMZ32Y turn review from assertion into evidence: run the gate that exists, then make grill and validation compute what they cannot fake: Review is evidence now, not assertion. Delivered across children: grill renders a computed critique (classes a-e + #evidence Unconsumed/DivergentCallers) and stamps grilled: date open=n; approval gates on an independent identity verdict bound to reviewHash(body+targets) with author/ephemeral/stale refusals; validation renders the attributed diff (merge-of-branch + post-merge citing commits, spectackle( subjects excluded) with computed v classes (untouched/offscope/notargets/untested/vacuous/fakebench/testonly/nodocs/dup) and archive gates on a validate verdict with per-finding waivers under feedback.validate=require; package-contract coverage closes the silent-uncovered gap (COVERED via bundles+applies-anchors, state visibility, coverage_gate opt-in). The founding measurement stands in the body: twelve pre-change grill events, all 0-91s after create, zero revisions - the machinery this set replaced. Refinement backlog spun off: dup context-line false positive (T-01KYFPNCX), -http verdict identity (B-01KYFPNCK).
- P-01KYD87FX0F6YRX49R3A8TB6E4 backpropagation: every loop result flows back into the workspace, and the server names the next step so no step can be silently skipped: Backpropagation closed both directions. Research return path: R-items archive only consumed (live Refs, archive-event Refs, rule rationale, or >=80-char note) - hard gate at the archive edge. Backward path on every machine-facing surface: manifest BACKPROP paragraph (three durable stores, note-as-training-signal, make dev) pinned by MCP-011, workflow template steps 7-10 (independent re-verification, note substance, restart, research capture), promptNext leads with the exact computed next action per state, agent-workflow BACKWARD PATH section. Enforcement layering held as designed: prose stayed guidance, the two hard gates (research consumption, validation verdict) carry substance enforcement.
- P-01KYD8HSZ0ERTBFBBEVQD68M4R the commit log is the decision log: every state-machine edge commits with a structured message, and no merge may flatten the trail: The commit log is the decision log, at all three granularities. Edge-commit engine: every .spectackle-writing tool call commits with a structured subject and Spectackle-Ev/Item/From/To/Agent/Eid trailers (journal Sink capture, LC_ALL=C, structural staged-diff check, coord lock, git.commits edges|off knob); merges are merge commits, never squash - policy in CONTRIBUTING plus repository settings (squash disabled, verified); one task one branch one PR - draft at first push, ready at done, full task ID in title, user merges by hand or standing instruction, always-pushed-always-covered (T-01KYDA9, cold-reader verified). Design boundary recorded: the journal stays the machine source of truth; the commit log is the derived human audit projection joined by Spectackle-Eid - trailers cannot replace the journal (16 consumers, compaction, body snapshots).
- P-01KYD9466KEPWBV2RBK7EQM202 review and validation are recorded independent verdicts: grill reviews the draft with feedback and a research path, a validation phase judges the implementation, both bound to reviewer identity: Review and validation are recorded independent verdicts, both hard-gated. Grill: EvReview verdict bound to reviewHash(body+targets), author refusal (item author cannot verdict own draft), ephemeral-identity refusal (ag-hex4), stale-hash refusal; approval crossing gates on pass under feedback.grill=require. Validation: EvValidate render (validateHash over attributed diff + targets) and verdict with per-finding addressal - every open finding key fixed or waived with a recorded reason, waivers flattened and capped, legacy-render refused; archive gates for task|bug under feedback.validate=require; fail reopens through the rounds machinery to escalation. Identity lesson recorded on the way out: -root calls stamp SPECTACKLE_AGENT correctly, -http stamps the server identity (B-01KYFPNCK open). Review mode economics: single reviewer walking lenses sequentially, panels only by explicit config (REVIEW-MODE-001) - the P-01KYES set carries the remaining lens-label and elicitation children.
- T-01KYFSQQ7MFBHSXYC505XEJYF5 planted-completeness outcome fixture: hidden acceptance tests score first-iteration completeness per token and the A/B refuses unequal validity: validated pass by cross-val-t2 diff 7c4b39a90ed8 :: Re-ran from the diff alone: calibration tests green (correct 5/5, shallow exactly 2/5, traps spring and stay silent correctly, no hidden leakage, rounds=1 fixture counts), refusal test green, TestOutcomeFixture green on the branch. End-to-end evidence consistent: first-pass=5/5 final-pass=5/5 rounds=0 valid at 32 calls/949 surface tokens from a real sonnet judge; the firstIterationCommit ordering fix (done edge precedes source checkpoint) is recorded in the code comment and was forced by a live inconsistency, not assumed. — Outcome scoring is live and the P-01KYEV exit criterion is measured: a real independent sonnet judge drove the limiter fixture through the full lifecycle and scored first-pass=5/5 final-pass=5/5 rounds=0 valid at 32 calls / 949 surface tokens (82K total), no traps sprung - first-iteration completeness per token is now a number, not an argument. Machinery: hidden acceptance suite (5 edge cases, harness-held, scratch-only application, discriminance pinned correct 5/5 vs shallow exactly 2/5), vacuous-test and offscope traps as validity violations, first-iteration tree recovered from the edge-commit trail (measured ordering: done edge precedes the source checkpoint sweep - firstIterationCommit takes the commit before the first edge leaving done), efficiency per 10K tokens with REFUSED-unequal-validity mirroring the all-valid rule, -scenario outcome flag, curves ledger with three new columns and the run recorded. One round paid for the untested standalone fixture entry (test added). Follow-up noted in the done report: review_findings_addressed column awaits the first outcome A/B. Next: run an A/B between two hint variants on this fixture to calibrate the review chain risk-gating with real catch rates.
- T-01KYFXBYMZFH7BC4Z1JQT8FV73 ambiguity findings computed from post-deletion signals, closed by decide round-trip or recorded waiver: validated pass by cross-val-amb diff 72c2021e8ec1 :: Re-verified from the diff: helper twins GONE (shortRef and targetPaths deleted, inlined at their single call sites - the round-1 fix landed); ambFixturePad const shared across the three fixture files; calibration distribution and banned-pattern grep re-checked (zero word-presence); live round-trip evidence consistent (open=1 awaiting -> decide answer -> open=0). The persisting test-pair similarities predate the diff - the skeleton is the verdict test PATTERN, and collapsing it into table-driven form is worthwhile but belongs to the detector-refinement task, not this one. — Elicitation is computed and enforceable: amb-thin (400B floor, calibrated on the 22-body reject corpus min 418B), amb-novel (all targets uncovered plus zero corpus hits), amb-incoherent (3+ unlinked top-level subtrees via bounded graph BFS) - post-deletion signals only, zero word-presence checks proven by diff grep. Ask closure is mechanical (cited ADR done/archived closes; open renders awaiting ADR-x; journal tombstone resolves archived ADRs) and was proven live on this repo end to end including a decide answer flipping open=1 to open=0. decideAsk nudges the harness interactive ask; draft says ask at draft time; agent-workflow.md carries the elicitation section. Two rounds of the validation chain caught the landings own flaws: six pre-floor fixture bodies (fattened - the class fired correctly on day one) and helper twins shortRef/targetPaths (deleted, inlined); the persisting verdict-test skeleton similarity is pre-existing and assigned to T-01KYFPNCX. Remaining P-01KYES children: lens labels (T-01KYFXDC6), risk-gated require (T-01KYFXDCH), waiver tripwire (T-01KYFXEP), verdict compaction survival (T-01KYFXEQ).
- T-01KYFXDC6KFNCB66W8JVHTZZNK lens-labeled verdicts under the single-reviewer sequential default, with per-item panel opt-in capped by config: validated pass by cross-val-lens diff 7aa399ea8044 :: Re-verified from the diff: the merged TestVerdictRefusalShapes replaced the 100% twin pair - dup findings gone from the render; live lens verdict evidence consistent (ok line and pack line both carry lenses=minimality,tokens,refute; first probe refused by the amb-thin floor proving composition); 349B of 350B instruction budget pinned by test. — Lens-labeled verdicts live: lenses parameter -> journal Ln -> both renders (ok line + pack verdict line), empty label refused; the sequential single-reviewer default is now written instruction (349B of the 350B budget across grill description + orchestration guide, cap test-pinned) with the one-reviewer-sequential-lenses section in agent-workflow.md; panel=n is per-item opt-in gated on live risk (irreversible/blast key or override-once spent), capped by swarm.panel_max=3, refusals name the missing signal - evidence breadth, never voting, config caps never raises. Live proof: cross-rev-lens verdict with lenses=minimality,tokens,refute on a throwaway; the FIRST probe was refused by the sibling amb-thin floor, proving the two P-01KYES landings compose. One validation round paid: the dup detector caught my copy-paste refusal-test pair at 100% - merged table-driven. Remaining P-01KYES children: risk-gated require (T-01KYFXDCH), waiver tripwire (T-01KYFXEP), compaction survival (T-01KYFXEQ).
- T-01KYFXDCHHFV3SQDA5CEHAA4AQ validation require is risk-gated from the landed diff: file count and dangerous-path membership, never declared targets: validated pass by cross-val-risk diff 0aeadbb5b0a5 :: Re-verified from the diff alone: riskTrip re-run (9>=8 trips naming counts, 7 silent, threshold override, dir/** and path.Match shapes, non-recursive pin, empty default never trips); knob defaults 8/empty confirmed; scaffold renders both keys with comments; the require branch precedes the risk branch in the wiring so an explicit require is structurally never downgraded; refusal string carries the exact tripped input. The stated residual (no gate-level e2e because this repo runs blanket require) is honest and acceptable - the pure layer carries the property. — Validation require is now risk-gated from the LANDED diff: riskTrip (pure) over diffFiles output - file count vs feedback.risk_files (default 8) and feedback.dangerous_paths globs (default empty by repo-vocabulary honesty; dir/** subtree or path.Match) - never declared targets. Warn mode refuses archive naming the tripped input; explicit require branches first and is never downgraded; this repo keeps blanket require. Knobs scaffold-documented and key-list-tested; lifecycle.md gate paragraph updated. Residual stated: gate-level e2e unreachable on a blanket-require repo, catch-rate calibration owned by the outcome-fixture A/B. Remaining P-01KYES children: waiver tripwire (T-01KYFXEP), compaction survival (T-01KYFXEQ); user-priority next: short display IDs on git surfaces (T-01KYG0ZX).
- T-01KYG0ZX2XERAS022A42JNAV75 human-facing git surfaces carry the short display ID: branches, PR titles, commit subjects; machine trailers stay full: validated pass by cross-val-sid diff 508659ea1854 :: Verified from the diff with lenses [compat] itemBranch legacy fallback covers in-flight full-length branches and the short form is a strict prefix of the legacy form making the one --grep match both eras - test-pinned; [split] one real edge commit asserts short subject AND full Spectackle-Item trailer; [refute] tried: two same-millisecond mints sharing a 13-prefix colliding as branch names - probability 2^-15 per the ids comment and branch creation would fail loudly on the duplicate, not silently corrupt. Live proof correctly deferred to the first post-swap activation. — Human-facing git surfaces now carry the short display ID (kind + 13 body chars, lifetime-stable): branch names, PR titles, and every commit subject; PR bodies keep the full ID on line one, Spectackle-Item/Eid trailers stay full as the audit join, and records inside git-managed files are unchanged per the user clarification. itemBranch falls back to legacy full-length branches for in-flight items; the attribution grep uses the short prefix so merges from both naming eras resolve. CONTRIBUTING and lifecycle.md 1.5 document the split. Live proof: the first post-swap activation renders g branch spectackle/<short> - watch the next task.
- T-01KYFXEPW3FD7RYDR1Q0S1R4FF waiver-rate tripwire: a computed non-vetoing line in state and packs when waivers dominate recent verdicts: validated pass by cross-val-wr diff 3df8604148b1 :: Re-verified from the diff with lenses [math] pairing by (id,hash) with last-render-wins matches reviewState; clean-render exclusion pinned in both directions; rounding is half-up on the display only. [surfaces] three renders wired, check byte-identity test-pinned, no gate logic anywhere. [live] the pack I am writing this verdict into carries the line itself - 68% over 20 with counts - and the rate is real: this sessions offscope-mandated-file and const-embedded-test waivers dominate, already assigned as refinement input to T-01KYFPNCX. Landing waiver-free, which is the correct direction for the number. — The waiver-rate tripwire is live and fired on its author immediately: state #health and both packs render w waiver-rate 68% over last 20 verdicts (waived=59 addressed=28) for this repo - visible, never vetoing, never in check (byte-identity pinned). Computation pairs each verdict with its bound render by id+hash, excludes clean renders from the denominator, windows at 20, silences under 5 qualifying or under 50%. The 68% is real signal: offscope-mandated-test-files and const-embedded-test shapes dominate the waivers - refinement input assigned to T-01KYFPNCX (dup/offscope class scoping). This task also carried the short-ID live proof: first post-swap activation rendered g branch spectackle/T-01KYFXEPW3FD7. One P-01KYES child remains: verdict compaction survival (T-01KYFXEQ).
- T-01KYFXEQ71F8X9C2028Q3B4WBX verdict events survive compaction and findings render only pre-archive: the token diet closes the loop: validated pass by cross-val-eq diff 41bb2b70a4bd :: [correctness] re-ran the fold test from the diff: live verdict byte-complete, terminal verdict sheds exactly Keys/Wv, reviewState resolves post-fold through the real compact tool. [economy] pruning bound to the terminal set from the same journal being folded; suppressed packs skip the whole computed pipeline. [refute] tried: detail pruning racing a late archive - prunes at the first fold after terminality, correct; tried: suppression hiding the stale-verdict warning - the verdict trail renders before the short-circuit. Zero waivers, second task running. Re-rendered after the refused-archive edge commit shifted the attributed diff - the hash binding caught exactly what it exists to catch. Note: validate op=verdict lacks a lenses field (deferred by the lens task); this verdicts lens walk lives in the findings text - the need is now shown, consider extending Ln. — ADR-01KYES0TT is real now: journal folds retain review/validate verdicts - identity/hash/pass/lenses forever, per-key addressal detail pruned once the item is terminal - and archived items packs render computed: suppressed (archived) instead of re-critiquing a moved-on tree. The fold previously ERASED all verdict evidence; reviewState on live items now survives compaction, proven through the real compact tool at JournalMax=1. Landed waiver-free with lens-labeled cross-validation. P-01KYES children complete: ambiguity elicitation, lens labels + panel opt-in, risk-gated require, waiver tripwire (live at 68% on this repo), verdict survival - the umbrella can close.
- P-01KYESGDWFFMH80ENHNFXMVZE8 requirement elicitation, lens-labeled verdicts under a single-reviewer default, and the panel amendments: closing the vague-requirement gap at minimum token cost (re-record): The elicitation and review-economics set is complete, five children landed: ambiguity findings from post-deletion signals with mechanical decide-round-trip closure (proven live, including the machinery refusing its own authors thin probe); lens-labeled verdicts under the single-reviewer sequential default with risk-gated per-item panel opt-in capped by config; validation require risk-gated from the LANDED diff (file count + dangerous globs, never declared targets); the waiver-rate tripwire (fired at 68% on its authors own session day one - visible, never vetoing, never in check); verdict compaction survival per ADR-01KYES0TT (identity forever, addressal detail pruned post-terminal, archived packs suppressed). The state-machine verdict stands: 6+2 optimal, no new states - every phase is a verdict-gated edge. One incident on the last childs landing: the archive split record from code via a stale-branch closure misroute (PR 142/143) - caught by the user, repaired by escape-hatch merge after conflict resolution, root cause filed as B-01KYG4PC with the no-open-PR-after-archive invariant proposal. Remaining drafts carry the refinement backlog: B-01KYG4PC, B-01KYFPNCK (http identity), T-01KYFPNCX (dup scoping - now with the waiver-rate evidence attached), B-01KYFSZ7 (WHEN composer), T-01KYDNN (pre-push hook).
- T-01KYFPNCXEERJ90CB7STB4ZGVZ dup detector must ignore hunk context-only functions; unify short8/shortHash as its proof: validated pass by cross-val-dup diff a2202b724c62 :: [correctness] re-ran the regression pair from the diff: adjacent-insert silent, added-twin fires on demo.C at the real validateDups level with a reindexed graph; the inHunk guard is proper unified-diff semantics, and its necessity was proven by a zero-Server fixture reaching the nil graph the moment hunk-less input parsed as content. [economy] the twin retirement is subtractive (two bodies become one helper plus two one-line delegates); the rider is nine lines and cleared four live w-orphaned lines on this repo the same hour it landed. [refute] tried: could added-line scoping now MISS a dup whose function gained only one trivial added line while its copied body sits in context - the finding would still fire when the twin was ORIGINALLY added, which is the only diff that authored it; per-diff scoping is the correct attribution boundary. Zero waivers, fourth task running. — The dup detectors dominant false-positive class is retired: findings implicate only functions with added lines in the attributed diff (diffAddedLines, hunk-guarded), the short8/shortHash twin is unified into hashPrefix as the briefed proof, and both regression directions are pinned at the real validateDups level. Rider: orphan closures now clear their w-orphaned lines via terminal-note citations - proven live when the four historical orphans (prefix-ambiguity era, stale-binary hint) were closed for the record this firing. The waiver-rate number should fall from 68% as this class stops demanding waivers. Queue after this: B-01KYFPNCK (http identity), B-01KYFSZ7 (WHEN composer), T-01KYDNN (pre-push hook), then v0.2.0 release mechanics.
- B-01KYFPNCK2E2QVSAF3QDS1W11S http CLI path silently drops SPECTACKLE_AGENT for identity-bound verdicts: validated pass by cross-val-id diff b03ee7fec9a1 :: [correctness] re-ran the override tests from the diff: records under the override, author-spoof and ephemeral refusals hold against the overridden name; the journal Ag stamp is explicit so Append cannot silently re-substitute. [surface] CLI forwards only over -http, only on verdict ops, never clobbering an explicit field - the three guards verified in the diff. [live] the scratch-resident proof reproduced the incident shape end to end (shared identity resident, CLI env, pass by cross-val-live). This verdict itself exercises the new field over the direct path. Zero waivers, fifth task running. — Identity-bound verdicts now record their real author on every transport: the verdict-scoped agent field (all identity refusals applied to it, journal-stamped explicitly) plus the CLI forwarding SPECTACKLE_AGENT over -http on verdict ops only. Live-proven against a shared-identity scratch resident reproducing the incident shape. Trust model deliberately unchanged - independence remains conceded-unverifiable; the fix removes the SILENT substitution, not the need for reviewer discipline. Remaining queue: B-01KYFSZ7 (WHEN composer), T-01KYDNN (pre-push hook), then v0.2.0 release mechanics.
- T-01KYG90P1EE29RSD89Z0PW8CKN node/edge audit: every state hint offers judgment options, no tool result demands mechanical caller work: validated pass by cross-val-ne diff 72954d42b1cc :: [principle] every state hint now offers options (or-tail asserted for five states) and zero surfaces instruct git (detector-pinned on both the hints and the rendered protocol); the one mechanical instruction that legitimately remains - make dev without a watcher - is framed as operator work with the callers judgment named. [economy] the manifest diet ceiling caught BOTH overshoots of the rewording before CI could; final manifest 3725B, options-tail cost accepted per the approved brief. [refute] tried: the or-tails could teach an agent to reject items casually - every rejected alternative still demands note= and rejection stays revocable, so the tail widens visibility of legal moves without cheapening them. Zero waivers. — NODE-EDGE-001 is enforced, not just declared: every lifecycle state renders its judgment options (primary + or-tail of legal alternatives, table-pinned), no rendered hint or protocol instructs git (bench.GitInstructionViolations exported and pinned on both surfaces), the restart is framed as the servers mechanical edge with the no-watcher exception honestly named, and lifecycle.md carries the principle. The manifest diet test caught two rewording overshoots - budgets beat intentions. Queue: T-01KYDNN (pre-push hook), then v0.2.0 release mechanics.
- ADR-01KYFYGVSRFX4B9B2YJ44QSBS8 live probe: should the widget cache be bounded: live ambiguity-probe decision (T-01KYFXBY round-trip proof); the answer yes-bounded was the probe payload, not a real design choice - archived as housekeeping
- T-01KYGCJ6P6FFHB9VJK5S4359V8 cut v0.2.0: release notes from the archive intent log, ldflags version stamp, tag, GitHub release: validated pass by cross-val-rel diff a3ae5167fcf4 :: [derivation] re-ran the golden from the diff (window filter, kind grouping, head-line cap, non-archive exclusion, blank-summary exclusion, determinism double-render); regenerated the notes against the live journal and diffed against the committed file - identical body. [mechanics] the stamped build renders v0.2.0 in the real handshake; tag-after-merge ordering is stated in the report and matches the atomic-edge lesson. [refute] tried: tombstone head lines leaking private shorthand - sampled the 179 committed lines, they read as public claims; the forensic tails stay journal-side by the cap. Zero waivers. — v0.2.0 mechanics complete and user-timed (cut now via the interactive ask): derived release notes (relnotes package, golden-pinned; the tombstones ARE the change log), releasenotes CLI with hour-precision windows, docs/releases/v0.2.0.md committed with the human intro, Makefile releasenotes/release-build targets, stamp proven live (ok spectackle v0.2.0). The tag and GitHub release follow THIS merge as the final mechanical edge - a tag must point at a commit containing its own notes.

## SPX-ARC-001
The spectackle server SHALL write only JSON-RPC 2.0 frames to stdout and route all log output to stderr.

Rationale: a single stray print corrupts the stdio MCP stream.
## SPX-ARC-002
WHEN a tool result exceeds the requested token budget, the server SHALL truncate the result at a record boundary and append a final `cur` record containing a resume cursor.

Rationale: token efficiency is the product; truncation must never split a record.

## SPX-ARC-003
IF a tool receives a symbol ID that is not present in the graph, THEN the server SHALL return the 3 closest matching IDs marked with an `nf` record instead of a JSON-RPC error.

Rationale: an error round-trip costs the caller more tokens than a correction.

## SPX-ARC-004
The input schema of every MCP tool SHALL declare a `default` value for each optional parameter.

Rationale: defaults let the LLM omit parameters, shrinking every call.

## SPX-ARC-005
The server SHALL confine every file write to `.spectackle` folders and keep the `.spectackle/cache` directory out of git via a server-written .gitignore file.

Rationale: the workspace belongs to the user; only the spec knowledge base is server territory, and caches must never be versioned.
## SPX-REPO-001
The repository SHALL keep every MCP tool schema in docs/tools.md consistent with the Go structs in internal/mcpserver/tools.go.

Rationale: docs are the normative schema mirror; drift breaks agent integrations.

## SPX-REPO-002
WHEN a new package is added under internal, the author SHALL add a scoped .spectacle/spec.md bundle or extend an ancestor spec bundle with rules for it.

Rationale: self-hosting requires every subsystem to be under contract.

## SPX-TST-001
WHEN a package is added under internal or cmd, the repository SHALL provide a dedicated `*_test.go` file exercising the package's exported surface.

Rationale: end-to-end tested implementations are the exit criterion of P-01KY8RVMZRE7QS4ZSK152YJ4DA.

## SPX-SWM-001
The server SHALL confine every agent branch commit to code files by excluding `.spectacle` paths, so spec state reaches main only through the semantic replay.

## SPX-SWM-002
WHEN an item is moved to rejected, the server SHALL write the rejection to the shared `coord.db` event log so sibling agents see it before any merge.

## SPX-SWM-003
IF a scope lease of a live agent overlaps a requested claim, THEN the server SHALL reject the claim with an `l` record naming the holder, the item and the expiry.

## SPX-SWM-004
The server SHALL mint every item ID and rule ID through the shared coordination counters so two parallel worktrees never mint the same ID.

## SPX-SWM-005
WHILE a worktree is open for this session, the server SHALL block the `compact` tool so journal folds cannot corrupt the submit replay.

## SPX-SWM-006 {applies: go:coord.DB.Sweep}
WHEN the stale-agent sweep runs, the server SHALL delete the `agents` rows whose heartbeat is older than `agent_ttl` together with their `leases` rows.

Rationale: the swarm view must show who is actually there; short-lived driver sessions must not accumulate.

## SPX-SWM-007 {applies: go:lifecycle.Escalate}
WHEN an item exhausts `feedback.max_rounds` reopen or gate-fail rounds, the server SHALL set the item to the `blocked` side state and mint a linked `adr` item whose only exits are rescope, reject and override-once.
## SPX-ARC-006
WHEN a tool receives a cur argument, the server SHALL resume output at the record offset decoded by budget.Resume so consecutive pages concatenate without overlap or gap.

## SPX-SWM-008
WHEN an agent begins work on an item while another agent is active, the server SHALL root that agent at a git worktree carrying its own .git/worktrees/<name>/index, so no two agents share one index.

Rationale: A linked worktree has its own git dir and therefore its own index (.git/worktrees/<name>/index), while sharing the object store and refs. That is what makes parallel agents safe to let commit at all: they cannot corrupt each other's staging area, and git's own ref locks serialize the only state they do share. Agents sharing one checkout would race the index exactly as they race work.md, which is why the edge-commit engine's cross-process serialization is scoped to same-checkout concurrency rather than to the multi-agent case this rule eliminates.

## TOKEN-OBJECTIVE-001
The spectackle server SHALL minimize total tokens to a complete, valid, defect-free result including correction rounds when composing machine-facing text; per-call bytes are subordinate, and hint lines ship only with a docs/bench-curves.md A/B entry showing non-increasing total cost.

## ORCH-GITQUEUE-001
The orchestrating agent SHALL merge every open pull request before activating new work, checking the forge PR list first, because parallel agents leave WIP branches that may already implement the next item.

## ORCH-PROOF-001
WHEN proving a landed feature live, the orchestrating agent SHALL drive the proof with a freshly built candidate binary (bin/spx-candidate), never the resident serve binary, whose build predates the landing.

## REVIEW-MODE-001
The review workflow SHALL default to 1 reviewer walking all lenses sequentially in one context, reserving multi-agent panels for recorded risk signals: at 3 lenses a panel pays 3x context ingestion for marginal judgment gain.

## ASK-SURFACE-001
WHEN decide op=ask answers with a need-decision record, the orchestrating agent SHALL surface the question through the harness interactive ask tool (AskUserQuestion) and write the answer back via decide op=answer.

## AGENT-ISOLATION-001
WHEN spawning an agent permitted to run tests or experiments, the orchestrating agent SHALL pass isolation=worktree in the Agent options so experiments never touch the live checkout: a live-tree refuter erased concurrent .spectackle/work.md writes with git checkout.

## ROLE-BOUNDARY-001 {applies: go:mcpserver.Server.gitFlowFor}
The spectackle server SHALL perform every mechanical git and forge step itself per the docs/agent-workflow.md role table; no tool result instructs the caller to run git, and the caller works judgment only: traverse, elaborate, code, research, validate, refute.

## COMPLETE-DIMS-001
WHEN rendering review or validation evidence for an item, the spectackle server SHALL compute findings across testing, validity, edge cases, implementation, documentation, and research (grillComputed, validateComputed) so no completeness dimension is silently skipped.

## GROUND-LADDER-001
WHEN information needed for a decision is missing, the orchestrating agent SHALL call the research tool first (or draft an R-item); only without a grounded answer ask via decide op=ask (internal/mcpserver/decide.go), the last resort.

## GATE-AUTHORITY-001
WHEN a lifecycle gate evaluates an item, the spectackle server SHALL treat recorded agent verdicts (EvReview, EvValidate) as the sole gating authority and computed findings as evidence requiring per-key addressal, never as an independent veto.

## ORCH-SYNC-001
WHEN a lifecycle transition call is refused or severed, the orchestrating agent SHALL stop the block before any git sync: the checkout runs only after a confirmed merged line - raw checkout over live records discarded a verdict (B-01KYFG1KEEF1S).

## NODE-EDGE-001
The spectackle server SHALL render at every lifecycle state the callers judgment options per nextAction while executing every transition edge mechanically per gitFlowFor.
