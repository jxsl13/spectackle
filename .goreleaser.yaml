version: 2

project_name: spectacle

builds:
  - id: spectacle
    main: ./cmd/spectacle
    binary: spectacle
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X github.com/jxsl13/spectacle/internal/mcpserver.Version={{.Version}}

archives:
  - id: spectacle
    ids:
      - spectacle
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}
    formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]

checksum:
  name_template: checksums.txt

changelog:
  use: github
  filters:
    exclude:
      - "^docs:"
      - "^spec:"
      - "^chore:"
