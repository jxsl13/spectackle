---
schema: v1
---

## intent
- B-01KYDGYMDCF2V86YYWHK181SPT records were never committed: the always-covered invariant failed for every .spectackle write: records commit mechanical at every transition; enumeration immune to output trimming; swarm worktrees excluded with a stated reason; invariant pinned by TestFullLoopLeavesNothingUncommitted
- B-01KYDKNQRWF719MBA8Y234KF88 automation commits carry a hardcoded spectackle@localhost identity, so they are unverifiable and misattributed: bootstrap note: the transitions landing this fix necessarily ran the pre-fix binary, so PR 62 own records commits are the last placeholder-authored ones; the live identity proof is the first records commit of the next iteration, made by the rebuilt server
- B-01KYEEJKQ0FCNTSJ6DDM2FVT1H detached-head fast-forward at submit advances no named branch while the result claims merged to main: Minor finding of the same review: when vacateBranch falls through every candidate and detaches the main checkout, a later submit fast-forwards the DETACHED head — no named branch receives the work although the result says merged to main. Self-healing in practice (the archive merge lands the branch into the real base, online via the PR and offline via the forge double), so deferred rather than lost, but the claim is wrong while it lasts. Fix direction: FFMainPreservingRecords detects a detached head and answers a distinct loud line naming the deferral instead of the merged claim; alternatively vacateBranch refuses detach when a submit is known to follow. Low urgency: requires a repo with no resolvable base at all.
- B-01KYHHCFCWEV48MC3MHT3M0PZY wt.Add silently force-removes a stray worktree directory that has no ledger row: validated pass by cross-val-wtadd diff 34d080daa092 :: Diff-only review of PR 166 from a fresh clone: all six requirements hold; both regression tests demonstrably fail against pre-fix logic (validator re-ran them on the parent commit); leftover-recovery path stays green; .git file-vs-dir probe correct for linked worktrees. Residual, pre-existing and out of scope, filed separately: wt.Add(root,root,...) self-collision destroys the main checkout (empirically confirmed - the guard classifies dirty-vs-clean, never is-this-the-main-root); TOCTOU window between guard and removal (same window existed unguarded before); dotDir/workspace.Dot drift has no compile-time tie; the git-worktree arms clean-pass branch lacks a dedicated test (shared logic with the tested plain arm).
- B-01KYHQ8A7NFRC8RMGCDJMC6H1C wt.Add with wtRoot equal to the main checkout destroys the entire main working tree: validated pass by cross-val-selfcol diff 34d080daa092 :: Fresh-clone review of PR 168: both guards sit before any removal and force cannot bypass either; the validator reproduced the original destruction on the parent commit in three scenarios (Add(root,root) clean AND dirty, unrelated full checkout under force - its .git degraded from directory to worktree file and a marker vanished) and confirmed the fix branch refuses each with the tree intact. Dangling-symlink path proven unreachable (os.Stat gates the block). Residual, self-disclosed, low: case-insensitive alias slips the string-compare guard but the .git-directory syscall backstop catches the destructive class; a case-variant alias to a linked worktree falls back to ordinary stray handling. Realistic production route noted: an absolute worktrees_dir override pointing at a full checkout - guard (c) covers it. Build/tests/gofmt clean.
