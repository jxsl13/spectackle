---
schema: v0
---

## T-0034 langspec framework + python/javascript reference specs
kind: task
state: done
created: 2026-07-24
parent: P-0019

Scope ONLY: new internal/langspec/ (langspec.go, python.go, javascript.go, langspec_test.go, .spectacle rule via driver), internal/graph/graph.go (Lang consts LangPy py, LangJS js ONLY), internal/index/langs.go (extLang entries .py/.js/.mjs). Design: type Def struct{Kind graph.NodeKind; Re *regexp.Regexp; Name int; Sig int}; type Spec struct{Lang graph.Lang; Exts []string; Qual QualMode; Defs []Def}; QualMode enum QualFileStem|QualDirPkg|QualFlat; type SpecParser struct{S Spec} implements index.LanguageParser (Lang/Extensions/Parse); Parse: line scan, per match mint ids.Mint(string(S.Lang), qual) with qual per mode (filestem: base name without ext + '.' + name; dirpkg: dir base + '.'; flat: name), Node{Kind,Lang,File,Line,EndLine:line,Sig: optional group}, deterministic, sha256 hash, no edges; func All() []index.LanguageParser returns SpecParser for every registered spec. python.go: defs ^\s*def\s+(\w+)\s*\( KFunc (indented = methods too, same kind v0), ^class\s+(\w+) KType. javascript.go (.js/.mjs): ^(?:export\s+)?(?:async\s+)?function\s*\*?\s+(\w+) KFunc, ^(?:export\s+)?class\s+(\w+) KType, ^(?:export\s+)?(?:const|let|var)\s+(\w+)\s*=\s*(?:async\s*)?(?:\(|\w+\s*=>) KFunc arrow. Tests: per-spec fixture strings -> expected node IDs/kinds/lines; determinism; e2e via index.New(graph.NewMem(), store.NewMem(), append([]index.LanguageParser{index.GoParser{}}, langspec.All()...), resolve.Default().All()) on temp tree with .py/.js -> nodes py:mod.func js:mod.Class present. Add EARS rule via driver: rule op=add dir=internal/langspec pattern=U system='the langspec registry' response='define a language purely as one `Spec` data value so adding a language never modifies the indexing pipeline' stem SPX-LSP applies=['go:langspec.SpecParser.Parse'] item=P-0019 (AFTER implementation + make build so the anchor resolves). Verify: go build ./... && go vet ./internal/langspec/ ./internal/index/ ./internal/graph/ && go test ./internal/langspec/ ./internal/index/ ./internal/graph/ ./internal/mcpserver/ -race green. NO server.go changes.

## T-0036 langspec batch A: rust + java
kind: task
state: done
created: 2026-07-24
parent: P-0019

Scope ONLY: new internal/langspec/rust.go, java.go, rust_test.go, java_test.go. NO edits to langspec.go/graph.go/langs.go or any shared file - register via func init() { registry = append(registry, <lang>Spec) } in YOUR file; use graph.Lang literals (graph.Lang("rs"), graph.Lang("java")) not new consts (orchestrator adds consts+extLang later). Cookbook: Spec{Lang, Exts, Qual, Defs}; Def{Kind, Re, Name(group), Sig(group,0=none)}; SpecParser handles the rest. rust.go: Exts .rs, QualFileStem; defs: ^\s*(?:pub(?:\(.*?\))?\s+)?(?:async\s+)?(?:unsafe\s+)?fn\s+(\w+) KFunc; ^\s*(?:pub(?:\(.*?\))?\s+)?(?:struct|enum|trait)\s+(\w+) KType; ^\s*(?:pub\s+)?(?:const|static)\s+(\w+) KVar. java.go: Exts .java, QualFileStem; defs: ^\s*(?:public\s+|private\s+|protected\s+|abstract\s+|final\s+|static\s+)*(?:class|interface|enum|record)\s+(\w+) KType; method heuristic ^\s{2,}(?:public\s+|private\s+|protected\s+|static\s+|final\s+|synchronized\s+)+[\w<>\[\],\s]+?\s(\w+)\s*\([^;]*$|...\)\s*(?:throws[^{]*)?\{ - keep it PRAGMATIC: it must match plain 'public static void main(String[] args) {' and 'private int count() {' and must NOT match field declarations ending ';' or control keywords (if/for/while/switch/catch/return - exclude via (?!...) or post-filter list). Tests: fixture strings per language covering every def + at least 2 negative lines (field decl, if-statement); determinism; init-registration proven via langspec.All() containing the new specs (count or lang set). Verify: go build ./internal/langspec/ && go test ./internal/langspec/ -race green.

## T-0037 langspec batch B: typescript/tsx + ruby + php
kind: task
state: done
created: 2026-07-24
parent: P-0019

Scope ONLY: new internal/langspec/typescript.go, ruby.go, php.go + one _test.go each. NO shared-file edits - init() append registration in your own files, graph.Lang literals ("ts", "rb", "php"). typescript.go: Exts .ts/.tsx, QualFileStem; defs: the three javascript patterns (function/class/arrow - copy shape from javascript.go) PLUS ^(?:export\s+)?interface\s+(\w+) KType, ^(?:export\s+)?type\s+(\w+)\s*= KType, ^(?:export\s+)?(?:const\s+)?enum\s+(\w+) KType. ruby.go: Exts .rb, QualFileStem; defs: ^\s*def\s+(?:self\.)?(\w+[?!]?) KFunc (name group must allow ?/! suffix - check ids.Mint accepts it: qual chars must satisfy idRe [^\s~]+, yes), ^\s*(?:class|module)\s+(\w+) KType. php.go: Exts .php, QualFileStem; defs: ^\s*(?:public\s+|private\s+|protected\s+|static\s+|abstract\s+|final\s+)*function\s+(\w+) KFunc, ^\s*(?:abstract\s+|final\s+)?(?:class|interface|trait|enum)\s+(\w+) KType. Tests: fixtures per language covering every def + negatives (ruby: 'end' lines, method calls; php: closure 'function () use' must NOT match - name group requires \w+ so it wont, prove it; ts: type alias + interface + tsx component arrow); determinism; All() contains the specs. Verify: go build ./internal/langspec/ && go test ./internal/langspec/ -race green.

## T-0038 langspec batch C: C headers/sources + C++ - real c: nodes complete the cgo chain
kind: task
state: done
created: 2026-07-24
parent: P-0019

Scope ONLY: new internal/langspec/c.go, cpp.go + c_test.go, cpp_test.go. NO shared-file edits - init() append in your files. CRITICAL DESIGN: c.go MUST mint lang tag "c" with QualFlat so nodes are c:<name> - these UPGRADE the cgo/cuda stub nodes (indexer: parser nodes take precedence, stubs only materialize for missing endpoints) and give the go->c->cu chain real header locations. c.go: Exts .c/.h, QualFlat; defs: function definition/prototype ^(?:[A-Za-z_][\w\s\*]*?[\s\*])(\w+)\s*\([^;{}]*\)\s*[;{]\s*$ KFunc - MUST match 'int launch_saxpy(int n, float a, const float *x, float *y);' and 'static void helper(void) {' - MUST NOT match calls (lines starting with whitespace: anchor ^ without \s), control statements (exclude names if/for/while/switch/return/sizeof via post-filter map), #define/#include (anchor excludes #), struct usage. Plus ^(?:typedef\s+)?(?:struct|union|enum)\s+(\w+) KType, ^#define\s+(\w+)\( KFunc macro-fn. cpp.go: Exts .cc/.cpp/.cxx/.hpp, QualFlat lang "cpp"... NO - use graph.LangCpp value "cpp" via literal graph.Lang("cpp") (const exists); defs: ^(?:class|struct)\s+(\w+) KType, ^(?:[A-Za-z_][\w:&<>,\s\*]*?[\s\*])?(\w+)::(\w+)\s*\( KFunc with Name group 2 qualified via group... SpecParser has single Name group - for methods use Name=2 and accept flat method name, note the limitation. Also plain functions like c.go pattern. ACCEPTANCE e2e (in c_test.go): index.New with GoParser+CudaParser+langspec.All() over ../../examples/saxpy => node c:launch_saxpy now has File=saxpy/kernels/saxpy.h (or .cu - both define it; deterministic first-in-path-order wins, assert File != "" and Line>0) AND the chain edges still hold (cgo + launch). Also assert NO false c: nodes named 'if' or 'return' appear when parsing a fixture with control flow. Verify: go build ./internal/langspec/ && go test ./internal/langspec/ -race green.
