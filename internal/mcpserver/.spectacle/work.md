---
schema: v0
---

## T-0031 research + grill tools (handlers only, no registration)
kind: task
state: draft
created: 2026-07-24
parent: P-0018

AFTER foundation task is done (needs new item fields). Scope ONLY: new internal/mcpserver/research.go+research_test.go, internal/mcpserver/grill.go+grill_test.go. NO tools.go/server.go edits (orchestrator registers). research: researchIn{Q string; Targets []string; Depth int default 2; Budget int default 2500}; (s *Server) research(in) via same handler shape as state; builder researchText: sections omit-if-empty: #impact (seeds from Targets node-IDs + graph.Find(Q) top hits, g.Impact depth, n/e lines), #contracts (reuse splitContractRules for targets paths + item dir), #rejections (cache.Search kinds rejection), #history (cache.Search kinds journal), #docs (cache.Search kinds section, s-lines), #gaps (targets not in graph -> g unknown <t>; targets without anchors via drift.Load -> g unanchored; reuse coverageGaps for dirs), #open (q-lines: generated questions e.g. 'q <target> has no binding rule', 'q no similar rejection found - risk unknown'); budget.TruncateRecords; STRICTLY read-only. grill: grillIn{ID string; Budget int default 1500}; loads item (nf if unknown); sections: #targets (unknown->nf, unanchored->g), #contracts (targets w/o binding rule), #briefs (children tasks failing heuristics: body<300 chars OR no '/' path token OR no verification command token like 'go test'/'make' -> one b-line each naming the failing heuristic), #tests (target packages under internal|cmd without *_test.go -> g), #rejections (similar via cache.Search), #questions (q-lines checklist: scope disjointness, rollback, exit criterion present?); THEN writes evidence: it.Grilled=today (item.Upsert) + journal EvGrill + cd.Emit('grill',id,...) - the ONE write. Tests: seeded workspace (draft items via lifecycle.Draft) asserting sections, heuristics matrix, grilled: header set after call, research read-only (mtime check).

## T-0032 decide tool + gate-fail rounds + prompt updates (no registration)
kind: task
state: draft
created: 2026-07-24
parent: P-0018

AFTER foundation task. Scope ONLY: new internal/mcpserver/decide.go+decide_test.go, internal/mcpserver/swarm.go (workSubmit gate-fail increments + escalation), internal/mcpserver/prompts.go+prompts_test.go (next skips blocked/open-needs; workflow gains optional task arg). NO tools.go/server.go. decide: decideIn{Op ask|answer|ls; ID; Question; Kind radio|confirm|text default radio; Options []string; Item; Choose}; handler (s *Server) decide(ctx, req *mcp.CallToolRequest, in) like rule (needs req.Session for elicit). ask: mint D via lifecycle.Draft(kind=decision, title=Question, body='options: ...' + 'blocks: <Item>'), if Item!='' append D-id to that item's Needs (item.Upsert); try elicitation (pattern elicitSlots tools.go:552: radio->string prop with enum=Options, confirm->boolean prop, text->string; Action accept -> choice from res.Content); on accept: resolveDecision(D, choice): D state=done body records choice, journal EvDecide, cd.Emit decide; if the linked item is blocked and choice in {rescope,reject,override-once} -> lifecycle.ResolveBlocked; else clear the D from item.Needs; return 'ok D-x <choice>'; on decline/error: D stays submitted, return 'need decision D-x <question> | <options joined>'. answer: validate Choose against stored options (free text ok for kind text), then resolveDecision. ls: open D items as i-lines. swarm.go: in workSubmit before EACH 'return text(res)' of GATE 1 and GATE 2 fails ONLY (not merge-conflict/main-dirty): rounds++ via helper (item.Upsert+journal Rnd); at max -> lifecycle.Escalate + cd.Emit('escalate',...)+ teardown like abort? NO - keep worktree open, return blocked message 'i <id> blocked rounds=N/N - decide D-x'. prompts.go: promptNext skip items state blocked OR with open needs (needs resolved = referenced item state done|archived or missing); promptWorkflow: optional 'task' argument - empty behaves as today; set -> prepend a LIFECYCLE block: run full SDD cycle for <task>: research q -> draft proposal targets -> grill + close gaps -> decide op=ask on unresolved uncertainty (never unstructured chat) -> move approved -> fanout one fresh cheap-model implementer per disjoint task -> check until ok -> done->archived. Tests: ask headless (in-memory transport has no elicitation? verify: mcp client Connect without elicitation handler -> Elicit errors -> need-path) covers need-path; answer resolves+journal; blocked+override path with a pre-escalated item; next skips blocked; workflow task arg contains LIFECYCLE.
