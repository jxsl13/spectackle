---
schema: v0
---

## P-0027 find/get node records show the definition span — start AND end line
kind: proposal
state: approved
created: 2026-07-24
grilled: 2026-07-24
targets: go:graph.Node

Requirement: search output shows where functions begin but not where they end. graph.Node.EndLine exists (anchor spans use it) but the n-record renderer emits only file:Line. Fix: n records render file:Line-EndLine when EndLine>0 (e.g. internal/graph/graph.go:210-250), unchanged single line when EndLine==0. Token cost: ~4 chars/record — well inside SPX-ARC-002 budgets; every consumer (impact packs, find, get, research) inherits the span for free. Update docs/tools.md grammar line for n records.

## T-0049 n records render file:start-end span
kind: task
state: done
created: 2026-07-24
parent: P-0027

SCOPE: the single function in internal/mcpserver that renders n node records (find it via spectacle: find q=nodeRec / inspect tools.go around getNode/find rendering; it formats '<file>:<line>'), its test file, and docs/tools.md (grammar section, n-record line only).

CHANGE (MCP-002 rule family): when n.EndLine > n.Line, render '<file>:<Line>-<EndLine>' (e.g. internal/graph/graph.go:210-250); when EndLine==0 or ==Line, keep '<file>:<Line>' exactly as today. Apply in EVERY place n records are rendered if there is more than one formatter — grep-free discovery via spectacle find scope=code q=records; consolidate to one helper if trivially possible without touching unrelated code.

TESTS: unit test with two nodes (EndLine set / unset) asserting both forms in find output over the wire (connectRoot pattern in tools_test.go); adjust ANY existing assertions that match the old single-line form if they break (they assert Contains, so most survive).

ROLLBACK: single revert; format-only change.
EXIT CRITERION: go build ./... && go vet ./... && go test -race ./internal/mcpserver/ green; docs/tools.md grammar updated. Constraints: never edit .spectacle/ (server-owned); never commit/push; scope is the renderer + its tests + one docs line — nothing else.

## P-0029 commands tool: harness detection + slash-command generation (claude/copilot/codex/kimi) from one server-side source
kind: proposal
state: approved
created: 2026-07-24
grilled: 2026-07-24
targets: go:mcpserver.Server.registerTools, .claude/commands/spectacle.md

Requirement: the harness entry-point commands (/spectacle workflow + state) must be GENERATABLE from the MCP server for the common harnesses — Claude Code, GitHub Copilot, OpenAI Codex, Kimi Code — instead of hand-written per harness. New tool `commands`:
- op=detect: inspect the repo root for harness markers (.claude/ -> claude; .github/prompts/ or .github/copilot-instructions.md -> copilot; AGENTS.md or .codex/ -> codex; .kimi/ or AGENTS.md -> kimi) and report as dense records (h <harness> <marker>).
- op=gen [harness=...]: render the two command bodies from ONE embedded server-side template set into each harness's convention: claude -> .claude/commands/{spectacle,spectacle-state}.md; copilot -> .github/prompts/{spectacle,spectacle-state}.prompt.md; codex+kimi -> managed, marker-delimited section in AGENTS.md (both read AGENTS.md; one shared renderer). No harness given: use detection; detection empty -> native elicitation (multi-choice via one boolean per harness, exactly the decide/rule form pattern); headless/declined -> mint an open D-item (kind=text, comma-separated harness list) and return need — answered later via decide op=answer, then op=gen harness=<answer>.
- MIGRATION: the existing hand-written .claude/commands/spectacle*.md become generated output — running op=gen harness=claude on this repo must reproduce their semantic content (two-mode /spectacle, state alias) from the templates; the files are then owned by generation (header comment 'generated by spectacle commands — edit the server templates, not this file').
Single source of truth: templates embedded in the server (go:embed), one content model (workflow two-mode + state), per-harness dialect wrappers only. Idempotent: re-gen overwrites only its own managed files/section. Rebrand note: T-0050 (runs later, alone) sweeps all names incl. these templates.
Rollback: additive tool; generated files are regenerable.

## T-0052 commands tool: detect + gen for claude/copilot/codex/kimi, template-driven, migration of .claude commands
kind: task
state: approved
created: 2026-07-24
parent: P-0029

SCOPE (disjoint from T-0051 which only owns docs/validation-ddnet.md): internal/mcpserver/commands.go (NEW), commands_test.go (NEW), templates/ under internal/mcpserver (NEW, go:embed), tools.go (ONLY: registerTools registration block for `commands` + docs line in the tool description), docs/tools.md (new #15 section + grammar h record), .claude/commands/spectacle.md + spectacle-state.md (REGENERATED by the tool at the end — see MIGRATION), .github/prompts/ + AGENTS.md in THIS repo (generated output, dogfooding all four harnesses).

DESIGN (P-0029, rule MCP-003 family + the new elicitation rule):
- type commandsIn struct { Op string `json:"op" jsonschema:"detect|gen"`; Harness []string `json:"harness,omitempty" jsonschema:"claude|copilot|codex|kimi — omit to auto-detect"` }
- op=detect: root markers — .claude/ dir => claude; .github/prompts/ dir OR .github/copilot-instructions.md => copilot; .codex/ => codex; .kimi/ => kimi; AGENTS.md alone => codex AND kimi (both consume AGENTS.md). Output: one `h <harness> <marker>` line per hit; none: `nf harness — pass harness=... or answer the decision`.
- op=gen: harness list = arg > detection > elicitation. Elicitation: req.Session.Elicit with RequestedSchema {type:object, properties:{claude:{type:boolean},copilot:{type:boolean},codex:{type:boolean},kimi:{type:boolean}}} (pattern: elicitSlots in tools.go, decideAsk in decide.go). accept => selected harnesses; decline/cancel/no-UI => mint decision via lifecycle.Draft(kind=decision, kind:text body 'options: free text — comma-separated subset of claude,copilot,codex,kimi') + move submitted + `need decision D-x ...` (mirror decideAsk exactly; reuse its helpers where exported or duplicate minimally).
- Templates: internal/mcpserver/templates/commands/{workflow.md.tmpl,state.md.tmpl} via go:embed, text/template, data {Binary:"spectacle", Tool:"spectacle"}. Content = the semantic content of today's .claude/commands/spectacle.md (two-mode: empty args => state; args => 8-step SDD lifecycle) and spectacle-state.md — READ those two files first, lift their bodies into the templates (dropping claude-specific frontmatter into the claude dialect wrapper only).
- Dialects: claude => .claude/commands/spectacle.md + spectacle-state.md (frontmatter as today + generated-header comment); copilot => .github/prompts/spectacle.prompt.md + spectacle-state.prompt.md (markdown, '---\nmode: agent\n---' frontmatter + generated-header); codex/kimi => ONE managed section in AGENTS.md delimited by '<!-- spectacle:commands:begin -->' / '<!-- spectacle:commands:end -->' containing both command descriptions (create file if missing; replace ONLY the managed section if present; never touch other content). Generated-header on every artifact: '<!-- generated by spectacle `commands` — edit internal/mcpserver/templates, not this file -->'.
- Idempotency: second op=gen run must produce byte-identical files (test).
- Writes go through the server process itself (os.WriteFile) — these are NOT .spectacle lifecycle files; no journal writes except ONE journal event ev=rule? NO — emit coord event only (s.cd.Emit("commands", strings.Join(harnesses,","), root)) and NO journal write (this is codegen, not spec history).

MIGRATION (dogfooding, part of this task): after implementing, run the driver on THIS repo: commands op=detect (expect h claude .claude/), then commands op=gen harness=claude,copilot,codex,kimi — regenerating .claude/commands/* from templates and creating .github/prompts/* + AGENTS.md managed section. The regenerated claude files MUST preserve the two-mode semantics ($ARGUMENTS dispatch) — diff them mentally against the originals; semantic loss = fix the templates, not the files.

TESTS (commands_test.go, connectRoot wire pattern): detect on tempdir with .claude => h claude; detect on empty tempdir + gen => decision minted (need decision, D-item exists kind=decision); gen harness=copilot writes .github/prompts/*.prompt.md with generated-header; gen harness=codex twice => AGENTS.md managed section present exactly once, byte-identical second run; gen harness=claude writes both files; elicitation-accept path via the connectDecide elicit-handler pattern (decide_test.go) selecting {claude:true,codex:false...} => only claude files written.

ROLLBACK: additive tool + regenerable files; single revert.
EXIT CRITERION: go build ./... && go vet ./... && go test -race ./internal/mcpserver/ green; make lint-specs clean; driver run on this repo shows h claude + generated files present. Constraints: never edit .spectacle/ directly; never commit/push; tools.go only the registration block; TestToolSurface will need `commands` added to its want-set — that assertion lives in tools_test.go, add it there (allowed, one line).
