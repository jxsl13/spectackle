---
schema: v1
---

## B-01KYHEQG6PEBFBX49MPEZMVNH6 compact apply=true archives done items bypassing the validation gate and the atomic git closure edge
kind: bug
state: done
created: 2026-07-27
grilled: 2026-07-27 open=0
targets: internal/mcpserver/tools.go

OBSERVED (2026-07-27, live incident on B-01KYHCHRN0): compact apply=true swept the done item straight to archived via raw lifecycle.Move(StateArchived) (tools.go:2352, gateOpts carrying ONLY WithAuditGate) while PR 163 was open with CI failing and no validation verdict existed. GUARD MAP (grill-verified): lifecycle.Move enforces transition legality + audit gate + open-children; the validate-require gate (tools.go:1568-1587 via validateGateGap/validateRisk), the atomic archive edge with gitFlowFor + archived->done compensation (tools.go:1672-1694, gitflow.go gitFlowMerge incl. stranded-PR check), and the research unconsumed-citation check (tools.go:1559-1563) all live ONLY in the mcpserver move handler - compacts raw call bypassed every one, stranding the PR with no compensation record. NODE-EDGE-001 and ROLE-BOUNDARY-001 both mandate every transition edge runs gitFlowFor mechanically; anchors are structurally blind to a caller that skips it (ROLE-BOUNDARY-001 pins gitFlowFor's body, not its callers). FIX (Option A, closes the class): compact's done-item sweep calls s.move(moveIn{ID, To: archived, Note: compact}) - the same-package handler - so validate gate, gitFlowFor, compensation, and the research check all auto-apply, and any FUTURE move-handler guard covers compact for free (guard duplication across callers is this bugs root pattern). Compact branches on res.IsError (refuse() returns IsError=true with err==nil - the current err-only check misses every guard refusal) and renders c done-item <id> blocked: <reason first line> while the item STAYS done; successful archives render as today. Dry-run listing unchanged. COST accepted and documented in the sweep comment: each archived item runs the real closure (CI wait up to 5m per item, checkout churn) - compaction of journal events must never close a lifecycle more cheaply than the move edge would. lifecycle.Move(StateArchived) census: move handler + compact are the only archive callers; no second sweep site. TEST (both missing today - the sweep has zero coverage): (1) blocked: fixture with feedback.validate=require, item driven to done, compact apply=true -> output carries done-item and blocked, item still done; (2) closure: gitRoot(t)+writeOfflineGitConfig fixture (offline forge merges locally), verdict from a second SPECTACKLE_AGENT, compact apply=true -> item archived through the handler path, merged line rendered. VERIFY: go build ./... && go test ./internal/mcpserver/ -count=1 && gofmt -l . empty. SCOPE: the compact sweep block + tests; no lifecycle package changes. ROLLBACK: revert.
