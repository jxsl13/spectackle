---
schema: v1
---

## B-01KYHQ8TQ6E78VP0B74VVZZ7Z5 validate op=pack on an archived item renders bare nf journal refs instead of the suppressed-pack line
kind: bug
state: draft
created: 2026-07-27
grilled: 2026-07-27 open=0
targets: internal/mcpserver/validate.go

OBSERVED (2026-07-27, B-01KYHHCFCW immediately after its archive): validate op=pack id=<archived item> returns exactly one line - nf j:internal/wt#17 j:internal/wt#18 j:internal/wt#19 - and nothing else, exit 0. EXPECTED per the archived-pack convention (compaction-survival work): archived items render computed: suppressed (archived) or an explicit item-is-archived refusal; bare nf nearest-match refs are the not-found grammar for lookups and tell the caller nothing about WHY the pack is empty. LIKELY MECHANISM: the pack resolves the item to its tombstone and its verdict-ref resolution walks journal indexes that no longer line up post-archive (or post-fold); the nf fallback then swallows the real answer. FIX: validate op=pack on a terminal item short-circuits to an honest line naming the state (i <id> archived - pack suppressed; verdicts live in the journal tombstone) before any ref resolution. TEST: e2e - archive an item through the offline fixture, call validate op=pack, assert the suppressed line and NO nf output. VERIFY: go build ./... && go test ./internal/mcpserver/ -count=1 -run Validate && gofmt -l . empty. SCOPE: the pack entry path only. ROLLBACK: revert.

## B-01KYHV740TE8Q8KHKCV95VC3JS offline archive gate is weaker than the online draft-flip it mirrors: red-gated done reads as passing, and everActive dies with journal compaction
kind: bug
state: draft
created: 2026-07-27
grilled: 2026-07-27 open=0
targets: internal/mcpserver/gitflow.go, internal/mcpserver/validate.go

FOUND by cross-val-offline (PR 167, H1+H2, empirically demonstrated). H1: tools.go move() journals EvMove BEFORE gitFlowFor runs the gate, and a red offline gate only renders an ephemeral GATE E line - lastGateResult (validate.go:1006) reads ANY EvMove->done as last=pass, so a red-gated item archives ungated (and the validate packs g gate line shows a false pass to human validators - the second consumer the first draft missed). H2: compacts fold switch (tools.go:2405-2426) has no EvMove case - live items activations fold away and everActive reads false, silently skipping the offline archive gate (JournalMax 300 is shared per context dir - realistic, not contrived). GRILL-CORRECTED DESIGN (round 2): the archive re-run condition at gitflow.go:507 is ALREADY correct and stays untouched - only its inputs lie. (1) CARRIER: extend-the-EvMove-note is unimplementable (Move already returned; journal has no update-in-place) - reuse swarm.gateFail()s shipped pattern instead: on a red gate, gitFlowOffline appends a separate same-state EvMove{Fr:state,To:state,Note:gate fail}; mirror in gitFlowReadys online gate branch so the validate display stops lying online too; lastGateResults matching logic is untouched. (2) COMPENSATION INTERPLAY (new, found in round 2): the archived->done compensation event has To==done and would read as a passing done edge, so the SECOND archive attempt after a refused red one would skip the gate - lastGateResult must ignore Fr==StateArchived events. (3) FOLD SURVIVAL: add case journal.EvMove to the fold switch with a pre-pass (reusing the terminal map): per NON-terminal item keep the last To==active event and the last gate-outcome event (To==done with Fr!=archived, or same-state gate fail); terminal items fold fully as today. (4) REJECTED: the item-record fallback - item.Item has no such fields, and by gitFlowMerge time the record is already DELETED (lifecycle archive() removes it before gitFlowFor runs); inventing persisted fields costs more than the keep-list and re-imports the stale-copy hazard the compensation already hand-manages. TEST MATRIX (9+1): T1 red-gate done journals the marker, archive refuses, item stays done, and a SECOND archive attempt still refuses (pins the compensation interplay); T2 green retry archives; T3 lastGateResult unit (pass/fail/pass-again sequence, compensation event ignored); T4 everActive unit incl. synthetic fold; T5 compact between activation and archive does not skip the gate (tiny JournalMax) AND a green lifecycle across a fold archives cleanly; T6 terminal items EvMove still folds; T7 compactCandidates trigger math untouched; T8 never-active archive still gate-free (extend the existing test); T9 validate pack shows g gate last=fail after a red done edge. VERIFY: go build ./... && go test ./internal/mcpserver/ -count=1 && gofmt -l . empty. SCOPE: gitflow.go (two gate branches), validate.go lastGateResult (one skip condition), tools.go fold switch, tests. ROLLBACK: revert.
