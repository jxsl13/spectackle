---
schema: v0
---

## T-0030 foundation: item fields + blocked state + rounds/escalation in lifecycle + journal/replay
kind: task
state: active
created: 2026-07-24
parent: P-0018

Scope ONLY: internal/item/item.go+item_test.go, internal/workspace/workspace.go+workspace_test.go, internal/lifecycle/lifecycle.go+lifecycle_test.go, internal/journal/journal.go+journal_test.go, internal/replay/replay.go + new replay_test.go. NO mcpserver changes. (1) item.go: fields Rounds int (header rounds:), Grilled string (grilled:), Needs []string (needs:, splitList), Override bool (override: true); kindLetter += decision:D; IDRe+reItemHeading [PTBR]->[PTBRD]; StateBlocked const blocked; parse+serialize (writeWork optional lines after goal:), tests roundtrip. (2) workspace.go: FeedbackCfg{MaxRounds int yaml max_rounds; Grill string yaml grill} as Config.Feedback; defaults 3 and warn in defaultConfig AND load() zero-block. (3) journal.go: EvGrill/EvDecide/EvEscalate consts; Event fields Rnd int json rnd, Gr string json gr, Nd []string json nd (reject snapshot roundtrip). (4) lifecycle.go: blocked as side state - Allowed(blocked)=[] and allowed() rejects every LLM move from/to blocked with error naming the linked D (find via Needs); Move done->active reopen: Rounds++ persisted + journal Rnd; if Rounds>=ws.Cfg.Feedback.MaxRounds instead call new exported Escalate(ws, mint Minter, it) (item.Item, decision item.Item, error): sets state blocked, mints D-item kind=decision title='escalate <id>: rescope|reject|override-once' body lists options (omit override-once if it.Override already true), links it.Needs=append(D-id), journal EvEscalate, returns both; new exported ResolveBlocked(ws, id, outcome, note): rescope->draft (Rounds=0, keep Grilled), reject->rejected (note required, snapshot incl new fields), override-once->active (Rounds=0, Override=true; error if already Override); reject-snapshot save/restore includes Rounds/Grilled/Needs/Override via Rnd/Gr/Nd (+Override in Nd? no: add Ov bool json ov). Also add Ov bool to journal Event. Tests: reopen increments, 3rd reopen escalates (blocked + D exists + needs link), blocked immovable via Move, ResolveBlocked all three outcomes, override only once, snapshot roundtrip keeps counters. (5) replay.go: mixed-journal test proving grill/decide/escalate events are tolerated (skipped or applied) without error; add minimal default-skip if needed. Verify: go build ./... may FAIL only in internal/mcpserver if signatures unchanged - it must NOT: change NO existing exported signatures (Move/Draft keep signatures; Escalate/ResolveBlocked are new). go vet + go test -race on the five packages green; also go build ./internal/mcpserver/ must still compile.
