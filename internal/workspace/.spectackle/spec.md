---
schema: v1
---

## intent
- T-01KYDNNVCRFSDVF6ZCXM1XAJB2 the server recommends and scaffolds a pre-push hook running the local gates, so human pushes are gated like automated ones: validated pass by cross-val-hook diff 5ad38304b535 :: [consent] the default writes nothing into .git (asserted), foreign hooks are refused with chaining guidance, and the opt-in is a deliberate subcommand - the rejected config-knob alternative is recorded with the committed-file-flips-push-behavior reasoning, which is the strongest part of the landing. [unification] the hook shells to spectackle verify on PATH: one gate definition, config-inherited, rebuild-proof. [refute] tried: PATH without spectackle makes the hook fail every push - acceptable and self-explaining (the hook comment names the requirement, and a failing hook is git-bypassable with --no-verify, the operators own escape). Re-ran the end-to-end hook execution from the diff. Zero waivers. — Human pushes and automated transitions now share one local gate definition: spectackle verify (the workspace verify commands), recommended by a state #health hint when unguarded, installed only by the operators explicit spectackle hook install (never clobbering foreign hooks, never auto-installed via config - a committed knob flipping push behavior for every clone is the slipped-in class ADR-01KYDG forbids). Live on this repo: the hint renders and stays for the operators call. The draft backlog is now EMPTY - remaining work is v0.2.0 release mechanics (release notes from the archive intent log, ldflags version stamp, tag) inside the Aug 1-2 window.
