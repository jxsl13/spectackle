---
schema: v1
---

## T-01KYE4M52DEW8VE3BE3MMNWPNW tricky judge scenario: rule slots, a reopen cycle into blocked, and the decide exit — the side states meet an agent
kind: task
state: done
created: 2026-07-26

The judge stage covers draft, active-adjacent, archived and rejected; the scripted driver alone touches the tricky rest. The mandate wants agents over ALL states, and the manifest-trim question needs judges that exercise the flows whose guidance a trim could silently break (rule slots, escalation, decide). Change: agent-prep gains -scenario basic|tricky (default basic, byte-identical); tricky writes a scenario sidecar and a second fixed brief with three goals: (1) a rule with stem JUDGE-API in dir api whose response names a number — exercises slot elicitation, the need-refusal path and the W001 lint; (2) a task titled stubborn rework loop driven to done, reopened, driven to done again, reopened again until the server side-steps it to blocked and mints a decision, which the agent resolves choosing rescope so the task ends in state draft — exercises reopen counting, the offline draft-flip, ROUNDS escalation, and decide op=answer; (3) a final check with no E findings. Scorer branches on the sidecar: tricky validity = find scope=rule q=JUDGE-API answers an r line, the named task ends draft (via get), meter.log carries a decide call whose argv contains op answer at exit 0 (an agent cannot legally end that task at draft through decide without the escalation having minted the ADR), and the check taxonomy holds; the goal line names all three. Probed ground truths recorded here so the scorer is not built on hope: find scope=history shows only create and archive events so intermediate moves are NOT scorable there; the second reopen at max_rounds=2 answers ROUNDS E naming the ADR; decide op=answer choose=rescope lands the item at draft. Tests: tricky prep pins brief content and sidecar; a scripted simulation drives all three goals through the shim and scores valid; a basic workspace scored by the tricky scorer stays impossible by construction (sidecar absent); basic prep byte-identical, pinned. VERIFY: go test ./internal/bench/ ./cmd/spectackle/ -count=1 green; a live tricky-judge batch n=3 aggregate-scored in one command, results and observed wandering recorded in the archive note.
