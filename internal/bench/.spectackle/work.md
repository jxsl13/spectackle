---
schema: v1
---

## T-01KYE9G26FFGMVWJQNWH1JXMPY worktree judge scenario: an agent delivers a code change through start, edit under the reported root, and submit — the swarm core meets its judges
kind: task
state: done
created: 2026-07-26

With B-01KYE8 the work flow is drivable headlessly, so the last untested tool family gets its judge scenario. agent-prep gains -scenario worktree: the brief names the deliverable (the function Serve in api/api.go returns 7 on the repository default branch, the task titled worktree delivery run ends done, check reports no E) and one constraint — workspace files are read-only except under a root the tools report — while HOW remains the judged discovery: the tool list already names work, and the manifest MODES pointer plus the start hint carry the rest. Scorer (scenario sidecar worktree): the main api/api.go contains return 7 read directly from disk; the task line shows done in the live state listing (done items never tombstone); the meter carries exit-0 work calls for both start and submit — this is what distinguishes the worktree flow from an illegal direct edit that would satisfy the file assertion, so WorkOK is a first-class goal column; the inherited integrity checks and the E-free check apply. Tests: worktree prep pins brief and sidecar; a scripted simulation through the shim — draft, approved, start, an edit under the root start reports, submit with explicit item from the same shim (each callOnce is its own process, exercising the B-01KYE8 reattach through the metered surface), move to done, check — scores valid with all four goal columns true; a workspace where the file was edited directly on main without work calls scores invalid on WorkOK. VERIFY: go test ./internal/bench/ -count=1 green; a live batch of three fresh judges aggregate-scored in one command, results in the archive note, whichever way it lands.
