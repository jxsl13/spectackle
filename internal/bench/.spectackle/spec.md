---
schema: v1
---

## intent
- T-01KYDPSW87FKX8FWQBVNDN89TT the harness: generated all-state fixtures, a scripted lifecycle driver over the real tool surface, token and validity metering, A/B against two builds: the harness stands: generated all-state fixtures, a scripted driver over the real tool surface with agent-realistic ambiguity recovery, byte and token metering per tool, validity gated on the server severity taxonomy, A/B between two binaries via spectackle bench -against, make bench target. Baseline measured: 3486 bytes, about 871 tokens, for a complete eight-state lifecycle. Found and fixed on its first three runs: expand-built ambiguity refusals still exited zero (a hole in the B-01KYD4H sweep), the blocked escalation is a success not a refusal, and check validity must follow the E versus W taxonomy or it measures the runner instead of the surface
- T-01KYDTQDN1EHCSJ5QH5N3XTZ36 bench fixture v2: multi-dir rules inventory so text-surface changes measure at real magnitude: Motivation, measured: the state rules-inventory collapse (T-01KYDQ) benched at minus 17 bytes on the fixture but minus 702 bytes on this repository — a 40x underestimate, because the fixture holds a single directory with a single rule while the real repository renders a dozen-plus per-dir inventory lines in every state call. A harness that underestimates a change class by that factor cannot rank competing text implementations, which is its entire purpose. Change: enrich the generated fixture with a realistic directory topology — three to four source dirs (api, store, cli, plus one nested dir) each with a compilable Go file — and seed each with two to three rules through the real tool surface (rule op=add) in an unmetered seed phase that runs after Fixture and before the metered script; seeding through tools keeps the server the sole author of record files, so grammar changes never desynchronize hand-written fixtures. The metered Script stays byte-identical in structure (same steps, all eight states still covered by construction) so cross-version step comparisons stay meaningful; only the ambient workspace is richer. Report labels the fixture as v2; absolute totals are not comparable to v1 baselines and the record must say so. Acceptance: (1) TestScriptCoversAllStatesByConstruction unchanged and green; (2) a state call in the enriched fixture renders a multi-dir rules inventory (asserted by a new unit test on the seeded workspace); (3) self A/B — current binary against itself — reports 0B delta at equal validity, proving seed determinism; (4) the magnitude proof: bench -against a pre-collapse binary (first parent of the T-01KYDQ merge) shows the collapse class at over 100 bytes on the enriched fixture, versus the 17 bytes v1 measured. VERIFY: go test ./internal/bench/ -count=1 green including the full-run test; spectackle bench self-A/B delta 0B; the magnitude number recorded in the archive note.
- T-01KYDZ724PE9P98MDXT5Y8BT0M agent-judge stage: bench prepares a metered fixture and brief for an independent agent and scores its run mechanically: first live judge baseline against this build, two independent fresh haiku agents, zero documentation beyond tool names, guided only by the server texts under judgment: judge A 17 calls 4677 bytes ~1169 tokens valid, judge B 17 calls 3846 bytes ~961 tokens valid — both reached task archived, bug rejected, check ok, confirmed by the mechanical scorer, not by agent self-report. The 831-byte spread between two valid runs at identical call counts is the wandering signal future text A/Bs should move: better guidance shows up here as fewer bytes to the same verdict. P-01KYDP stage 4 delivered; every stage of the proposal is now landed
- B-01KYE0RCKEFGBVT4GQ06GAR3FD judge fixture leaves root files uncovered, so the brief goal check-reports-ok is not literally reachable: Found by live judge C: the v2 seeds cover api, api/handlers, store and cli but not the root main.go, so every check on a fresh judge workspace answers with the uncovered gap line instead of plain ok. The brief states the third goal as a final check call reports ok — a diligent agent takes that literally and rabbit-holes into rule-writing to clear the gap (judge C spent 29 extra calls there; judges A, B and D shrugged and stopped, which happens to match the scorer, whose CheckOK accepts any E-free output). Brief and scorer must agree and the straight path must exist: add a root-context rule to seedRules (the scripted bench does exactly this with its rule/add step for the same reason), bumping the seeded totals to 8 rules across 5 dirs; update TestSeededFixtureRendersMultiDirCleanInventory accordingly. VERIFY: a fresh agent-prep workspace answers check with plain ok; the multi-dir inventory test pins total=8 dirs=5 findings=0.
- T-01KYE2DGSDEG5R0P6C8WHP8WZB judge scores aggregate across runs: one summary line with validity ratio and calls and bytes spread, so variance is read from n runs, not one: live three-judge batch, one command, all fresh runs against the current build: agents n=3 valid=3/3 calls=12/14/14 bytes=1038/1119/1135 — the byte spread collapsed from 831 at the baseline pair to 97 across three runs, evidence the guidance improvements are stable rather than lucky, and the aggregate line is now the standard verification currency for text changes: n=3, all valid, spreads quoted
- T-01KYE3XEK2EE1VJQ9VKE5TZKJB manifest-aware judges: agent-prep optionally prepends the connect-time manifest to the brief, and the score reports session cost: experiment answered, both directions quantified for the first time. Manifest trio versus zero-doc trio, same build, same goals, mechanical scoring, all six valid: calls 8/9/9 versus 12/14/14, tool bytes 639/715/715 versus 1038/1119/1135 — the dieted core manifest cuts wandering by a third and two manifest runs were BYTE-IDENTICAL at 715, the tightest spread ever measured. Session-shaped totals: 4112-4188 bytes with the 3473-byte manifest riding along, roughly 3.7 times the zero-doc session on this deliberately short flow. Conclusion recorded for future trims: connect-time guidance genuinely buys navigation (fewer calls also means fewer reasoning tokens in real sessions, beyond the metered tool bytes), its upfront cost dominates only on flows shorter than a handful of calls, and it amortizes with session length — from here every manifest trim or growth can be argued against these two measured curves instead of taste
- T-01KYE4M52DEW8VE3BE3MMNWPNW tricky judge scenario: rule slots, a reopen cycle into blocked, and the decide exit — the side states meet an agent: live tricky batch, three fresh haiku judges, zero coaching, mechanical scoring: agents n=3 valid=3/3 calls=30/34/38 bytes=3223/3612/4202 — every one navigated rule slot elicitation under the W001 lint, the reopen counter, the ROUNDS side-step into blocked, and the decide rescope exit purely from server texts, with the recent refusal and need-exit fixes plausibly load-bearing on exactly these paths. Agent-side state coverage now spans draft, active, done, archived, rejected, blocked and the rescope exit; submitted and approved remain scripted-driver territory by design (judge briefs do not involve an approval authority). The tricky curve (about 3600 bytes median at about 34 calls) joins basic (about 1100 at 13) and basic-with-manifest (about 700 tools at 9) as the third reference curve for future text A/Bs
- B-01KYE6GCM4EVXS9NHZDXTT91GQ the judge meter is not tamper-evident: an agent that bypasses the shim or rewrites the log scores as cheap instead of disqualified: Found live on judge U2 (T-01KYE5 rerun): its completion notification reported 130 tool uses while the meter held about 81 lines at scoring time — and a post-finish rescore found meter.log REWRITTEN down to 28 lines with the fourteen recorded decide refusals gone, flipping the run from 81-call invalid toward a cheap-looking reading. Whether the agent truncated the log, re-ran parts through the real binary it could read out of meter.sh, or re-prepped, the conclusion is the same: the meter is writable evidence in the workspace the agent controls, the brief-level ONLY-via-shim rule is unenforced, and a desperate agent under a hard scenario will violate it. Scores derived from a tamperable meter cannot rank builds. Fix direction, detection over prevention (prevention is unwinnable on a shared filesystem): the shim appends a per-line sequence number and the prep records the shim inode and a nonce inside meter.sh; the scorer disqualifies a run — a new verdict, DISQUALIFIED, distinct from invalid — when the meter has sequence gaps or resets, when its line count is implausibly below the server journal write-event count for the workspace, or when the recorded nonce disagrees. The aggregate treats disqualified like invalid for the exit gate but labels it separately, because the remedies differ: invalid means the texts failed the agent, disqualified means the measurement failed the harness. VERIFY: unit tests for gap, reset and nonce mismatch each yielding DISQUALIFIED; a straight scripted run through the shim stays valid; the U2 forensics recorded as the motivating case.
- T-01KYE9G26FFGMVWJQNWH1JXMPY worktree judge scenario: an agent delivers a code change through start, edit under the reported root, and submit — the swarm core meets its judges: first live worktree batch, three fresh haiku judges: agents n=3 valid=2/3 calls=14/16/19 bytes=1405/1823/2369. W2 and W3 navigated the full swarm core — draft, approved, work start, edit under the reported root, submit with the cross-process reattach, done — purely from server texts at 16 and 19 calls: the machinery B-01KYE8 opened is agent-navigable on first contact. W1 delivered the file change and the done state but flow=false: no successful work start plus submit pair in its meter — it edited main directly, the exact shortcut the flow column exists to catch, caught on its first live outing. W1s meter also moved backward during the run without tripping the B-01KYE6G checks, which exposed the next integrity hole recorded as its own bug: the shim forwards every subcommand, so re-running prep through the sanctioned shim resets nonce, baseline and seeds consistently. Two readings of W1 both stand: the scorer worked, and the guidance did not stop a determined shortcutter — one of three at this n is variance to watch, not yet a text defect. Judge coverage now spans every tool family and every reachable state
- B-01KYEAE3A8FPMSHJD4KSJ2WGZM the meter shim forwards every subcommand, so an agent can re-prep its own workspace and reset the tamper evidence consistently: Observed on live worktree judge W1: its meter moved backward from fifteen to eleven lines during the run and finished at fourteen with forty-one agent tool uses, yet none of the B-01KYE6G checks fired — sequence contiguous, nonce matching, journal delta within bound. The consistent explanation: the shim passes its ENTIRE argv to the real binary, so <shim> bench -agent-prep DIR re-runs prep, which rewrites meter.sh with a fresh nonce, resets the journal baseline, and re-seeds — a full evidence reset that leaves every integrity check green. The brief-level never-call-another-binary rule does not help because the reset travels through the sanctioned shim itself. Fix: the shim refuses any first argument other than call (one guard line before the passthrough, exit 2 with a refusal naming the restriction), and prep additionally records its own generation stamp in the scenario sidecar so the scorer can flag a workspace whose prep artifacts are newer than its oldest meter line if a future bypass appears. VERIFY: unit test drives the shim with bench and manifest argv and asserts refusal without execution; a re-prep attempt through the shim in the tamper test yields the refusal; the existing three tamper shapes stay covered.
- ADR-01KYEVT1G6F2A847PX2TZ84R4Q Activate the outcome-scored benchmark proposal (hidden acceptance tests measure first-iteration completeness per token, role-boundary contract, catch-rate calibration for the review chain), and where in the queue?: kind: radio
- T-01KYFSN48KEK88SNR9MSJRFWA4 role-boundary contract: the server owns every mechanical git step and the bench fails any transcript instructing the agent to run git: validated pass by cross-val-t1 diff 2f14e8919f70 :: Round-1 vacuity fixed and re-verified: the two-hit input now pins count=2 before the shape loop. Re-ran the calibration proof from the diff alone - scripted bench valid=true with the detector armed, zero hits on the live surface; negative control covers real record shapes including mid-sentence git prose. Anchor a-line for ROLE-BOUNDARY-001 at gitflow.go:715-736 confirmed in the diff. — Role-boundary contract live: ROLE-BOUNDARY-001 anchored to gitFlowFor, bench detector (two calibrated regexes, zero false positives on the live surface) wired into both modes - scripted Run transcripts and the agent meter shim, which now tees transcript.log scanned into AgentScore.Violations, required empty by every scenario Valid gate. One validation round paid: validate caught my fatal-shape test asserting only inside a range (its own vacuous class) - fixed by pinning count=2; the machinery catching its own author is the strongest evidence this chain works. Rider: seven doubled-WHEN root rules re-composed clean; composer normalization gap filed as B-01KYFSZ7. Learning landed: rule anchor + detector + this tombstone.
- P-01KYEVSK7BEEZBE7GGS4HSD5SV outcome-scored benchmarks: hidden acceptance tests in seeded fixtures make first-iteration completeness a measured number per token: Outcome-scored benchmarks delivered by two children. Role boundary: ROLE-BOUNDARY-001 anchored to gitFlowFor with the bench detector as its verifiable artifact (calibrated regexes, both modes, shim transcript tee) - a tool surface that instructs the agent to run git is now a measured validity failure. Outcome scoring: hidden acceptance tests (harness-held, discriminance-pinned) define complete; first-pass/final-pass/rounds columns; traps for vacuous tests and offscope edits; efficiency = first-iteration completeness per 10K tokens; comparisons REFUSE unequal validity. Exit criterion met live: sonnet judge, first-pass 5/5, rounds 0, valid, 949 surface tokens - the cost-vs-quality axis the whole workflow optimizes is now measured, not argued. Deferred with owners: review_findings_addressed column and the first hint-variant A/B on the outcome fixture (calibrates feedback.validate risk-gating with real catch rates) - both noted in the children tombstones as the next benchmark iteration.
- B-01KYGZNT0WEZ4V5B7VTVWADFDR meter shim sequence check false-disqualifies judges that batch parallel tool calls: validated pass by cross-val-shim diff eb6b5e095b4b :: [rootcause] the corrected mechanism is proven three ways in the diff: the multiline-argv regression test reproduces the exact incident signature, the sanitizer kills it, and the hole-no-duplicate forensics that ruled out racing are recorded in the done note. [integrity] every tamper property survives - mid-file deletion still disqualifies (via completeness), duplicates disqualify, the journal write-delta owns truncation, and the 24-way stress pins the lock. [honesty] warn-2 retired as ambiguous rather than retro-blessed - the right call, a post-hoc hole cannot be distinguished from truncation. [refute] tried: tr eating meaningful newlines from argv changes what the writeTools parser sees - the parser reads whitespace-split fields and the tool name position is unaffected. Zero waivers. — Meter integrity no longer false-positives: argv is one sanitized line per entry (the real incident mechanism - multiline draft JSON in argv, proven by a regression test driving the shim with a multiline body), the mkdir spinlock hardens against genuine parallel-call races (portable; the reviewer correctly killed the briefs flock(1)-on-darwin assumption), and the scorer judges sorted completeness so order never lies while holes and duplicates still disqualify. warn-2 stays retired as ambiguous. The n>=3 catch-rate rerun is unblocked - the next benchmark iteration owns it.
- B-01KYH3SP63E3NVV5V31G2GTD6K bench prep leaves meter and transcript logs git-trackable; the first sweep commits them and the offline merge then refuses forever: validated pass by cross-val-art diff eb6b5e095b4b :: [rootfix] re-ran both tests from the diff: post-prep git status is artifact-free, and the interleaved shim drive - the exact shape the require-3 judge proved impossible - archives and merges. [placement] the ignore lands in AgentPrep before any judge action, appended to any existing fixture .gitignore; execution paths unaffected (gitignore governs tracking only). [refute] tried: the lock dir entry meter.log.lock/ could mask a real fixture dir of that name - no fixture generator creates one and the trailing slash scopes it to directories. Zero waivers. — Bench harness artifacts can no longer poison the fixtures git flow: prep gitignores them before the first sweep, pinned by the post-prep status test and by reproducing the require-3 judges structurally-impossible drive as a passing e2e (shim-driven task, every call dirtying the logs, offline merge completes). The forensic report that found this - a benchmark judge tracing a harness bug to source, refusing the workaround, and stopping honestly - is the sessions best evidence that the refusal-driven workflow produces exactly the agent behavior it was designed for. Measurement infrastructure is now clean for future catch-rate batches.
- T-01KYH54HE1EYZTETA37HFTNPBZ outcome brief tool list is complete: validate and work were discoverable only through refusals, costing every require judge a loop: validated pass by cross-val-brief diff c2b8c47d1145 :: [evidence] the change is two words on one line, justified by two independent judge reports; the honesty test checks all four briefs against the registered surface in both directions. [restraint] siblings audited and deliberately unchanged with the reasoning recorded - no speculative list inflation. [refute] tried: compact/commands/reindex also exist and are unlisted everywhere - correctly so, no scenario needs them and listing them would be map noise. Zero waivers. — The outcome briefs tool list is a complete map (validate, work added; siblings audited and correctly unchanged), pinned by a both-directions honesty test against the registered tool surface. Evidence-driven from two judge reports that paid refusal-scavenger loops - hint quality measured by what independent agents actually stumble on, which is the mandates core loop working.
- B-01KYJ67RF9ERESZKRFK30SMN12 agent-score hides the violations that flipped a run INVALID and mislabels the verdict as goals not reached: validated pass by validator-heron diff eb6b5e095b4b :: Independent fresh-clone validation of PR 192. Reproduced: build, all 23 bench tests, gofmt green; diff confined to agent.go, bench_test.go and records; the Valid invariant is byte-identical at all three scoring sites (Valid = GoalsOK && !Disqualified && no violations) with per-scenario GoalsOK conjunctions matching the old inline forms; adversarial matrix confirmed — DISQUALIFIED wins precedence while still rendering violation lines, goals-red + violations honestly says goals not reached while showing the violations, the B-01KYJ67RF9 case (goals held + violation) renders the violation line and INVALID — violations (1), and valid runs stay clean. Zero findings. — AgentReport is never-silent about violations: one agent violation <text> line per entry renders before the verdict, and INVALID splits into violations (n) when the scenario goals all held vs goals not reached when they did not. New AgentScore.GoalsOK carries the goal conjunction alone, set beside each of the three Valid assignments (worktree, outcome/default, tricky) so the render never re-derives scoring; Valid semantics untouched. Pinned by TestAgentReportViolationsNeverSilent covering the exact 2026-07-27 mislabel (task=archived check=true first-pass 5/5, vacuous-test violation). Independent validator verified the invariant at all three sites and the four-case verdict matrix including DISQUALIFIED precedence. Closes the hand-forensics gap the outcome batch orchestrator paid twice.
- B-01KYT9AT0CFBCVG13K6M0R0XCT the bench per-call total is not byte-reproducible across runs, so the reproducibility assertion I shipped in v0.9.0 flakes CI: validated pass by verifier-flake diff 2b9b74891b1f :: C1 REAL: 8x local Run()+30x in full-suite runs never showed Bytes variance (too fast to hit same-ms ID collision locally), but mechanism is real+documented: ids.go MinRecordPrefixLen=13 covers 48-bit ts+15 random bits, same-ms pair collides p=2^-15, forcing adaptive width growth -> explains CI 4162B/4168B. SchemaBytes/ManifestBytes stable acros [body truncated at tombstone retention cap]
- B-01KYTBR3BAF9KAQDD1N06FWX4R the doc comment above TestSchemaMeteringIsRealAndInert still describes the assertion that was removed from it: validated pass by verifier-comment diff 258acfcbd7c5 :: Sentence-checked new comment (bench_test.go:726-737) replacing prior claim. ALL TRUE. <4096 floor + 0-on-fail confirmed in schemaBytes (bench.go:284-361; all error paths return 0). schemaBytes(bin) takes no root param, uses own os.MkdirTemp throwaway root (line 290) - cannot perturb fixture, confirmed. Adaptive-prefix mechanism real: internal [body truncated at tombstone retention cap]
- B-01KYTES575EBVSCCKA08P0BF32 the manifest is static text is imprecise: manifest() appends a runtime-resolved URL from build info: validated pass by verifier-measured diff 8bf52dc3bf9d :: All claims TRUE, verified by measurement. C1: built 3 binaries (plain go build, -buildvcs=false, -buildvcs=false+ldflags injecting v9.9.9-fake into mcpserver.Version); manifest bytes identical 4299B/4299B/4299B; fake version confirmed present in binary via strings but absent from manifest output, manifests diffed byte-identical. C2: read ser [body truncated at tombstone retention cap]
- T-01KYSPFXHNFZ7R35H9ADD09R8Z give the judge harness the real tool descriptions so the 20KB schema surface has a measurable benefit side: validated pass by verifier-bench diff 5cef9287b445
