---
schema: v1
---

## intent
- T-01KYAX81A0EBMTFJQ4J49F66ZE internal/mcpclient: transport selection, tool calls, dense result rendering: internal/mcpclient live: Dial/Call/Instructions/Close over both transports via the go-sdk client, byte-identical rendering proven across stdio and http, go.mod unchanged. Subcommand wiring remains open as a follow-up now that cmd/spectackle is free.
- B-01KYDQTEM9EN8VZN2VA9G9J7H5 TestDialHTTPMatchesStdio flakes under full-suite parallel load, three sightings: Three occurrences across two days, always in the full go test -race ./... run, never in isolation (three consecutive isolated runs pass each time it is checked). The test dials an HTTP server; under parallel suite load the dial or the server startup races a timeout. Suspects: a fixed dial timeout too tight for a loaded machine, or a port-availability race with concurrently running server tests, possibly including a resident dev server on 7412 or 7413 outside the suite. Not yet diagnosed — this record exists because the third sighting makes it a pattern, and a flaky test in the local gate erodes trust in the gate exactly like the fuzz flake did in CI (B-01KYDP precedent: exploratory or flaky steps do not belong in deterministic gates). VERIFY for the eventual fix: the full race suite passes twenty times consecutively on a loaded machine.
- T-01KYE0553VFWMB1NJD9SECFTEC tool refusals over the CLI drop the transport blurb: the refusal text is the signal, the spawn paths are noise: judge A/B on the live metric, four runs, all valid, mechanical scoring: baseline judges A 17 calls 4677B and B 17 calls 3846B; candidate judges D 17 calls 2279B and C 46 calls 5896B. On the straight path the diet dropped 41 to 51 percent at identical call count and equal validity; per-call cost halved across both candidate runs (roughly 250 to 130 bytes per call). Small n, directional not statistical, stated as such. Judge C wandered for reasons the candidate did not cause and thereby earned its keep twice: the fixture leaves root files uncovered so the brief goal check-reports-ok is not literally reachable (B-01KYE0), and elicitation need responses exit zero over the CLI so an unperformed action reads as success (B-01KYE0RCT) — both recorded with fix directions. The judge stage is doing exactly what P-01KYDP built it for: catching guidance failures no scripted driver can experience
- B-01KYE69HSJE8HB7PGDFMHKE6NN schema refusals name only the rejected property, never the accepted shape — the CLI should render the tool schema once on validation failures: verification batch, three fresh tricky judges, briefs hardened against the U2 protocol violation, mechanical scoring: agents n=3 valid=3/3 calls=17/19/21 bytes=2360/2568/2982 — decide refusals down to 1/2/2 per run (fourteen at the motivating U2, six at the luckiest reference run), rule refusals steady at 2, and the tricky reference curve moves from 30/34/38 calls at 3223/3612/4202 bytes to 17/19/21 at 2360/2568/2982: minus forty-four percent calls and minus twenty-nine percent bytes median at full validity. The schema-shape line generalized exactly as the rule-specific pilot predicted — one CLI change covering every tool, taxing only refusals that already failed
