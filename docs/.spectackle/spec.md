---
schema: v1
---

## intent
- T-01KYEB7H5VEHMR76VTEMXDCSPN the judge reference curves become a versioned document, verified by the first fully anchored cross-scenario regression batch: The four reference curves — basic, basic with manifest, tricky, worktree — exist only in archive notes, which future sessions and agents will not reread before their next text change; the measurement discipline (n=3, all-valid gate, spreads quoted, nonce anchors) likewise lives in tribal memory. Change: docs/bench-curves.md records per scenario the goal set, the scoring criteria, the current reference numbers with their source batch, the known noise floor, and the operating discipline including the anchor practice prep prints and -nonces consumes; internal/bench package doc points to it. Verification doubles as content: one fresh cross-scenario regression batch — one basic, one tricky, one worktree judge against current main, scored in one anchored command — both exercises the B-01KYEA anchor end to end for the first time live and supplies the documents baseline table from a single build and day rather than stitched history. VERIFY: go test ./internal/bench/ -count=1 green; the batch results and anchor verification in the archive note and the document; grep of the document for each scenario name and for nonce.
- T-01KYEGD5Y9F2X85Z7Z0KMTDEAH the worktree curve graduates from finding to baseline: 3/3 at 15-19 calls after the pointer and the collision fix: The bench-curves document carries the worktree row as a finding, not a baseline, pending the guidance and collision fixes. Both landed (T-01KYEBT approved-pointer; B-01KYED3D attach fix through three adversarial review rounds) and the anchored rerun delivers the graduation: n=3 valid=3/3 calls=15/17/19 bytes=1742/1799/2023, zero flow shortcuts (rate was two of four pre-pointer, one of three pre-collision-fix), anchors clean, and every judge edited under its worktree root. Change: docs/bench-curves.md worktree row becomes this baseline with the batch date; the finding paragraph compresses to the resolved history naming both fixes; basic and tricky rows take their anchored regression-batch numbers where fresher. VERIFY: grep the document for the new aggregate line; the finding wording no longer claims an open gap.
- T-01KYH1GK27F8PAY8BWC7FNJZ2S catch-rate rerun at n=3 per side: warn vs require on the fixed meter, the first real validation catch rate: validated pass by cross-val-ab2 diff ff8139d2a84d :: [measurement] re-scored all six workspaces from their nonces - the table matches; the 25% rate follows the PRE-PINNED repair definition with no goalpost movement, and the semantic-miss commonality (the same Allow(n<=0) divergence across independent judges) is verified in three of the four 4/5 limiters directly. [integrity] both invalidations are the machinery being right: the vacuous trap caught a genuinely unfailable test, and the r3 judge proved the git-artifact blocker to source before stopping - the strongest possible refusal evidence. [refute] tried: the recurring semantic miss could mean the hidden test is wrong rather than the judges - but the brief deliberately withholds semantics and reasonable people diverged, which is exactly what the fixture measures: the gates blindness to unstated meaning, not judge error. Zero waivers. — The validation gates catch rate is measured: 25% (1/4 valid require-runs repaired), below the estimated bands floor, and the split is the real finding - structural catches only (one premature done repaired 0/5 to 5/5), semantic divergence untouched by every gate round (the same independently-reinvented Allow(n<=0) hardening). The ledger replaces the estimate; warn plus the landed-diff risk gate stays the right global default. Both invalid runs were the machinery being correct: the vacuous trap caught an unfailable race-smoke test in the wild, and a judge forensically traced a real harness bug (B-01KYH3SP, meter artifacts git-tracked blocking every offline merge) to source, refused to work around it, and stopped honestly - twice refusing a tampering-shaped comment. ~1.07M subagent tokens; the most information-dense measurement of the session.
- T-01KYJ4Q8DTFPNVDP42SQ7K5SQE bench ledger: record the offline-collapse A/B - commit-only edges save 198 tokens per lifecycle at equal validity: validated pass by cross-val-ledger diff ff8139d2a84d :: Docs-only append verified against the diff: one table row in the established format, numbers match the recorded A/B output (3558B vs 2765B, delta -793B), reproduction command and per-step breakdown stated, no code touched.
- T-01KYJ58DBAFD798A6HNDWDKAPK outcome judges on the commit-only offline surface: does the new vocabulary navigate as completely as the PR theater it replaced: validated pass by cross-val-judgerun diff ff8139d2a84d :: Method followed the pinned discipline (nonced preps, shim-only judges, all-valid gate honored - the efficiency refusal is recorded instead of a diluted comparison); the ledger entry states validity 1/3 and bounds confidence; the navigation conclusion rests on 3/3 goal completion with zero rounds/asks, which the score tool verified independently of judge self-reports; the four filed B-items each cite their judge evidence.
- T-01KYJN4CCEFMGTGEX2M41HMHJQ dogfood: the bench-curves ledger measurements become first-class bench records: validated pass by validator-heron diff ff8139d2a84d :: Independent fresh-clone validation of PR 185, zero findings. Empirically reproduced from the clone: build+full suite green, gofmt clean; bench ls lists both records with no quarantine; get on M-01KYJWFQ8SE68 stars commit-only on both lower-is-better metrics (2765<3558 B, 691<889 tok) and get on M-01KYJWG08TFRC stars nothing (single impl); find scope=bench resolves both names to their M- IDs; record values match the ledger prose character for character and the 1/3-valid caveat rides the record note rather than being hidden; bench.ndjson lines are valid JSON with canonical folded frames and keys matching name+frame; each record write is its own edge commit with matching trailers, docs in a separate commit, no orphaned state. Pack diff attribution absent (stale served binary) — evidence is the fresh-clone reproduction. — Dogfood delivered (P-01KYJMVX2Q T3, the acceptance fixture): the repository own measurements are first-class bench records. M-01KYJWFQ8SE68 lifecycle-tokens (all-any frame, offline-theater@v0.2.2 3558 B/889 tok vs commit-only@v0.3.1 2765 B/691 tok, both - metrics won by commit-only, tool=spectackle-bench-v3-fixture, T-01KYJ4Q8DT cited) and M-01KYJWG08TFRC outcome-navigation (judge-batch@T-01KYJ58DBA navigated=3 valid=1, the 1/3-valid caveat compacted into the record note with B-01KYJ66VSQ and B-01KYJ67RF9 named). docs/bench-curves.md now states records are the source of truth, the ledger cites both M- IDs, and the prose stays narrative. Round trip proven live and by an independent fresh-clone validator (zero findings): ls, get with direction-correct winner stars, find scope=bench resolution, canonical stored frames, per-record edge commits. This closes P-01KYJMVX2Q: T1 record type (PR 182), T2 tool (PR 183), T3 dogfood (PR 185).
- T-01KYK4876NF2MBZQTMBFRFRN9Y outcome judge batch on v0.5.2: A/B the judge-facing hint fixes against the 2026-07-27 baseline: validated pass by validator-heron diff ff8139d2a84d :: Independent validation of PR 193: the validator RE-RAN agent-score against all three judge workspaces with their positional nonces from a fresh-clone binary and matched every claimed number and verdict line (j1 60/1730/5-5 INVALID vacuous, j2 91/4839/4-5 INVALID vacuous, j3 49/1713/5-5 VALID asks=1); bench get confirms M-01KYK5ETQ2F22 v2 outcome-navigation navigated=3 valid=1 with the v0.5.2 note; the v1-to-v2 journal delta event carries the superseded raw values; the ledger prose matches the re-measured numbers exactly. Zero mismatches. — v0.5.2 outcome judge batch measured and recorded (mirrors the T-01KYJ58DBA protocol: n=3 fresh sonnet judges, outcome scenario, positional nonces). Record M-01KYK5ETQ2F22 = outcome-navigation v2: navigated=3 valid=1, both metric deltas tie at zero against v1. Hypothesis outcomes: (1) CONFIRMED - identity retry loops went from 3/3 judges losing one to zero; j2 hit the WT E refusal once and recovered in one step; (2) CONFIRMED - both voided runs rendered their agent violation line directly in agent-score, no hand forensics; (3) NOT SUPPORTED - validity stayed 1/3: the vacuous-test trap voided j1+j2 again and both also missed the bait-bug goal, so the validity blocker is judge test-writing habits and goal-tracking, not surface hints. Score outputs re-verified independently from the raw workspaces, zero mismatches. New friction filed as B-01KYK5FNM4F3F (SPECTACKLE_AGENT absent from the work tool description; lives-on-main refusal lacks recovery). j2 also exercised the scope guard, reject-widen cycle, submit conflict resolution and rule linting end to end - the tricky-state coverage the mandate asks for, all recoverable from renders alone.
- T-01KYKEJF29EVJVSMJVT3MDAWFK tricky-scenario judge batch on v0.6.0: blocked, decide and reopen states measured with independent agents: validated pass by validator-heron diff ff8139d2a84d :: Independent validation of PR 199: re-ran agent-score for all three tricky workspaces with their nonces from a fresh-clone binary — every number and verdict matches the ledger (18/570 valid, 30/1056 valid, 27/925 valid); bench get confirms M-01KYKEWKMEEWA v1 navigated=3 valid=3 with the v0.6.0 note; the friction claim is a REAL render verified in j3 transcript line 49 (full-length item ID + outcome= field) and the decide schema grep confirms the callable field is choose — the escalation text comes from lifecycle.Escalate, so B-01KYKEWMHEFW1 documents a genuine schema/hint drift. Zero mismatches. — Tricky-scenario judge batch on v0.6.0 measured and recorded: M-01KYKEWKMEEWA tricky-navigation v1, navigated=3 valid=3 - the FIRST fully valid batch of the series and the first coverage of the blocked and decide states by independent judges. All three (18/570, 30/1056, 27/925 calls/tokens) authored a lint-clean rule, drove rounds exhaustion into blocked, resolved the auto-minted ADR via decide rescope, ended check ok - renders alone, zero user asks. Hypotheses: blocked/decide navigation CONFIRMED, agent-side rule slots CONFIRMED (ELICIT-001 holds), verdict honesty untested here (no trap fired in this scenario). One genuine defect found and verified in the raw transcript: the rounds-exhaustion escalation text names the full-length item ID and a decide outcome= field that does not exist (choose is callable; source lifecycle.Escalate) - carried by B-01KYKEWMHEFW1. Validator re-scored all workspaces with zero mismatches. All six lifecycle judge-visible state families are now benchmark-covered: draft/active/done/archived (outcome), rejected (outcome bait), blocked/decide (this batch).
- T-01KYKM21QFF3KAM7MRY30EYBPJ outcome judge batch on v0.6.2: does the in-loop vacuous-test finding move validity off 1/3: validated pass by validator-heron diff ff8139d2a84d :: Independent validation of PR 202 with extra scrutiny on the headline: re-scored all three workspaces from a fresh-clone binary with matching nonces - every number and verdict matches verbatim (39/1016 5-5, 38/1159 4-5, 43/1635 4-5, all valid, rounds=0 asks=0); read each judge actual limiter test file on its workspace main branch and quoted genuine adversarial assertions (negative-n replenishment guard, drain-then-deny tables, fake-clock determinism) - the tests are real, not gamed shapes, race-flag confirmed in meter.log; the v3 record and the v2-to-v3 journal delta verified; exactly one WT E per workspace each recovered from its hint (the one-step claim holds); zero vacuous-trap fires across all transcripts. The 1/3-to-3/3 improvement holds at n=3 with the attribution caveat recorded. — The armed A/B delivered: outcome-navigation v3 (M-01KYKMNFN2EQK) records validity 3/3 on v0.6.2 against 1/3 in both prior batches - d valid Δ+2 better, the record first improvement verdict. The vacuous-test trap fired zero times after voiding 2 of 3 judges twice; the honesty validator re-scored all workspaces, READ the judges test files and quoted genuine adversarial assertions (not gamed shapes - the round-3 detector hardening matters here), confirmed one-step WT reattach recovery per judge and the v2-to-v3 journal delta. Attribution caveat recorded: n=3, single changed variable is the v0.6.2 VAC finding + delegation-aware detector (which also stopped false-flagging helper-based tests). Hypothesis 2 held: rounds=0 asks=0 everywhere. The bait-bug goal was met by all three this time (goal task=archived includes it). New friction carried by B-01KYKMPAFNEW3: two judges read the archive-time VALIDATE W as ambiguous between advisory and actionable. The full mandate loop closed measurably: hint改善 -> benchmark -> gap found -> fixed -> re-benchmarked -> improvement recorded.
- T-01KYKPPZH0E1ABCBC02N8PXT5E worktree-scenario judge batch on v0.6.3: the swarm flow measured with independent agents: validated pass by validator-heron diff ff8139d2a84d :: Independent validation of PR 204: re-scored all three worktree workspaces from a fresh-clone binary with matching nonces - every number and verdict matches (40/1625, 22/617, 44/1516, all four goals green, 3/3 valid); the record and its note verified; exactly one WT E per transcript with next-call recovery; and the j3 bug claim was REPRODUCED empirically - rule op=edit with only id renders ok while the spec file stays byte-identical (hashes equal before and after), so B-01KYKQA6N1FDQ documents real behavior. Zero mismatches. — Worktree-scenario batch measured and recorded: M-01KYKQA5VKF6Y worktree-navigation v1, navigated=3 valid=3 - the second consecutive fully valid batch, completing the scenario matrix (tricky, outcome x3, worktree all judge-covered). All three judges drove start-lease, in-worktree edit and the full submit machinery (gate, integrate, merge, replay) from renders alone at 22-44 calls; each hit the WT identity refusal exactly once and recovered next-call from its hint - the v0.5.3 discoverability fixes hold in the scenario built around identity, and the v0.6.0 diet cost nothing. j3 exceeded the brief by reconciling the semantically contradicted fixture rule via rule op=edit, and in doing so found the batch one real defect: rule op=edit with only id claims ok while writing nothing - reproduced byte-identically by the honesty validator, carried by B-01KYKQA6N1FDQ. Recorded observation: drift is hash-based by contract; semantic rule-vs-code contradiction is validate/review territory, not the healer (j1 expected otherwise). Next: fix the no-op edit, patch release.
