# spectackle v0.2.0

spectackle now drives its own complete development lifecycle: every state
transition is a mechanical, audited git edge, and every judgment — review,
validation, elicitation — is an identity-bound, computed-evidence verdict.
These notes are generated from the archive tombstones of the work itself.

## Delivered proposals

- **P-0084** — proposal langspec engine: end-keyword body spans, so end-terminated languages get real spans and call edges — Per ADR-0012 (engine-endspan) resolving R-0005.
- **P-0085** — proposal langspec Def/Call hardening: close the R-0005 regex misses across all brace-style languages — Per ADR-0012 (engine-endspan) resolving R-0005.
- **P-0086** — proposal hand-written parser fixes: go call-edge coverage, asm linker-suffix symbols, cuda kernel modifiers — Per ADR-0012 resolving R-0005.
- **P-0087** — proposal class-level regression invariants: catch the next instance of each defect family, not just the nine already fixed — Nine defects were found by dogfooding this repository with its own server (B-0001 through B-0…
- **P-01KYCG2J4GE0R** — proposal langspec engine: end-keyword body spans, so end-terminated languages get real spans and call edges — Per ADR-01KYCFH6F8E76RV63TTRG6RP7D (engine-endspan) resolving R-01KYCC8MRRFAMBKA8E6HKPFP7F.
- **P-01KYCG2J4GE3Y** — proposal hand-written parser fixes: go call-edge coverage, asm linker-suffix symbols, cuda kernel modifiers — Per ADR-01KYCFH6F8E76RV63TTRG6RP7D resolving R-01KYCC8MRRFAMBKA8E6HKPFP7F.
- **P-01KYCG2J4GE5Z** — proposal langspec Def/Call hardening: close the R-01KYCC8MRRFAMBKA8E6HKPFP7F regex misses across all brace-style languages — Per ADR-01KYCFH6F8E76RV63TTRG6RP7D (engine-endspan) resolving R-01KYCC8MRRFAMBKA8E6HKPFP7F.
- **P-01KYCT5VV0FT2** — proposal class-level regression invariants: catch the next instance of each defect family, not just the nine already fixed — Nine defects were found by dogfooding this repository with its own server (B-01KYC5ZJ6GF8ZT6M…
- **P-01KYCZ04BRFJF** — proposal globally unique, chronologically sortable record IDs: close the cross-clone collision hole — Requirement: reference IDs can be minted twice when several agents work in parallel; move to chronologically sortabl…
- **P-01KYD87FJREJ5** — proposal turn review from assertion into evidence: run the gate that exists, then make grill and validation compute what they cannot fake — Supersedes the draft it cites in refs, which carried two errors its own valida…
- **P-01KYD87FX0F6Y** — proposal backpropagation: every loop result flows back into the workspace, and the server names the next step so no step can be silently skipped — Supersedes the draft it cites in refs: that draft's scope note carried …
- **P-01KYD8DK52EG5** — proposal close the four open field-reported defects: gitignore, worktree root, degraded index, stale hint — Requirement: fix GitHub issues 26, 27, 28 and 29, in parallel where scope allows.
- **P-01KYD8HSZ0ERT** — proposal the commit log is the decision log: every state-machine edge commits with a structured message, and no merge may flatten the trail — REQUIREMENT.
- **P-01KYD9466KEPW** — proposal review and validation are recorded independent verdicts: grill reviews the draft with feedback and a research path, a validation phase judges the implementation, both bound to reviewer identity — Supersedes th…
- **P-01KYDBRWFZFXS** — proposal the git workflow becomes server automation: state transitions drive branch, push, draft pull request, ready and merge — Requirement: no change may exist unpushed or uncovered; a task in progress is its own bra…
- **P-01KYDF59RYEVY** — proposal total decoupling: the LLM issues no git or forge command, and every one is mechanical in the server — Requirement: the LLM does spec-driven thinking and nothing else.
- **P-01KYDPRXQSF8X** — proposal every MCP text earns its tokens: a benchmark harness scores hint and record variants by tokens against validity, judged by independent agents over tricky fixture repos covering all eight states — Requirement: …
- **P-01KYESGDWFFMH** — proposal requirement elicitation, lens-labeled verdicts under a single-reviewer default, and the panel amendments: closing the vague-requirement gap at minimum token cost (re-record) — RE-RECORD of P-01KYERRMHSFYH8CY01…
- **P-01KYEVSK7BEEZ** — proposal outcome-scored benchmarks: hidden acceptance tests in seeded fixtures make first-iteration completeness a measured number per token — USER REQUIREMENT (2026-07-26): benchmarks must put token usage in relation …

## Features

- **T-0128** — archived with parent P-0087
- **T-0128** — task registry invariants for parsers and Specs, plus the B-0008 kind-gate fix — IMPLEMENTER IN OWN WORKTREE.
- **T-0129** — task tool-surface invariant: no bundle directory may appear outside a legitimate context dir — IMPLEMENTER IN OWN WORKTREE.
- **T-0130** — task worktree submit end to end under the four conditions the old tests never had — IMPLEMENTER IN OWN WORKTREE.
- **T-0131** — archived with parent P-0087
- **T-0133** — task convention: every change lands through a pull request with auto-merge on green CI — Requirement: every change lands through a pull request that merges itself once CI is green, instead of being merged by hand.
- **T-0134** — task ids: mint, encode, parse and adaptively shorten globally unique time-ordered record IDs — IMPLEMENTER IN OWN WORKTREE.
- **T-0135** — task item and lifecycle: accept the new ID shape while legacy sequential IDs stay resolvable — IMPLEMENTER IN OWN WORKTREE.
- **T-0136** — task tool boundary: accept a short ID prefix everywhere an ID is taken, and name ambiguities — IMPLEMENTER IN OWN WORKTREE.
- **T-01KYCG4YA0EZC** — task langspec hardening web: javascript (+NEW test), typescript, dart — FAMILY NOTES: javascript has NO existing _test.go — create it.
- **T-01KYCG4Z98FY8** — task langspec hardening scripting: python (+NEW test), perl, php, shell, r — FAMILY NOTES: python has NO existing _test.go — create it.
- **T-01KYCT7KG8EB2** — archived with parent P-01KYCT5VV0F5ZTSHRNWK7JVM18
- **T-01KYCT7KG8EB2** — task registry invariants for parsers and Specs, plus the B-01KYCQY9SREEMS0KEQQJQ6XPC5 kind-gate fix — IMPLEMENTER IN OWN WORKTREE.
- **T-01KYCT7KG8EF3** — task worktree submit end to end under the four conditions the old tests never had — IMPLEMENTER IN OWN WORKTREE.
- **T-01KYCT7KG8EJM** — task tool-surface invariant: no bundle directory may appear outside a legitimate context dir — IMPLEMENTER IN OWN WORKTREE.
- **T-01KYCT7KG8F8X** — archived with parent P-01KYCT5VV0F5ZTSHRNWK7JVM18
- **T-01KYCXJS68E0A** — task convention: every change lands through a pull request with auto-merge on green CI — Requirement: every change lands through a pull request that merges itself once CI is green, instead of being merged by hand.
- **T-01KYCZ6Q9RE98** — archived with parent P-01KYCZ04BRFJF9AH75QRWMGXPC
- **T-01KYCZ6Q9RFE2** — task item and lifecycle: accept the new ID shape while legacy sequential IDs stay resolvable — IMPLEMENTER IN OWN WORKTREE.
- **T-01KYCZ6Q9RFMK** — task ids: mint, encode, parse and adaptively shorten globally unique time-ordered record IDs — IMPLEMENTER IN OWN WORKTREE.
- **T-01KYCZ8QR8F4M** — archived with parent P-01KYCZ04BRFJF9AH75QRWMGXPC
- **T-01KYD2XQG6E38** — task rule op=edit: recompose from the stored pattern instead of silently rewriting the old text, and stop eating the separator — Fixes GitHub issues 25 and 30 together, because both live in spec.EditRule and one of the…
- **T-01KYD5JB7GF34** — task serialize server-side whole-file rewrites through the coord.db lock table — IMPLEMENTER IN OWN WORKTREE.
- **T-01KYD87ZN7EJ4** — task package-local contract coverage: silent by default with visibility in state, counted by check only under coverage_gate — IMPLEMENTER IN OWN WORKTREE.
- **T-01KYD88KEDEAQ** — task evidence sweeps scoped to an item's targets: declared-but-unconsumed symbols and minority call shapes, with explicit per-symbol suppression — IMPLEMENTER IN OWN WORKTREE.
- **T-01KYD88KV5EX2** — task the backward path in every machine-facing surface: state-computed next steps, archive notes as the training signal, post-merge restart in the loop — IMPLEMENTER IN OWN WORKTREE.
- **T-01KYD88M80EQE** — task research return path enforced at the archive gate: an R-item archives only consumed or explicitly closed — IMPLEMENTER IN OWN WORKTREE.
- **T-01KYD8EDGCEGJ** — archived with parent P-01KYD8DK52EG5A1AKC1KSRR4Z2
- **T-01KYD8F5WGFMC** — archived with parent P-01KYD8DK52EG5A1AKC1KSRR4Z2
- **T-01KYD8GDBKF3F** — archived with parent P-01KYD8DK52EG5A1AKC1KSRR4Z2
- **T-01KYD8HC02EDS** — archived with parent P-01KYD8DK52EG5A1AKC1KSRR4Z2
- **T-01KYD8M955E9N** — task merge policy: never squash - merge commits everywhere the workflow speaks, so the per-edge decision trail survives into main — IMPLEMENTER IN OWN WORKTREE.
- **T-01KYD94KP4FBH** — task grill computes its critique and stamps a verdict; the verdict is an independent review event with feedback and a research-demand path; the fakeable word-checks are deleted — IMPLEMENTER IN OWN WORKTREE.
- **T-01KYD94M3EFXC** — task validate: the post-implementation phase - computed pack over the diff, independent verdict gating archive, findings reopen the item as the implementer's next brief — IMPLEMENTER IN OWN WORKTREE.
- **T-01KYD94MG8FBM** — task edge-commit engine in gate: every tool call that writes .spectackle state commits it with a structured decision message composed from its journal events — IMPLEMENTER IN OWN WORKTREE.
- **T-01KYD9JTSDFYH** — task steps are judgments, automations are implications: the reviewer's verdict is authoritative over computed findings, per-finding, with recorded reasons — IMPLEMENTER IN OWN WORKTREE.
- **T-01KYD9RJTREBE** — task redundancy findings in the validation pack: diff-scoped duplicate-block detection against the graph, so implementations reuse instead of re-writing — IMPLEMENTER IN OWN WORKTREE.
- **T-01KYDA92TGF3F** — task one task, one branch, one pull request: every finished task opens its own PR immediately, never batched — IMPLEMENTER IN OWN WORKTREE.
- **T-01KYDAS36XFHJ** — task exhaustive, deterministic transition matrix over every state pair — Requirement: every state transition to every other state is tested deterministically.
- **T-01KYDBTDF0EG2** — archived with parent P-01KYDBRWFZFXSBRF4PNRD6R4D9
- **T-01KYDBVC9QEZS** — archived with parent P-01KYDBRWFZFXSBRF4PNRD6R4D9
- **T-01KYDBWE98FPZ** — archived with parent P-01KYDBRWFZFXSBRF4PNRD6R4D9
- **T-01KYDEJ4QHEZR** — task a task's referenced GitHub issues close when its work merges — Requirement: an issue referenced by a task closes automatically when that task completes.
- **T-01KYDJC0T9E36** — task done awaits CI like archive does: the LLM waits on the transition while the MCP polls, and the verdict lands in that result — Requirement, refined across four user messages and recorded so the refinement survives:…
- **T-01KYDKNR0SF3X** — task forge transport retries network and 5xx failures with backoff inside a five minute budget before surfacing — User requirement, stated twice in quick succession so it is firm: the MCP retries with backoff for up to…
- **T-01KYDKNR8KF66** — task a reopened task flips its pull request back to draft: the PR draft state mirrors the item state in both directions — User requirement: a pull request that is open but no longer final must return to draft.
- **T-01KYDN4Z88ETH** — task runners fire only at finalization: draft pull requests skip CI, done gates locally before flipping ready, skipped stubs never count as green — Requirement: minimize CI minutes.
- **T-01KYDNNVCRFSD** — task the server recommends and scaffolds a pre-push hook running the local gates, so human pushes are gated like automated ones — User suggestion during the CI-minimization work: git pre-hooks as an MCP-recommended gat…
- **T-01KYDPSW87FKX** — task the harness: generated all-state fixtures, a scripted lifecycle driver over the real tool surface, token and validity metering, A/B against two builds — IMPLEMENTER: the orchestrator, in the open worktree — the …
- **T-01KYDQWEQ5ETH** — task state's rules inventory collapses to its summary: per-dir lines appear only where findings exist, proven by A/B — First text optimization under the benchmark discipline.
- **T-01KYDTQDN1EHC** — task bench fixture v2: multi-dir rules inventory so text-surface changes measure at real magnitude — Motivation, measured: the state rules-inventory collapse (T-01KYDQ) benched at minus 17 bytes on the fixture but minu…
- **T-01KYDYSK4TE5R** — task goreleaser changelog filter excludes spectackle process-commit subjects — Follow-up named by T-01KYD8M9, whose file scope excluded .goreleaser.yaml: the never-squash policy carries server process commits (subjects…
- **T-01KYDZ724PE9P** — task agent-judge stage: bench prepares a metered fixture and brief for an independent agent and scores its run mechanically — Stage 4 of P-01KYDP, the only honest judge of GUIDANCE quality: a scripted driver cannot get…
- **T-01KYE0553VFWM** — task tool refusals over the CLI drop the transport blurb: the refusal text is the signal, the spawn paths are noise — Found by the first live judge runs (T-01KYDZ archive note): every schema or tool refusal reaching a …
- **T-01KYE2DGSDEG5** — task judge scores aggregate across runs: one summary line with validity ratio and calls and bytes spread, so variance is read from n runs, not one — The judge history shows why one run cannot carry a verdict: at the sa…
- **T-01KYE30QAHF00** — task manifest diet: situational guidance moves to an on-demand guide prompt, and bench meters the manifest as its own once-per-session line — The instructions manifest is 7238 bytes read at the START of every MCP sessi…
- **T-01KYE3XEK2EE1** — task manifest-aware judges: agent-prep optionally prepends the connect-time manifest to the brief, and the score reports session cost — The manifest diet (T-01KYE3) re-priced connect-time guidance but nothing measures …
- **T-01KYE4M52DEW8** — task tricky judge scenario: rule slots, a reopen cycle into blocked, and the decide exit — the side states meet an agent — The judge stage covers draft, active-adjacent, archived and rejected; the scripted driver alo…
- **T-01KYE5E0G3EPV** — task the rule need-refusal teaches the whole call shape once, measured against the tricky curve — Wandering analysis of the first tricky judge batch (T-01KYE4 meters): 17 to 22 of 30 to 38 calls per run were refusals �…
- **T-01KYE8783JF4E** — task the items summary renders only non-zero buckets and includes the side states: blocked was invisible in the one line agents read first — Dissecting the state surface after a full lifecycle: the items summary render…
- **T-01KYE9G26FFGM** — task worktree judge scenario: an agent delivers a code change through start, edit under the reported root, and submit — the swarm core meets its judges — With B-01KYE8 the work flow is drivable headlessly, so the las…
- **T-01KYEB7H5VEHM** — task the judge reference curves become a versioned document, verified by the first fully anchored cross-scenario regression batch — The four reference curves — basic, basic with manifest, tricky, worktree — exist o…
- **T-01KYEBTE1PFR3** — task the approved-transition result names the work flow: half the worktree judges shortcut past a flow nothing pointed at — Data from the worktree scenario across two batches: two of four live judges delivered the file…
- **T-01KYEGD5Y9F2X** — task the worktree curve graduates from finding to baseline: 3/3 at 15-19 calls after the pointer and the collision fix — The bench-curves document carries the worktree row as a finding, not a baseline, pending the guid…
- **T-01KYEHVXBGEK1** — task the resident server restarts itself when its binary goes stale: the last manual step after every merge becomes mechanical — Spun off from P-01KYDF as its only unlanded remainder — item four of its gap list: CONT…
- **T-01KYEWT8GMFAQ** — task the manifest teaches memory-to-spec: standing decisions and constraints are cast into rules and decisions, never kept in agent-private memory — USER DIRECTIVE (2026-07-26): agent memory is independent of the speci…
- **T-01KYF3BVBSFG6** — task raise MinRecordPrefixLen to 13: lifetime-stable short prefixes per the ADR-0013 amendment the user decided for v0.2.0 — USER DECISION (ADR-01KYEP, answered 2026-07-26): raise the floor from 6 to 13.
- **T-01KYFPNCXEERJ** — task dup detector must ignore hunk context-only functions; unify short8/shortHash as its proof — OBSERVED (T-01KYD87ZN validation): v dup go:mcpserver.short8 ~= go:mcpserver.shortHash 100% fired although BOTH functions…
- **T-01KYFSN48KEK8** — task role-boundary contract: the server owns every mechanical git step and the bench fails any transcript instructing the agent to run git — IMPLEMENTER IN OWN WORKTREE.
- **T-01KYFSQQ7MFBH** — task planted-completeness outcome fixture: hidden acceptance tests score first-iteration completeness per token and the A/B refuses unequal validity — IMPLEMENTER IN OWN WORKTREE.
- **T-01KYFXBYMZFH7** — task ambiguity findings computed from post-deletion signals, closed by decide round-trip or recorded waiver — IMPLEMENTER IN OWN WORKTREE.
- **T-01KYFXDC6KFNC** — task lens-labeled verdicts under the single-reviewer sequential default, with per-item panel opt-in capped by config — IMPLEMENTER IN OWN WORKTREE.
- **T-01KYFXDCHHFV3** — task validation require is risk-gated from the landed diff: file count and dangerous-path membership, never declared targets — IMPLEMENTER IN OWN WORKTREE.
- **T-01KYFXEPW3FD7** — task waiver-rate tripwire: a computed non-vetoing line in state and packs when waivers dominate recent verdicts — IMPLEMENTER IN OWN WORKTREE.
- **T-01KYFXEQ71F8X** — task verdict events survive compaction and findings render only pre-archive: the token diet closes the loop — IMPLEMENTER IN OWN WORKTREE.
- **T-01KYG0ZX2XERA** — task human-facing git surfaces carry the short display ID: branches, PR titles, commit subjects; machine trailers stay full — IMPLEMENTER IN OWN WORKTREE.
- **T-01KYG90P1EE29** — task node/edge audit: every state hint offers judgment options, no tool result demands mechanical caller work — IMPLEMENTER IN OWN WORKTREE.

## Fixes

- **B-0009** — bug search cache staleness: bundle freshness is decided by mtime and size alone, while the files table's sha column is never written or read — DEFECT note: Bundle freshness now consults the files.sha content hash whene…
- **B-01KYCQY9SRE61** — bug langspec Parse gates body spans and call edges on KFunc/KMethod only, so kernel-originating calls (KKernel) never mint edges — DEFECT note: fixed in T-01KYCT7KG8FD5VG9XKC2SQ37TD: kernel-originating call edges now m…
- **B-01KYCT1VX8F92** — bug search cache staleness: bundle freshness is decided by mtime and size alone, while the files table's sha column is never written or read — DEFECT note: Bundle freshness now consults the files.sha content hash whene…
- **B-01KYD1G9G1EVC** — bug rule op=edit silently discards the edit and answers ok when the pattern slot is omitted — GitHub issue 25.
- **B-01KYD1G9J5EHB** — bug indexer walks gitignored paths, so vendored and virtualenv copies inflate the graph and steal the unsuffixed node ID — GitHub issue 26.
- **B-01KYD1G9KQF87** — bug workspace resolution walks past .git files, so a nested worktree can never be the root and writes land in the parent checkout — GitHub issue 27.
- **B-01KYD1G9PSEH5** — bug a degraded index is invisible through the MCP surface: state answers ok graph after the typed-call pass fails — GitHub issue 28.
- **B-01KYD1G9RAEHW** — bug the stale-binary hint fires on released and packaged binaries, where its advice cannot be followed — GitHub issue 29.
- **B-01KYD1G9SJFE8** — bug rule op=edit drops the blank-line separator before the next rule, the loss accumulates, and lint does not notice — GitHub issue 30, filed alongside issue 25 against the same edit path.
- **B-01KYD4HBHQE9Z** — bug the call subcommand exits 0 on every tool refusal, so a scripted gate cannot detect one — DEFECT note: every pure refusal now carries IsError through one refuse constructor beside text, so the call subcommand exit …
- **B-01KYD4J254FK5** — bug MinRecordPrefixLen's rationale miscounts the encoding, so the six-character floor pins 28 timestamp bits, not 30 — DEFECT
- **B-01KYD57FN3ERH** — bug concurrent draft from two agents silently loses items: work.md is read-modify-written with no cross-process lock — MEASURED, not inferred.
- **B-01KYDDR98HEXE** — bug the draft pull request cannot be opened on entry to active: the branch has no commits yet — DEFECT, found by the automation running against a real forge on its first live transition after merge.
- **B-01KYDE7AX8ES6** — bug Ready reported success while the pull request stayed a draft: REST cannot un-draft — DEFECT, found live on the automation's own pull request 40, which it announced as readied and which GitHub still showed as a draf…
- **B-01KYDGYMDCF2V** — bug records were never committed: the always-covered invariant failed for every .spectackle write — Field report, verbatim question: why are uncommitted files lying in the git repo after an MCP full loop.
- **B-01KYDH5M1AFMP** — bug a mechanical merge that fails at archive has no retry path: archived is terminal — Found live on the automation's own pull request 43.
- **B-01KYDJ0205FR7** — bug zero check runs just after a push reads as no-CI, so the merge gate can fire before CI starts — Observed on the merge gate's first live run, pull request 45.
- **B-01KYDKNQRWF71** — bug automation commits carry a hardcoded spectackle@localhost identity, so they are unverifiable and misattributed — Field report: commits are unverified and the committer email looks wrong.
- **B-01KYDMP34XE8P** — bug unrecoverable git and forge failures reach the LLM as warnings, and coordination emit failures reach nobody — Requirement: every error the MCP cannot fix itself must reach the LLM cleanly — the named example bein…
- **B-01KYDN1BMXFAM** — bug the CI verdict is polled by branch name, so seconds after a push it can belong to the previous head — Observed on the archive of the severity-taxonomy fix: the archive-time records commit was pushed and the merge g…
- **B-01KYDPC0KPFNE** — bug exploratory fuzzing sits inside the deterministic merge gate, and its wind-down flake red-blocked a mechanical merge — Observed live on the SHA-pinned gate's first refusal: pull request 52 went red with context dea…
- **B-01KYDQTEJWET2** — bug expand-built refusals bypassed the IsError sweep and exited zero — Found by the benchmark harness on its first run and fixed inside T-01KYDP's pull request: the B-01KYD4H sweep converted every refusal returned as a…
- **B-01KYDQTEM9EN8** — bug TestDialHTTPMatchesStdio flakes under full-suite parallel load, three sightings — Three occurrences across two days, always in the full go test -race ./...
- **B-01KYDRZHRMER0** — bug writePIDFile is non-atomic: the pidfile is observable empty between create and write — Root cause, confirmed by reading writePIDFile in cmd/spectackle/main.go: the function opens the path with O_CREATE|O_EXCL, then…
- **B-01KYDS0ZXYESB** — bug archived transition for a never-active item pushes a branch that was never created — Reproduced live: moving a draft bug straight to archived (records-only closure, fix already merged elsewhere) emitted g records c…
- **B-01KYDV9PWZFEQ** — bug offline forge Ready mutates memory only: the draft flip never reaches forge-offline.json — Found by the enriched bench fixture (T-01KYDT) during the corrected re-measurement of the B-01KYDS merge: post-merge runs p…
- **B-01KYDY5ZGMEKQ** — bug archive with an existing but not checked-out item branch strands the records commit on the current branch — Second variant of the B-01KYDS stranding family, found live while closing pre-automation done items: B-01K…
- **B-01KYE0RCKEFGB** — bug judge fixture leaves root files uncovered, so the brief goal check-reports-ok is not literally reachable — Found by live judge C: the v2 seeds cover api, api/handlers, store and cli but not the root main.go, so eve…
- **B-01KYE0RCTXFYV** — bug elicitation need responses exit zero over the CLI: an unperformed action reads as success to a shell-driven agent — Found by live judge C: rule op=add with an invalid pattern answers need pattern EARS pattern: U ub…
- **B-01KYE69HSJE8H** — bug schema refusals name only the rejected property, never the accepted shape — the CLI should render the tool schema once on validation failures — Found by the tricky judge rerun for T-01KYE5: judge U2 burned FOURTE…
- **B-01KYE6GCM4EVX** — bug the judge meter is not tamper-evident: an agent that bypasses the shim or rewrites the log scores as cheap instead of disqualified — Found live on judge U2 (T-01KYE5 rerun): its completion notification reported 130…
- **B-01KYE8SQHWFFM** — bug work op=submit cannot reattach to an on-disk worktree from a fresh process, and its refusal denies the worktree exists — Probed headlessly on the judge fixture while designing a worktree judge scenario: work op=sta…
- **B-01KYEAE3A8FPM** — bug the meter shim forwards every subcommand, so an agent can re-prep its own workspace and reset the tamper evidence consistently — Observed on live worktree judge W1: its meter moved backward from fifteen to eleven l…
- **B-01KYED3DRAEBX** — bug gitflow and worktree flows collide on the branch name: an item that entered active can never work-start — Root cause of live judge X2s dead end, reproduced exactly: move to=active runs gitFlowStart, whose EnsureBra…
- **B-01KYEEJKEVF56** — bug abort and orphan adoption ignore DeleteBranch failures, so discarded work can silently resurrect through the attach path — Secondary finding of the adversarial review of B-01KYED3D: workAbort and the orphan-adoptio…
- **B-01KYEEJKQ0FCN** — bug detached-head fast-forward at submit advances no named branch while the result claims merged to main — Minor finding of the same review: when vacateBranch falls through every candidate and detaches the main checkou…
- **B-01KYEPC9SJE23** — bug work op=start resolves items against the primary checkout while state reads the serving worktree, so an item active only on an unmerged branch is visible yet unstartable — Reproduced live: B-01KYD4J254FK5BE486GKFNM…
- **B-01KYERTFRSFVD** — bug no body-edit path exists for draft-state items, so grill feedback cannot amend the record it critiques — Reproduced during P-01KYER: grill surfaced scope-disjointness and rollback questions; the draft tool always m…
- **B-01KYES20VSFJ1** — bug self-restart thrash: 31 exec swaps in 45 minutes, one per poll cycle, instead of one per real source change — Observed live on the resident server (serve.log): exec-replacing count went 4 to 31 between 09:48 and 10…
- **B-01KYES20Y0E9F** — bug self-restart builds and execs the dirty working tree, so the resident server runs unreviewed in-flight code mid-fix — Observed live: while B-01KYEP repairs were mid-flight and adversarial round 2 was still running,…
- **B-01KYF7DJATEED** — bug the committed-only watcher swaps while tool calls are in flight, severing the very edge that moved HEAD — Reproduced twice on the resident server within minutes of the committed-only landing: a records-committing e…
- **B-01KYFG1KEEF1S** — bug re-land the memory-to-spec nudge stranded on the closed PR 121 by the premature post-refusal sync — Incident: T-01KYEW archived with its records riding a close branch (PR 122) while its CODE sat on the item branch …
- **B-01KYFPNCK2E2Q** — bug http CLI path silently drops SPECTACKLE_AGENT for identity-bound verdicts — REPRO: SPECTACKLE_AGENT=cross-val-87zn spectackle call -http <addr> validate op=verdict records the verdict as the SERVER agent (dogfeed-o…
- **B-01KYFSZ7A6FZV** — bug rule composer accepts a trigger already starting with WHEN and emits WHEN WHEN — OBSERVED: seven root rules (ORCH-PROOF-001, ASK-SURFACE-001, AGENT-ISOLATION-001, GROUND-LADDER-001, GATE-AUTHORITY-001, ORCH-SYNC-00…
- **B-01KYG56YQAFD0** — bug archive split the record from the code: closure heuristics discard live work — ROOT CAUSE CONFIRMED (gitflow.go gitFlowMerge, the B-01KYDY guard): when the item branch exists but is not the current branch, the flow…
- **B-01KYGADQPKEZT** — bug archive tombstones the item before the closure merge succeeds; a transient-red CI strands a green PR with no flow actor — OBSERVED (T-01KYG90P closure, 2026-07-27): the archive move journaled the tombstone and remo…

## Decisions

- **ADR-01KYCZ13KRF84** — adr Which ID scheme closes the cross-clone collision hole? — kind: radio note: compact
- **ADR-01KYDBQGMRFBN** — adr Who merges a task's pull request, and by what method? — kind: radio note: decision fully implemented and repeatedly live-proven: forge Merge uses merge_method merge (never squash, 403 degrades to ReasonNoPermission…
- **ADR-01KYDGXWH4FX9** — adr May the git automation ever suppress an action silently? — kind: radio note: never-silent is implemented at every suppression point: gitGate emits its reason on the transitions it suppresses, deferrals carry g reco…
- **ADR-01KYEP4Z5CFGB** — adr MinRecordPrefixLen=6 pins only 28 timestamp bits (~17.5 min window): IDs minted in the same session but >17.5 min apart can share a 6-char prefix, so short prefixes are stable only within that window.
- **ADR-01KYES0TJ0E33** — adr Approve the review-machinery work? The 14 submitted chain items (P-01KYD9 family: grill as recorded independent verdict gating approved, validate phase gating archived, per-finding addressal) plus P-01KYER (elicitati…
- **ADR-01KYES0TMSETQ** — adr Where should validation be a HARD gate (feedback.validate=require)? Token analysis: break-even at ~30-50 percent defect-catch rate; always-on require does not pay for small items.
- **ADR-01KYES0TR8FP7** — adr May a reviewer waive an ambiguity finding without a user round-trip? Recommended: waivable with recorded reason plus computed waive-rate tripwire (ask-only would make automation authoritative over judgment, inverting…
- **ADR-01KYES0TTVERW** — adr Do review/validation verdict events survive journal compaction forever (~5KB per item, permanent audit corpus) or fold after archive? — kind: radio
- **ADR-01KYEVT1G6F2A** — adr Activate the outcome-scored benchmark proposal (hidden acceptance tests measure first-iteration completeness per token, role-boundary contract, catch-rate calibration for the review chain), and where in the queue? �…
- **ADR-01KYF367A2EAE** — adr v0.2.0 release cut: core cut (land addressal PR 105, fix both self-restart bugs, body-edit path, manifest nudge, elicitation gate, release notes and tag - ready in roughly 2-3 days at current pace) or full-chain cut …
- **ADR-01KYF58AKGEZ3** — adr Self-restart rebuild policy for the resident server: today it builds and serves the DIRTY working tree, so mid-implementation the lifecycle runs on unreviewed code (it served the refuted round-1 fix while round 2 was…
- **ADR-01KYFYGVSRFX4** — adr live probe: should the widget cache be bounded — kind: radio note: live ambiguity-probe decision (T-01KYFXBY round-trip proof); the answer yes-bounded was the probe payload, not a real design choice - archived as h…

## Research

- **R-0006** — research cross-clone ID collision reproduced end to end, and the data loss it causes — Empirical reproduction of the defect P-0088 exists to fix, driven through the real MCP server rather than argued from code.
- **R-0007** — research how to make the workflow force multi-perspective analysis, so specifications stop shipping with implementation gaps — PROBLEM note: Four of six lenses reported before the session limit killed the rest; 40 mech…
- **R-01KYCZRTD0FEW** — research cross-clone ID collision reproduced end to end, and the data loss it causes — Empirical reproduction of the defect P-01KYCZ04BRFJF9AH75QRWMGXPC exists to fix, driven through the real MCP server rather than arg…
- **R-01KYD0DRAGFS1** — research how to make the workflow force multi-perspective analysis, so specifications stop shipping with implementation gaps — PROBLEM note: Four of six lenses reported before the session limit killed the rest; 40 mech…
